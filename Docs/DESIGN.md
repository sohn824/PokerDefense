# 포커 디펜스 — 기본 구조 설계

> 이 문서는 **구조 설계**만 다룬다. 수치 밸런싱은 ScriptableObject 데이터로 분리하며 이 문서에 고정하지 않는다.
> 작업 진행 기록은 [HISTORY.md](HISTORY.md), 필요한 이미지 목록은 [ART_REQUEST.md](ART_REQUEST.md) 참조.

---

## 0. 확정된 전제

| 항목 | 결정 |
|---|---|
| 게임 모드 | **싱글 PvE만.** 네트워크 코드 없음 |
| 렌더링 | **2D 스프라이트 (URP 2D Renderer)** |
| 소환 규칙 | **족보 1개 = 유닛 1기** |
| 배치 규칙 | **누적 배치 + 동일 유닛 머지** |
| 엔진 | Unity 6000.4.0f1 / URP 17.4 / Input System 1.19 |
| 타깃 | 모바일 세로 (레퍼런스와 동일한 9:16 기준) |
| 스테이지 | **15웨이브 = 15라운드. 5·10에 보스, 15에 최종 보스.** 무한 진행 없음 |
| 재화 | **Chip.** 스테이지 안에서만 쓰고 종료 시 초기화. 메타 성장 없음 |
| 플레이 시간 | 1스테이지 15~20분 (웨이브당 50~70초) |

**플레이어가 외워야 할 관계는 두 단계뿐이다.** 족보 → 어떤 유닛이 나오는가, 유닛 → 어떤 전투 역할인가. 이 원칙을 지키기 위해 카드 무늬·숫자에 따른 유닛 속성, 카드 강화, 덱빌딩, 장비, 속성 상성, 액티브 스킬, 상태이상은 넣지 않는다. 카드는 끝까지 **족보를 만드는 재료** 역할에만 집중한다.

---

## 1. 라운드 루프

```
        ┌──────────────────────────────────────────────┐
        ↓                                              │
 [Draw] → [Exchange] → [Evaluate] → [Place] → [Combat] → [Result]
   5장     자리당 1회    족보 판정    배치/머지   웨이브     클리어 판정
                                                          │
                                                          └→ 실패 시 [GameOver]
```

각 페이즈의 책임과 종료 조건:

| 페이즈 | 하는 일 | 종료 조건 |
|---|---|---|
| `Draw` | 52장 덱을 셔플해 5장 드로우 | 즉시 |
| `Exchange` | 카드를 골라 교체. **여러 번 가능하되 한 자리는 한 번만** | 플레이어 확정 입력 |
| `Evaluate` | 최종 5장 → 족보 판정 → 유닛 1기 결정 | 즉시 |
| `Place` | 소환된 유닛을 그리드 슬롯에 배치. 동일 유닛끼리 머지 | 플레이어 확정 입력 |
| `Combat` | 웨이브 스폰, 유닛 자동 전투, 제한시간 카운트 | 적 전멸 or 시간 초과 |
| `Result` | 클리어/실패 판정, 보상 정산 | 즉시 → 다음 `Draw` |

**라운드 N은 웨이브 N을 치른다.** 둘은 1:1이라 HUD는 라운드만 보여준다.

**자동으로 넘어가는 구간과 플레이어 입력이 필요한 구간이 갈린다.** `GameFlowController`는 자동 구간만 맡는다.

| 전이 | 방식 |
|---|---|
| `Draw` → `Exchange` | 자동 (드로우 즉시) |
| `Exchange` → `Evaluate` | **플레이어** — 확정 버튼 |
| `Evaluate` → `Place` | 자동 (판정 즉시 소환) |
| `Place` → `Combat` | **플레이어** — 전투 시작 버튼 |
| `Combat` → `Result` | 자동 (전멸 or 시간 초과) |
| `Result` → 다음 `Draw` | 자동 |

**`Combat`으로 넘어가려면 두 조건을 모두 만족해야 한다.** 페이즈가 순차적·배타적이기 때문이다.

- `Place` 페이즈여야 한다. 교체 도중에 전투로 뛰면 그 라운드의 소환을 통째로 건너뛴다.
- 배치 대기 유닛이 없어야 한다. 남겨두고 넘어가면 다음 라운드의 소환이 덮어써서 조용히 사라진다.

**설계 결정: 교체는 자리 단위로 잠근다.** 라운드 안에서 몇 번이든 교체를 누를 수 있지만, 한 번 바꾼 자리는 그 라운드에 다시 못 바꾼다. 결과적으로 최대 교체 장수는 5장으로 "한 번에 몰아서 5장까지"와 같다. 나눠 바꾸게 한 이유는 **바뀐 결과를 보고 다음 선택을 하게** 만들기 위함이다. 한 장씩 열어보는 편이 기대감이 크다.

부작용 하나: 중간 정보를 보고 판단하므로 플레이어가 약간 더 강해진다. §7 열린 이슈 1번(저등급 편중)에는 유리한 방향이라 지금은 그대로 둔다.

**설계 결정: 페이즈는 순차적이고 배타적이다.** 레퍼런스 게임은 전투 중에도 소환이 가능하지만, 요청하신 플로우("라운드를 클리어하면 다시 카드 뽑기 플로우로")를 그대로 따른다. 나중에 동시 진행으로 바꾸려면 `Place`를 `Combat`과 병렬 상태로 승격시키면 되고, 그 외 시스템은 영향받지 않는다.

---

## 2. 어셈블리 / 레이어 구조

**asmdef를 쓰지 않는다.** 모든 코드가 Unity의 predefined assembly에 들어간다.

```
Assembly-CSharp           ← 게임 코드 전부 (Poker/Game/UI)
        ↑
Assembly-CSharp-Editor    ← Editor/ 폴더 밑. EditMode 테스트
```

`Assembly-CSharp-Editor`는 `Assembly-CSharp`을 자동으로 참조하므로 테스트가 게임 코드를 볼 수 있다. 에디터에서 폴더만 만들면 되고 설정할 것이 없다.

### 레이어는 폴더와 네임스페이스로만 표현한다

```
PokerDefense.Poker      ← 포커 규칙 전용
        ↑
PokerDefense.Game       ← 런타임 전체 (플로우/보드/유닛/적/데이터 SO)
        ↑
PokerDefense.UI         ← 카드 UI, HUD, 팝업
```

- `Poker`는 `Game`을 모른다. `Game`은 `UI`를 모른다 (UI가 `Game`을 구독).
- **이 방향을 컴파일러가 강제하지 않는다.** 한 어셈블리이므로 어느 쪽이든 서로를 참조할 수 있다. 지키는 것은 전적으로 관례다.

### 관례로 지킬 것

- **역방향 참조 금지.** `Poker` 네임스페이스 안에서 `PokerDefense.Game`/`PokerDefense.UI`를 `using` 하지 않는다. `Game`에서 `PokerDefense.UI`를 `using` 하지 않는다.
- **셔플에 `UnityEngine.Random`을 쓰지 않는다.** 전역 정적 상태라 인스턴스별 시드를 잡을 수 없고, 그러면 `Deck(int seed)`의 재현성 테스트가 성립하지 않는다. `System.Random` 주입을 유지한다.
- **`Poker`에 ScriptableObject·MonoBehaviour를 두지 않는다.** 족보 → 유닛 매핑처럼 게임 밸런스에 속하는 것은 `Game`의 `HandUnitTable`이 담당한다.

컴파일러가 막아주지 않으므로 이 세 줄이 유일한 방어선이다. 어겨도 빌드는 통과한다.

### 테스트가 asmdef 없이 동작하는 근거

테스트 러너는 asmdef를 보지 않고 **`nunit.framework`를 참조하는 로드된 어셈블리**를 스캔한 뒤 `AssemblyFlags.EditorOnly`면 EditMode로 분류한다. **테스트 폴더 이름이 정확히 `Editor`여야 한다.** (근거: Test Framework의 `EditorLoadedTestAssemblyProvider` / `FolderPathTestCompilationContextProvider`)

**포기한 것:** `[UnityTest]`·`LogAssert` 등 `UnityEngine.TestTools`(순수 NUnit만 가능), PlayMode 테스트, 레이어 의존의 컴파일러 강제. 필요해지면 테스트용 asmdef 하나만 되살리면 된다.

### 폴더 구조

```
Assets/_Project/
├── Art/            Sprites/, UI/, VFX/   (생성형 AI 산출물 배치처)
├── Audio/
├── Data/           ScriptableObject 에셋 인스턴스 (Units/, Enemies/, Waves/)
├── Prefabs/        Units/, Enemies/, Projectiles/, UI/
├── Scenes/         Boot.unity, Game.unity
└── Scripts/
    ├── Poker/          namespace PokerDefense.Poker
    ├── Game/           namespace PokerDefense.Game
    │   ├── Flow/       RoundPhase, RoundContext, RoundController,
    │   │               CombatContext, CombatController, StageContext,
    │   │               StageController, SupportSummon, GameFlowController
    │   ├── Board/      GridBoard, PlacementController
    │   ├── Units/      UnitInstance
    │   ├── Enemies/    EnemyInstance
    │   └── Data/       UnitDefinition, HandUnitTable, EnemyDefinition,
    │                   WaveDefinition, StageDefinition, EconomyDefinition
    ├── UI/             namespace PokerDefense.UI
    └── Tests/Editor/   → Assembly-CSharp-Editor (폴더명이 Editor여야 함)
```

---

## 3. 포커 코어 (`PokerDefense.Poker`)

### 3.1 카드

```csharp
public enum Suit  { Spade, Heart, Diamond, Club }        // 4
public enum Rank  { Two = 2, ..., Ten = 10, Jack, Queen, King, Ace = 14 }  // 13

public readonly struct Card { public readonly Suit Suit; public readonly Rank Rank; }
```

- `Ace = 14` 로 두고, 백스트레이트(A-2-3-4-5) 판정 시에만 A를 1로 재해석한다. 조커 없음 → 52장.

### 3.2 덱

```csharp
public sealed class Deck
{
    public Deck(int seed);      // System.Random 시드 주입 → 테스트 재현 가능
    public Card Draw();
    public int Remaining { get; }
}
```

**설계 결정: 덱은 라운드마다 새로 셔플한다.** 라운드 간 덱을 이어가면 카드 카운팅이라는 전략 축이 생기지만, 후반 라운드에서 덱 고갈 처리·리셋 타이밍 등 규칙이 붙는다. 지금은 라운드 독립이 단순하다. (교체는 같은 덱에서 뽑으므로 **한 라운드 안에서는 중복 카드가 나오지 않는다.**)

### 3.3 족보 판정

```csharp
public enum HandCategory
{
    HighCard, OnePair, TwoPair, ThreeOfAKind,
    Straight, Flush, FullHouse, FourOfAKind, StraightFlush,
    // 특수 (기본 카테고리의 하위 분류를 별도 승격)
    BackStraight,        // A-2-3-4-5
    Mountain,            // A-K-Q-J-10
    BackStraightFlush,   // A-2-3-4-5 동일 무늬
    RoyalStraightFlush,  // A-K-Q-J-10 동일 무늬
}

public readonly struct HandResult
{
    public readonly HandCategory Category;
    public readonly IReadOnlyList<Card> KeyCards;   // 연출용 하이라이트 (페어 2장 등)
}

public static class HandEvaluator
{
    public static HandResult Evaluate(IReadOnlyList<Card> five);
}
```

**주의: `HandCategory`의 enum 순서를 "강함"으로 쓰지 않는다.** 백스트레이트는 정통 포커에서 가장 약한 스트레이트지만 이 게임에서는 특수 유닛을 준다. 즉 **판정 결과(Category)와 보상 세기(유닛)는 별개 축**이며, 연결은 코드가 아니라 데이터 테이블(`HandUnitTable`)이 담당한다. 밸런싱을 코드 수정 없이 바꾸기 위함이다.

**참고 — 5장 드로우 시 등장 확률** (밸런싱 시작점):

| 족보 | 확률 |
|---|---|
| 하이카드 | 50.1% |
| 원페어 | 42.3% |
| 투페어 | 4.75% |
| 트리플 | 2.11% |
| 스트레이트(마운틴·백스 제외) | 0.31% |
| 플러시 | 0.197% |
| 풀하우스 | 0.144% |
| 마운틴 / 백스트레이트 | 각 0.039% |
| 포카드 | 0.024% |
| 스트레이트 플러시(특수 제외) | 0.00123% |
| 로열 / 백스트레이트 플러시 | 각 0.000154% |

→ **90% 이상이 하이카드·원페어로 몰린다.** 교체 1회로 이 분포가 다소 완화되지만, 저등급 유닛 위주 전개가 기본값이 된다. 대응책은 §7의 열린 이슈 참조.

### 3.4 테스트 (M1의 성공 기준)

`Scripts/Tests/Editor/`에서 13개 카테고리 각각의 대표 핸드 + 경계 케이스를 검증한다.
- 마운틴이 `Straight`가 아니라 `Mountain`으로 잡히는가
- 백스트레이트가 `Straight`로 잡히지 않는가 (A를 1로 해석)
- K-A-2-3-4 는 스트레이트가 **아닌가** (랩어라운드 불허)
- 로열 스트레이트 플러시가 `Flush`/`Mountain`/`StraightFlush` 어디로도 새지 않는가

**이 테스트가 전부 통과하는 것이 포커 코어의 완료 조건이다.**

---

## 4. 데이터 (ScriptableObject)

수치는 전부 여기로 뺀다. 코드에는 하드코딩된 밸런스 값을 두지 않는다.

| SO | 필드 | 상태 |
|---|---|---|
| `UnitDefinition` | id, 표시명, 플레이스홀더 색, 공격력, 공격속도, 사거리, 성급 배수(★1/★2/★3) | M3 완료 |
| `HandUnitTable` | `HandCategory → UnitDefinition` 매핑 1개 (에셋 1개, 전역) | M3 완료 |
| `EnemyDefinition` | id, 표시명, 플레이스홀더 색, 최대HP, 이동속도, 타입(일반/다수/보스) | M4 |
| `WaveDefinition` | 스폰 엔트리 리스트(적, 수량, 간격), 제한시간, 웨이브 번호 | M4 |
| `StageDefinition` | `WaveDefinition` 순서 리스트, 시작 라이프 | M4 |

**아직 넣지 않은 필드가 있다.** 쓰이지 않는 필드를 미리 두면 추측 코드가 된다.

- **스프라이트 대신 플레이스홀더 색.** M0~M5는 도형·색으로 진행하고 M6에서 스프라이트로 교체한다.
- **타겟팅 방식·투사체 프리팹 없음.** M4는 타겟팅 1종(사거리 내 가장 앞선 적)과 즉시 히트로 시작한다. 두 번째 방식이 실제로 필요해질 때 필드를 만든다.
- **처치 보상 필드 없음.** 적 처치로는 Chip을 주지 않기로 했다 (§9.2). 웨이브 구성만 바꿔도 경제가 흔들리는 것을 막기 위함이다.

---

## 5. 전투 (`Combat` 페이즈)

### 5.1 보드

- 중앙 **5열 × 3행 = 15슬롯** 그리드. 슬롯 하나에 유닛 하나.
- 적은 그리드를 감싸는 **고정 웨이포인트 트랙**을 순회한다. 유닛은 사거리 안에 들어온 적을 자동 공격한다.
- `GridBoard`가 점유 상태와 배치·머지 규칙을 소유한다. 슬롯은 `UnitInstance[15]`이고 인덱스로만 다룬다.
- 유닛/적은 서로를 직접 찾지 않고 활성 적 목록을 통해 조회한다 — 매 프레임 `FindObjectsOfType` 호출을 막기 위함. 이 목록은 `CombatContext`가 직접 들고 있으며 별도 클래스로 빼지 않는다.

**보드는 월드 스페이스다.** 카드 UI와 HUD만 uGUI로 남는다. 적이 월드를 이동하고 유닛이 사거리로 조준하므로, 보드가 캔버스에 있으면 사거리를 픽셀로 다뤄야 하고 URP 2D 렌더러·2D 물리·스프라이트 도구를 쓸 수 없다.

슬롯 클릭은 `Pointer` → `ScreenToWorldPoint` → `Physics2D.OverlapPoint`로 잡고, 카드 UI 위 클릭은 `EventSystem.IsPointerOverGameObject()`로 거른다. 누를 수 없는 칸은 `hitbox.enabled = false`로 검사에서 뺀다.

### 5.2 머지

**설계 결정: 머지는 티어 상승이 아니라 "성급(★) 상승"이다.**
동일 `UnitDefinition` + 동일 성급 유닛 2기 → 같은 유닛의 ★+1 (최대 ★3). 스탯은 `UnitDefinition`의 성급 배수를 적용한다.

대안으로 "동일 티어 2기 → 상위 티어 1기"가 있지만, 그러면 *하이카드 2기 = 원페어 1기*가 되어 족보를 맞춘 가치가 희석된다. 족보 축(유닛 종류)과 머지 축(성급)을 직교시키는 편이 낫다.

**머지 경로는 두 가지다. 둘 다 있어야 한다.**

1. **소환 유닛 → 보드**: 배치할 때 같은 유닛·같은 성급 칸을 고르면 합쳐진다.
2. **보드 → 보드**: 이미 놓인 두 유닛을 합친다 (`GridBoard.TryMergeSlots`). 재료 쪽 칸이 비워진다.

2번이 없으면 **★3에 도달할 수 없다.** 소환되는 유닛은 항상 ★1이라 ★2에 얹을 수 없고, 보드 위의 ★2 두 기를 합칠 방법이 사라지기 때문이다. UI에서는 유닛을 탭해 고르고 상대를 탭해 합친다.

성급은 공격력·공격속도에만 곱해지고 **사거리에는 곱해지지 않는다.** 성급으로 사거리까지 늘면 배치 위치를 고르는 의미가 줄어든다.

### 5.2.1 Joker — 희귀 유닛의 유일한 성장 수단

**Joker는 아무 유닛의 성급을 한 단계 올린다.** 동일 유닛이 필요 없다.

- 획득: **Wave 5 보스 ×1, Wave 10 보스 ×1.** 한 스테이지에 최대 2개. Wave 15는 게임 종료라 주지 않는다.
- ★1 → ★2, ★2 → ★3. ★3에는 쓸 수 없다.

포카드는 0.024%, 로열 스트레이트 플러시는 0.000154%다. ★3에 같은 유닛 4기가 필요하므로 중복 소환만으로는 도달할 수 없고, **희귀 족보를 뽑은 기쁨이 "어차피 못 키우는 ★1"로 끝난다.** Joker가 이 구멍을 메운다.

공급을 스테이지당 2개로 고정한 이유는 통제 때문이다. Chip으로 같은 유닛을 복제 구매하는 안도 검토했으나, 로열 스트레이트 플러시 복제에 값을 매기는 순간 싸면 착취되고 비싸면 무의미해진다. 보스 드롭은 그 문제가 없다.

Joker를 어디에 쓸지가 그 자체로 선택이 된다 — 지금 주력인 원페어 ★2를 ★3으로 만들 것인가, 방금 뜬 풀하우스를 ★2로 만들어 길게 쓸 것인가.

### 5.3 트랙과 적 이동

**트랙은 닫힌 루프다.** 적은 골에 도달해 빠져나가지 않고 죽을 때까지 계속 돈다. §5.4대로 라이프는 "적이 새어나갔을 때"가 아니라 "제한시간이 끝났을 때 잔여 적 수만큼" 깎이기 때문이다. 일반적인 타워 디펜스와 다른 지점이다.

- **웨이포인트를 씬 오브젝트로 두지 않는다.** 그리드를 감싸는 사각 루프라 `GridBoard`가 그리드 크기에서 계산하면 된다. 씬에서 점을 찍어 관리할 이유가 없다.
- **적 위치는 트랙 위 진행도(0~1) 하나로 표현한다.** 월드 좌표는 화면에 그릴 때만 변환한다. 상태가 순수 C#으로 유지되고, "가장 앞선 적"이 진행도 비교 한 번으로 끝난다.

### 5.4 클리어 판정

- 제한시간 내 웨이브 전멸 → 클리어, 다음 라운드.
- 시간 초과 → 잔여 적 수만큼 라이프 감소. 라이프 0 → `GameOver`.
- 잔여 적은 다음 웨이브로 넘기지 않고 제거한다 (누적으로 인한 데스 스파이럴 방지).

**한 웨이브의 최대 피해는 5로 제한한다.**

```
LifeDamage = Min(RemainingEnemies, 5)
```

시작 라이프는 15다. 상한이 없으면 Swarm 웨이브(적 10기 이상)를 한 번 놓치는 순간 게임이 즉시 끝난다. 실수 한 번이 곧 패배가 되면 보드를 실험해 볼 여지가 사라진다. 보스는 남아 있으면 5로 친다.

### 5.5 타겟팅과 공격

- **타겟은 사거리 안에서 가장 앞선 적** 하나다. M4는 이 한 종류만 만든다.
- **공격은 즉시 히트다.** 투사체는 비행 시간과 빗나감 처리가 따라붙는데 클리어 판정과 무관하다. 연출 단계에서 붙인다.
- 성급 배수는 공격력·공격속도에 곱해지고 사거리에는 곱해지지 않는다 (§5.2).

### 5.6 시뮬레이션 방식

PvE 전용이므로 결정론적 고정 틱을 도입하지 않는다. 일반적인 `Update` + `deltaTime` 으로 충분하다. (PvP를 나중에 붙일 경우 이 결정을 재검토해야 하며, 영향 범위는 `Units/`·`Enemies/`에 한정된다.)

**단, `deltaTime`은 인자로 받는다.**

```csharp
public sealed class CombatContext          // 순수 C#. MonoBehaviour가 아니다
{
    public void Tick(float deltaTime);     // Update()에서 Time.deltaTime을 넘긴다
    public CombatOutcome Outcome { get; }  // InProgress / Cleared / TimedOut
    public int RemainingEnemies { get; }
}
```

고정 틱을 강제하지 않으므로 위 결정과 충돌하지 않으면서, 테스트에서 `Tick(0.1f)`를 원하는 만큼 돌려 전투 한 판을 통째로 시뮬레이션할 수 있다. **M4의 검증 조건이 그대로 EditMode 테스트가 된다.** 포커 코어·라운드·보드와 같은 방식이다.

---

## 6. 씬 구성

- `Boot.unity` — SO 로드, 진입점. 곧바로 `Game` 로드.
- `Game.unity` — 보드, 그리드, UI 캔버스, `GameFlowController` 전부 포함.

씬은 2개면 충분하다. 타이틀/메타 진행은 지금 범위 밖.

---

## 7. 열린 이슈

### 해결 방향이 정해진 것

| # | 이슈 | 해결 |
|---|---|---|
| 1 | **저등급 편중** — 라운드당 유닛 1기 중 90%가 하이카드·원페어라 머지 재료가 안 모인다 | Chip 지원 소환 (M6, §9.3). 포커는 "고등급 확정 소환", Chip은 "머지 재료 수급" |
| 2 | 그리드가 가득 찼을 때 | 유닛 판매 (M6, §9.2). 보상 없이 버리는 임시 처리를 없앤다 |
| 3 | Chip의 용도 | 지원 소환 하나로 시작 (M6). 상점·장비·강화·리롤 없음 |
| 4 | 스테이지 길이 | 15웨이브 유한 스테이지 (M7, §9.5). 무한 진행은 나중에 |
| 5 | 희귀 족보 유닛의 성장 수단 | Joker (M7, §5.2.1) |

**1번 실측 근거:** M3에서 스카우트 4기를 모으는 데 **13라운드**가 걸렸다(★3 하나에 같은 유닛 4기 필요). M5에서도 3라운드에 소환 3기, 보드에 2기만 쌓였다.

### 아직 정해지지 않은 것

6. **유지 보너스가 운 좋은 판을 이중 보상한다.** 좋은 패는 교체할 이유가 없어 Chip 4개를 공짜로 받는다. 1번은 운 나쁜 판을 구제하려는 건데 방향이 반대다. 나쁜 패도 "포기하고 Chip"을 고를 수 있어 바닥은 생기지만 천장도 올라간다.
   → 수치로 조절 가능해 M6을 막지 않는다. 상한이 4라 폭주하지도 않는다. **밸런싱(M10) 때 확인.**

### 폐기한 것

**카드 특성** (무늬·숫자가 유닛 능력을 결정) — 채택하지 않는다. 13종 × 조합의 밸런싱 비용, 같은 이름의 유닛이 매번 다른 성능을 갖는 학습 비용, 그리고 **머지 규칙과의 충돌**(특성을 매칭에 포함하면 머지가 사망하고 무시하면 특성 선택이 무의미) 때문이다. 목적이었던 "낮은 족보에도 고민 만들기"는 **유지 보너스(§9.1)가 대신한다.**

---

## 8. 마일스톤

각 단계는 **검증 조건을 통과해야** 다음으로 넘어간다.

| # | 내용 | 검증 조건 |
|---|---|---|
| M0 | 폴더 뼈대, URP 2D 렌더러 설정 | 빈 씬이 에러 없이 실행됨 |
| M1 | 포커 코어 (Card/Deck/HandEvaluator) | §3.4의 EditMode 테스트 전부 통과 |
| M2 | 카드 UI + 드로우/교체 플로우 | 5장 드로우 → 자리당 1회 교체 → 족보명 화면 표시 |
| M3 | 그리드 + 배치 + 머지 | 소환된 유닛 배치 → 동일 유닛 2기 머지 시 ★2 및 스탯 상승 확인 |
| M4 | 웨이브 + 전투 + 클리어 판정 | 제한시간 내 전멸 시 클리어, 초과 시 라이프 감소 |
| M5 | 라운드 루프 결합 + HUD | 라운드 3회 연속 진행이 끊김 없이 동작 |
| M6 | **Chip 경제** (§9.1~9.4) | 교체를 포기하고 Chip을 챙기는 선택이 성립. **스카우트 4기 수급이 13라운드 → 4~7라운드로 내려옴** |
| M7 | **15웨이브 + 보스 + Joker** (§9.5, §5.2.1) | 15웨이브 완주, 보스에서 Joker 획득, 클리어·게임오버 후 재시작 가능 |
| M8 | **유닛 역할 + 적 타입** (§10) | 같은 총 DPS라도 보드 구성에 따라 클리어 여부가 갈림 |
| M9 | **Dealer Perk** (§11) | 보스마다 3중 택1, 런마다 플레이 방식이 달라짐 |
| M10 | 아트 교체 + 밸런싱 데이터 채우기 | 플레이스홀더 스프라이트가 전부 대체됨 |

M0~M9는 **플레이스홀더 도형/색상 스프라이트**로 진행한다. 생성형 AI 이미지는 M10에서 한꺼번에 교체하며, 필요 목록은 [ART_REQUEST.md](ART_REQUEST.md)에 정리해 두었다.

### M6~M9의 순서를 이렇게 잡은 이유

M5까지는 "게임이 돌아가는가"였고, M6부터는 **"왜 다음 라운드를 하고 싶은가"**다. 지금 플레이어가 실제로 고민하는 지점은 카드 교체와 배치·머지 둘뿐이라, 시스템을 늘리기보다 이 둘을 깊게 만드는 쪽으로 간다.

순서는 취향이 아니라 **의존 관계**로 정했다.

```
M6 Chip ──→ 유지 보너스
   │
   └──→ 유닛 판매 ──→ §7-2(보드 가득참) 해결

M7 보스 ──────────────→ M9 Dealer Perk

M8 유닛 역할 ──→ 적 타입   (역할이 갈리기 전에는 적 타입이 무의미)
```

- **M6이 먼저인 이유:** 가장 싸고 §7의 1·2·3번을 한꺼번에 닫는다. `RoundContext.ExchangeableCount`가 곧 미사용 교체권이라 보상 계산에 새 상태가 필요 없다.
- **M7이 M8보다 먼저인 이유:** Chip 경제는 여러 라운드에 걸쳐 쌓여야 체감된다. 3웨이브로는 축적이 안 되고 지원 소환을 몇 번 못 써본다. 재시작 수단도 여기서 붙는다(M5 현재 없음).
- **적 타입이 M8에 묶인 이유:** 타겟팅이 "사거리 내 가장 앞선 적" 한 종류인 동안에는 어떤 적이 나와도 "총 DPS가 충분한가"로 귀결된다. 유닛 역할이 갈려야 적 타입이 답이 있는 문제가 된다.
- **M9가 마지막인 이유:** Perk는 `RoundContext`·`GridBoard`·`CombatContext`를 가로질러 규칙을 바꾼다. 수정자를 어디에 두는지 정하지 않고 첫 Perk를 짜면 곳곳에 `if`가 흩어진다.

### 코드를 실제로 건드리는 지점

기획 대부분은 데이터·UI지만 아래는 기존 코드를 고친다.

| 항목 | 현재 | 필요 | 시점 |
|---|---|---|---|
| 교체 단계 족보 미리보기 | 확정 후에만 표시 | 교체 중 실시간 표시. **§9.1의 전제조건** | M6 |
| 유닛 이동 | 미지원 | `GridBoard.TryMoveSlot` | M6 |
| 라이프 감소 상한 | `Life - unresolved` | `Life - Min(unresolved, 5)` | M7 |
| 적 타입 Runner·Tank | enum에 없음 | **enum 값 추가만.** 분기하는 코드가 0곳이라 나머지는 에셋 | M8 |
| 공격 패턴 | 단일 타겟 즉시 히트 1종 | Rapid·Heavy·Multi·Splash·Pierce. M4에서 미룬 타겟팅 enum이 여기서 필요해진다 | M8 |
| Perk 수정자 | 없음 | 여러 클래스를 가로지르는 수정자를 어디에 둘지 **먼저 정한다** | M9 |

라이프 상한은 **기존 테스트 3개를 깨지 않는다.** 그 테스트들의 잔여 적이 전부 5 이하라 결과가 같다. 새 테스트만 추가하면 된다.

---

## 9. 스테이지와 경제 (M6~M7)

### 9.1 유지 보너스 — 이 게임의 핵심 선택

교체를 **덜 쓸수록** Chip을 받는다.

| 사용한 교체 | Chip |
|---|---|
| 0장 | +4 |
| 1장 | +3 |
| 2장 | +2 |
| 3장 | +1 |
| 4~5장 | 0 |

`Chip = Max(0, 4 - 사용한 교체 수)`. 사용한 교체 수는 `5 - RoundContext.ExchangeableCount`라 **새 상태가 필요 없다.**

이것이 교체를 "공짜 리롤"에서 **"현재 이득을 포기하고 더 높은 족보에 거는 행동"** 으로 바꾼다. 게임 이름값을 하게 만드는 지점이다.

> **전제조건: 교체 단계에서 현재 족보와 소환될 유닛을 실시간으로 보여줘야 한다.**
> "확정하면 원페어 = 건슬링어 + Chip 4개"를 모르면 유지 보너스는 감으로 찍는 도박이 된다.
> `HandEvaluator.Evaluate`는 순수 함수라 교체 중 아무 때나 현재 손패로 부를 수 있다. 비용은 사실상 없고, **이게 없으면 §9.1 전체가 성립하지 않는다.**

### 9.2 Chip

스테이지 안에서만 쓰고 종료 시 초기화한다. 메타 성장 없음.

| 획득 | 사용 |
|---|---|
| 유지 보너스 | **지원 소환 6 Chip** |
| 유닛 판매 (★1/★2/★3 = 1/2/4) | |
| 일부 Dealer Perk · 보스 보상 | |

**적 처치로는 Chip을 주지 않는다.** 처치 하나하나가 경제와 엮이면 웨이브 구성만 바꿔도 경제가 흔들려 밸런싱이 급격히 어려워진다. 덕분에 `EnemyDefinition`에 보상 필드를 추가할 필요도 없다.

판매 가격은 **유닛 종류와 무관하게 성급으로만 정한다.** 고급 족보라고 판매가까지 외우게 하지 않는다.

### 9.3 지원 소환

6 Chip → 랜덤 하위 유닛 ★1 한 기. 포커를 거치지 않는다.

| 등장 | 확률 |
|---|---|
| 하이카드 유닛 | 40% |
| 원페어 유닛 | 30% |
| 투페어 유닛 | 20% |
| 트리플 유닛 | 10% |

스트레이트 이상은 지원 소환에 나오지 않는다. **라운드당 최대 2회**로 제한한다 — 제한이 없으면 Chip을 모아 한 번에 쏟는 것이 정답이 되어 라운드별 판단이 사라진다.

### 9.4 Place 단계에서 가능한 행동

1. 새 유닛 배치 (빈 슬롯)
2. 새 유닛 즉시 머지 (같은 유닛·같은 성급 슬롯)
3. **보드 유닛 이동** — 현재 미지원. `GridBoard.TryMoveSlot` 필요
4. 보드 유닛끼리 머지
5. 유닛 판매
6. 지원 소환

> **UI 위험:** 지금 보드 탭 규칙은 2모드(대기 유닛 배치 / 슬롯 간 머지)인데 여기에 이동·판매가 붙는다.
> 해소안: **유닛을 고르면 이동 가능 칸과 머지 가능 칸을 함께 강조하고, 판매 버튼을 띄운다.** 빈 칸을 누르면 이동, 짝을 누르면 머지다. 모드를 늘리지 않고 흡수된다.

### 9.5 15웨이브 구성

| Act | 웨이브 | 구성 | 의도 |
|---|---|---|---|
| 1 | 1~4 | Normal → Runner 혼합 | 흐름 학습. 엉성해도 통과 |
| | **5 보스** | Boss + Normal ×3 | 머지를 한 번도 안 했으면 힘들다. **Joker ×1 + Perk 선택** |
| 2 | 6~9 | Swarm / Tank 혼합 | 단일 유닛만 모았으면 약점이 드러난다 |
| | **10 보스** | Boss + Tank ×2 | 첫 실패 가능 구간. **Joker ×1 + Perk 선택** |
| 3 | 11~14 | Runner·Swarm·Tank 복합 | Perk·머지·Chip 운영이 난이도에 반영된다 |
| | **15 최종 보스** | Final Boss + 소량 | 클리어. 라이프 3~10 남기는 것이 목표 |

`WaveDefinition`이 이미 엔트리별 `count`·`interval`·`startDelay`를 지원하므로 **코드 없이 에셋 15개로 구성된다.**

### 9.6 결과 화면

패배해도 성과를 보여준다. 메타 성장 없이도 "지난번보다 잘했다"를 느끼게 하는 유일한 장치다.

도달 웨이브 / 최고 족보 / 총 소환 수 / 최고 유닛 / 사용한 Chip / 남은 라이프. 클리어 시 클리어 시간 추가.

→ 런 통계 추적이 필요하다. 현재 어디에도 없다.

---

## 10. 유닛 역할과 적 (M8)

### 10.1 공격 패턴 5종

13종 유닛이 **각자 다른 시스템을 쓰지 않는다.** 패턴 5개를 나눠 쓴다.

| 패턴 | 내용 |
|---|---|
| Rapid | 빠른 단일 |
| Heavy | 느리고 강한 단일 |
| Multi | 여러 적 동시 |
| Splash | 작은 범위 |
| Pierce | 진행 방향 관통 |

Slow·Poison·Stun 같은 상태이상은 넣지 않는다.

### 10.2 족보별 유닛

**설계 원칙: 유닛이 족보의 기억 장치가 된다.** 이름과 실루엣이 족보의 구조를 그대로 보여주면 플레이어는 외우는 대신 알아본다. 13종을 서로 다른 판타지 캐릭터로 만들지 않고 **하나의 카드 군단**으로 통일한다.

| 족보 | 표시명 | 정식명 | 구조 | 역할 |
|---|---|---|---|---|
| 하이카드 | 스카우트 | 하이카드 스카우트 | 카드 1장 | Rapid — 빠른 단일 |
| 원페어 | 건슬링어 | 원페어 건슬링어 | **2** (쌍권총) | Rapid — 2연타 |
| 투페어 | 트윈레인저 | 투페어 트윈레인저 | **2+2** (양팔 2연장) | Multi — 2타겟 |
| 트리플 | 마크스맨 | 트리플 마크스맨 | **3** (3총구) | Heavy — 3발을 모아 한 발 |
| 백스트레이트 | 에이스 | 백스트레이트 에이스 | A→2→3→4→5 계단 | Rapid — 초고속 연사 |
| 스트레이트 | 랜서 | 스트레이트 랜서 | 일직선 창, 카드 5장 일렬 | Pierce — 직선 관통 |
| 마운틴 | 마운틴킹 | 마운틴 킹 | ▲ 산 왕관, 꼭대기에 A | Heavy — 보스 특화 |
| 플러시 | 보머 | 플러시 보머 | 같은 무늬 반복 | Splash — 범위 |
| 풀하우스 | 배터리 | 풀하우스 배터리 | **3+2** (주포 3 + 보조포 2) | Splash — 넓은 범위 |
| 포카드 | 쿼드캐논 | 포카드 쿼드캐논 | **4** (4연장 중포) | Heavy — 초고화력 저속 |
| 백스트레이트 플러시 | 템페스트 | 백스트레이트 플러시 템페스트 | 같은 무늬 A-5가 원형 회전 | Multi — 빠른 다중 |
| 스트레이트 플러시 | 레일거너 | 스트레이트 플러시 레일거너 | 같은 무늬 5장 일렬 = 레일 | Pierce — 강력한 관통 |
| 로열 스트레이트 플러시 | 소버린 | 로열 스트레이트 플러시 소버린 | 10-J-Q-K-A 왕좌, 왕관 | Splash — 최상급 광역 |

**핵심은 굵게 표시한 다섯이다.** 2 / 2+2 / 3 / 3+2 / 4를 실루엣으로 읽을 수 있어, 이름을 몰라도 무엇인지 알 수 있다.

- **원페어와 투페어를 시각적으로 구분한다.** 건슬링어는 `1×2`, 트윈레인저는 `2×2`다. 트윈레인저가 한 대상에게 4발을 쏘면 포카드처럼 보이므로, **적 둘에게 각각 한 쌍씩** 쏜다. Pair + Pair가 연출에서도 그대로 보인다.
- **마크스맨은 3연타가 아니라 3발을 모아 한 발이다.** 3연타로 만들면 건슬링어와 역할이 겹친다. 셋이 하나로 모이는 연출이 Three of a Kind와도 맞고 Heavy 역할도 지킨다.
- **배터리는 연출만 3+2다.** 주포 3문 → 보조포 2문 순으로 발사하되 피해 판정은 Splash 하나다. 구현 복잡도를 늘리지 않는다.

**상위 족보가 모든 면에서 하위보다 좋지는 않다.** 쿼드캐논 ★1은 보스·Tank에 강하지만 잔챙이 떼에는 비효율이고, 스카우트 ★3은 빠른 적 처리에 매우 좋다. 그래서 후반에도 하위 족보 유닛을 머지해 쓸 가치가 남는다.

### 10.3 이름 규칙

**표시명과 정식명을 분리한다.**

| 쓰이는 곳 | 표기 | 이유 |
|---|---|---|
| 보드 슬롯 | 표시명만 (`건슬링어`) | **슬롯이 화면 폭의 13.5%, 1080 기준 약 145px밖에 안 된다.** 긴 이름은 물리적으로 안 들어간다 |
| 소환 안내 | `원페어 → 건슬링어 소환` | 이 시점에만 족보와 유닛이 연결되면 된다 |
| 상세 정보 | 정식명 (`원페어 건슬링어`) | |

**표시명은 한글 6자 이내로 제한한다.** 이 상한이 없으면 이름이 계속 길어진다.

**유닛 이름은 한글로 쓴다.** UI 전체가 한글이고 족보명도 `원페어`·`트리플`이다. 여기에 영문 `Pair Gunslinger`를 붙이면 플레이어가 *원페어 = One Pair = Pair* 라는 번역을 한 번 거쳐야 해서, **이름을 기억 장치로 만들려던 목적이 오히려 깨진다.**

**백스트레이트 계열에 "Wheel"을 쓰지 않는다.** A-2-3-4-5를 휠이라 부르는 것은 영어권 관습이고, 한국식 포커에서는 백스트레이트다(§3.3). 한국 플레이어에게 아무것도 연상시키지 못한다.

### 10.4 외형 언어 규칙

- **카드 문양은 장식이지 능력이 아니다.** 유닛마다 고정 심볼 하나를 쓰고, **실제 손패의 무늬에 따라 외형이 바뀌지 않는다.** 카드 속성 시스템은 §7에서 폐기했고 외형이 그 오해를 만들면 안 된다. 같은 이유로 하이카드 유닛에 A 카드를 붙이지 않는다 — 랭크가 성능과 연결되는 것처럼 보인다.
- **성급이 올라도 구조 숫자는 절대 바꾸지 않는다.** 건슬링어 ★3에게 총 4자루를 들리면 족보 언어가 깨진다. **페어는 영원히 2, 트리플은 영원히 3, 쿼드는 영원히 4다.**
  성급은 갑옷 장식, 카드 오라, 무기 크기·화려함, 이펙트 강도로만 표현한다. 슬롯의 `★` 표기가 이미 성급을 담당하므로 **구조는 실루엣, 성급은 별**로 두 축이 분리된다.
- **소버린만 예외적으로 크게 만든다.** 13종 중 유일한 군주급이며 등장 확률이 0.000154%다. 등장 연출은 과해도 된다.
  단 이 연출을 소버린 전용으로 만들면 **평생 한 번도 못 보는 플레이어가 생긴다.** 스트레이트 플러시 이상(합계 약 0.0015%)에 등급별 3단계로 스케일하는 편이 실제로 발동한다.

### 10.5 구현 비용

**이름 변경은 에셋 13개의 `displayName` 문자열 수정이 전부다.** `UnitDefinition.displayName`이 이미 `[SerializeField] string`이고, `HandUnitTable`은 GUID로 참조하므로 에셋 파일명을 바꿔도 링크가 끊기지 않는다.

정식명을 따로 보관하려면 필드 하나(`fullName`)가 늘어난다. 상세 정보 화면이 생기는 시점(§9.6 결과 화면 또는 유닛 상세)까지는 표시명만으로 충분하다.

실루엣·모션·VFX는 M10 아트 단계다. 그때 이 표가 그대로 제작 기준이 된다.

### 10.6 적 종류

특수 능력을 넣지 않는다. **HP·이동속도·수량만 바꿔도 역할이 갈린다.**

| 적 | 특성 | 유리한 유닛 |
|---|---|---|
| Normal | 기준 | — |
| Runner | HP↓ 속도↑ | Rapid, Multi |
| Tank | HP↑ 속도↓ | Heavy |
| Swarm | HP 매우 낮음, 다수 | Splash, Multi |
| Boss | HP 매우 높음, 소수 | 단일 고화력 |

**현재 `EnemyDefinition`(HP·이동속도·타입)으로 전부 표현 가능하다.** `EnemyType`에 `Runner`·`Tank`를 더하면 되고, 코드가 이 enum으로 분기하는 곳은 지금 0곳이다. Shield·Armor·회복·순간이동 같은 기믹은 넣지 않는다.

**단, 적 타입은 §10.1 없이는 무의미하다.** 타겟팅이 "사거리 내 가장 앞선 적" 한 종류인 동안에는 어떤 적이 나와도 "총 DPS가 충분한가"로 귀결된다.

---

## 11. Dealer Perk (M9)

Wave 5·10 보스를 잡으면 3개 중 하나를 고른다. 스테이지 끝까지 유지되며 **한 게임에 총 2개**다.

목적은 규칙을 뒤엎는 것이 아니라 **이번 런에서 어떤 플레이를 더 가치 있게 만들지**를 정하는 것이다.

**선택지 3칸은 카테고리를 섞어 뽑는다** — 포커/경제에서 1, 유닛/머지에서 1, 완전 랜덤 1. 비슷한 것 셋만 나오는 상황을 줄인다.

**먼저 6개만 만들어 "고르는 것 자체가 재미있는가"를 검증한다.** 재미가 확인되면 늘린다.

| 계열 | Perk | 효과 |
|---|---|---|
| 포커 | Patience | 교체 0장이면 유지 보너스 +4 → +6 |
| 포커 | Insurance | 3장 이상 교체했는데 하이카드로 끝나면 +3 Chip |
| 경제 | Bargain | 지원 소환 6 → 5 Chip |
| 경제 | Interest | 웨이브 종료 시 보유 Chip 10당 +1 (최대 +2) |
| 머지 | Rookie Training | ★1 공격력 +20% |
| 머지 | Veteran | ★2 이상 공격력 +15% |

확장 후보: Second Chance(교체한 자리 재교체) / Pair Collector(원페어 종료 시 Chip) / Recycler(★1 판매가 인상) / Reinforcement(지원 소환 확률 상향) / Perfect Merge(★3 공격속도) / Full Board(10기 이상 시 전체 공격속도) / High Roller(스트레이트 이상 공격력) / Premium Hand(풀하우스 이상 공격속도) / Wild Joker(Joker 사용 유닛 공격력).

> **구조 주의:** Perk는 `RoundContext`(Second Chance)·`GridBoard`(Full Board)·`CombatContext`(공격력 배수)를 가로질러 규칙을 바꾼다. **수정자를 어디에 두는지 정하지 않고 첫 Perk를 짜면 곳곳에 `if`가 흩어진다.** M9 착수 시 이 구조를 먼저 정한다.

---

## 12. 플레이테스트 확인 항목

수치보다 아래 질문을 먼저 본다.

| 영역 | 질문 |
|---|---|
| 포커 | 교체에서 실제로 고민이 생기는가? 매번 5장 다 바꾸고 있지는 않은가? 반대로 Chip 때문에 아예 안 바꾸지는 않는가? |
| 경제 | 지원 소환을 한 판에 몇 번 쓰는가? Chip을 쌓기만 하는가, 늘 부족한가? |
| 머지 | ★2가 자연스럽게 나오는가? ★3을 한 판에 1~3기 보는가? 머지를 위해 일부러 모으는 판단이 생기는가? |
| 족보 | 스트레이트가 떴을 때 확실히 기쁜가? 희귀 유닛 ★1이 실제로 강한가? |
| 보드 | 15칸 중 평균 몇 칸을 쓰는가? 판매가 실제 선택지로 쓰이는가? |
| 전투 | 적 종류에 따라 유닛 가치가 달라지는가, 결국 총 DPS만 높으면 끝나는가? |

**M6 직후 반드시 측정할 것:** M3 실측 "스카우트 4기 = 13라운드"가 지원 소환 포함 **4~7라운드**로 내려오는가. 이게 안 내려오면 Chip 수급이나 지원 소환 비용을 조정해야 한다.
