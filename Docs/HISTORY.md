# 작업 히스토리

## 이 문서의 관리 규칙

- **최신 항목이 위로.** 각 항목은 `날짜 — 제목` / 결정과 이유 / 검증 순으로 적는다.
- **레거시 정리:** 이후 작업으로 완전히 대체돼 현재 코드·설계와 무관해진 항목은 삭제한다. 뒤집힌 이유가 지금도 의미 있으면 최신 항목에 한 줄로 흡수시킨다.
- **현재 상태 요약**과 **반복 함정**은 항상 최신이어야 한다. 이력을 읽지 않아도 이 둘로 상태를 파악할 수 있어야 한다.
- 설계 내용은 여기 쓰지 않는다. [DESIGN.md](DESIGN.md)가 유일한 설계 출처다.

---

## 현재 상태 요약

- **진행 단계:** M0~M10 완료. **M11(아트 교체) 완료** — 유닛 13종·적 7종·이펙트 6장·카드 5장·배경/트랙 4장 + 동반 코드 전부 스프라이트로 대체(검증 조건 DESIGN §8). 슬롯 레이아웃·트랙 여백 재검토는 DESIGN §7-4로 미룸. 아트 이력·스펙은 [ART_REQUEST.md](ART_REQUEST.md)
- **코드:** `Scripts/Poker/`, `Scripts/Game/`(Flow·Board·Units·Enemies·Data), `Scripts/UI/`, `Scripts/Tests/Editor/`
- **데이터:** 유닛 13종(정식명 `원페어 건슬링어`, 공격 패턴 5종을 나눠 씀) + `HandUnitTable` + `CardVisualSet`, 적 7종(Grunt·Runner·Swarm·Brute·MiniBoss·Boss·FinalBoss = 5타입 전부 사용. MiniBoss W5 전용, Boss W10·20·30·40·50, FinalBoss W50 전용) + 웨이브 50개 + `Stage_1` + `Economy` + `PerkTable`(특전 6종)
- **씬:** `Boot.unity`(빌드 0, 카메라만), `Game.unity`(빌드 1). 보드·적은 월드 스페이스, 카드 UI와 HUD만 uGUI
- **렌더링:** URP 2D. Game 뷰 Portrait 1080x1920, 카메라 직교 크기 6.6, 보드 중심 월드 y=3.4
- **어셈블리:** asmdef 없음. 게임 코드 `Assembly-CSharp`, 테스트 `Assembly-CSharp-Editor`(폴더명이 `Editor`여야 EditMode로 분류됨)
- **테스트:** EditMode **219/219 통과**
- **개발 도구:** `DevMode > 족보 소환`(교체 단계에서 원하는 족보를 손패에 쥐여 줌 — 족보 13종 ↔ 유닛 13종이 1:1) / `DevMode > 웨이브 스킵`(전투 없이 현재 웨이브를 클리어 처리하고 다음/지정 웨이브로). 게임 코드에는 `#if UNITY_EDITOR` 진입점만, 카드표·창은 `Assembly-CSharp-Editor`
- **전투:** 겨냥 대상은 언제나 사거리 내 가장 앞선 적 1기. 패턴은 그 한 발이 몇 기를 함께 때리는가만 정한다 — DESIGN §10.1
- **루프:** 50라운드 자동 진행 → 결과 화면 → 씬 리로드 재시작. 플레이어 입력 지점은 확정 / 전투 시작 / 보스 클리어 시 특전 선택 셋 — DESIGN §1
- **특전:** 10·20·30·40 보스 클리어 시 3중 택1. 특전이 바꾸는 값은 전부 `PerkSet` 하나에 묻는다 — DESIGN §11.1
- **보드 조작(이동·머지·판매·조커)은 전투 중에도 열려 있다.** 지원 소환만 `Place` 페이즈 전용 — DESIGN §1
- **Chip 경제:** 유지 보너스(교체를 덜 쓸수록 +Chip, 상한 3) / 판매 / 지원 소환 6 Chip(라운드당 1회) — DESIGN §9
- **스테이지:** 50웨이브, 시작 라이프 12, 웨이브당 라이프 피해 상한 5(보스가 남으면 무조건 상한), 보스 클리어 시 Joker 1개 + 특전 — DESIGN §9.5
- **UI는 한글.** 폰트 Maplestory(Light 기본 / Bold 굵기 연결), TMP Dynamic 아틀라스
- **슬롯에는 유닛 아트와 성급(★)만.** 13종 전부 아트가 채워져 플레이스홀더 색·이름 라벨 폴백은 걷어냈다 — DESIGN §10.3
- **유닛 아트는 방향별 4장, 모션은 코드가 낸다**(발사 반동 스케일 +6% + 대기 호흡). 조준 방향은 `CombatContext`가 유닛을 키로 기록 — DESIGN §10.4
- **총구는 방향별 목록**(`muzzlesSide / muzzlesFacing / muzzlesBack`, 좌우는 대칭). 발사 방식(동시/순서대로/무작위)은 `muzzlesFireTogether`·`muzzleRandomOrder`로 받는다 — DESIGN §10.4. 화염은 반동 배율을 따라간다
- **이펙트는 공용 2장이 기본, 유닛이 `attackEffect`·`hitEffect`로 덮어쓸 수 있다.** 지금 롱바렐만 전용(총구 섬광·관통 자국·빔 궤적 3장). 타격 쪽은 `HitEvent`가 때린 유닛을 함께 실어야 뷰가 고른다
- **빔 궤적만 공용 대역이 없다.** `pierceTrailEffect`가 비면 안 그린다(방향이 있는 유일한 이펙트). `CombatContext`가 유닛이 선 칸·겨눈 적·조준 방향을 넘기면 뷰가 총구→적으로 회전·신축해 그린다. **판정(트랙 뒤로 관통)과 연출(유닛→적)의 모양이 다르다.** 빔이 적을 지나쳐 뻗는 길이는 `pierceLength`(최소 0.3)
- **Splash 착탄 폭발 완료.** `SplashEvent`가 착탄 1회당 1건. 뷰는 스프라이트 전체 폭을 `splashRadius × 2`로 스케일 — 화면 크기 = 실제 피해 범위. `splashRadius`는 최초 설계값의 75%(보머 0.75). **지금 Splash는 플러시 보머 하나뿐** — 배터리·소버린은 "난사"로 보여 Multi로 옮겼다(이력 참고)
- **Multi는 사거리 안이라도 첫 타겟에서 `multiSpread` 밖의 적은 제외한다** — DESIGN §10.1. 소버린 1.5 / 템페스트 2.2로 실제 캡, 트윈레인저·배터리는 자기 사거리와 같은 값(무제한)
- **지켜야 할 관례:** 레이어 역방향 `using` 금지, `Poker`에서 `UnityEngine.Random` 금지(재현성), `Poker`에 ScriptableObject·MonoBehaviour 금지 — DESIGN §2
- **대기 중인 결정:** DESIGN §7 — Rapid가 어디서도 최적이 아님(계수 조정은 웨이브 재작성 이후로 미룸, 50웨이브 확장 후에도 미착수) / 라운드 전환 간격 없음 / 유닛 상세 블록 둘째 줄이 15px 넘침 + 트랙 상단 밴드와 겹침 / 트랙 여백이 커서 15칸이 돌 링 안의 작은 섬으로 보임 (§7-4)

---

## 반복 함정

같은 곳에 두 번 이상 걸렸거나, 걸리면 원인 찾기 어려운 것들.

### Unity / 코드

| 함정 | 대응 |
|---|---|
| **폰트에 없는 글자** — `♦`(U+2666), `—`, `–`이 Maplestory에 없어 두부(□) | `◆`, `-`로 대체. 새 기호 전에 `HasCharacter(c, false, true)`로 확인(Dynamic 폰트는 인자 없는 `HasCharacter`로 판정 불가) |
| 화면 표기에 `ToString()` | `Card.ToString()`("4h")·`UnitInstance.ToString()`(★)은 로그·테스트용. 화면은 `CardText`·`BoardScreen.Describe()` |
| **에디터 포커스를 잃으면 플레이어 루프가 거의 멈춘다** | `PlayerSettings.runInBackground = true`. MCP로 실시간 동작 볼 때 필수 |
| `manage_build`로 씬 목록 바꿔도 디스크에 안 써짐 | `File/Save Project` 실행해야 `EditorBuildSettings.asset`에 반영 |
| **TMP 폰트 에셋 아틀라스가 리로드·씬 저장 때 부푼다** | 다이나믹 폰트라 `Maplestory Light/Bold SDF.asset`이 1×1 → 1024² 커밋 상태로 바뀐다. 무관한 churn이면 `git checkout`으로 되돌린다. 폰트를 새로 만들 땐 `CreateFontAsset` 후 **서브에셋으로 추가** |
| Input System 전용(`activeInputHandler: 1`)이라 기본 입력 모듈이 터짐 | `EventSystem`에 `InputSystemUIInputModule` 부착 |
| 에디터 켠 채 패키지 제거 → `Library/ScriptAssemblies`에 고아 DLL | 수동 삭제 후 리프레시 |
| 컨트롤러 안 거치고 `GridBoard` 직접 변경 → 화면 굳음 | `PlacementController` 이벤트가 안 나가서다. 검증 스크립트에서 자주 걸린다 |
| **슬롯 인덱스로 든 전투 상태는 유닛이 움직이면 어긋난다** | 공격 쿨다운이 그랬다 — 빈 칸으로 옮기면 그 칸의 0을 물려받아 연사(10배). **유닛을 키로** 들 것 |
| **기존 에셋에 `[SerializeField]` 필드를 새로 넣으면 C# 초기값이 아니라 0** | YAML에 키가 없으면 `default(T)`. 필드 추가 시 기존 에셋에 값을 명시적으로 써넣고, **비어 있지 않은지 검사하는 에셋 가드 테스트**를 붙인다(`visualScale`·`multiSpread`·`perkReward`가 그랬다) |
| **`Awake`에서 만든 것을 다른 컴포넌트의 `Awake`가 읽으면 순서에 걸린다** | 실행 순서는 씬 설정에 달려 코드만 봐선 안 보인다. **처음 묻는 쪽이 만들게**(지연 생성) |
| **enum 값을 중간에 끼우면 기존 에셋이 조용히 어긋난다** | 에셋은 enum을 정수로 든다. `EnemyType`은 뒤에만 붙이고(Normal 0 / Swarm 1 / Boss 2 / Runner 3 / Tank 4) 정수 값 고정 테스트 |
| `Tick(0.05f)`를 N번 돌려도 정확히 N×0.05초가 안 됨 | 부동소수 누적이라 살짝 못 미쳐 제한시간 판정을 안 지난다. 한 스텝 더 돌린다 |
| 스크립트로 씬 `RectTransform` 옮길 때 `localPosition` | 직렬화되는 건 `m_AnchoredPosition`. `anchoredPosition`에 쓰고 `RectTransform`도 `SetDirty` |
| **"콘솔에 오류 없음" ≠ "컴파일됨"** | `refresh_unity`가 파일 변경을 못 잡으면(`refresh_triggered: false`) 컴파일 자체가 안 돈다 — 콘솔은 깨끗하고 `execute_code`는 옛 어셈블리를 본다. **`Library/ScriptAssemblies/Assembly-CSharp.dll` 수정 시각을 소스와 비교.** 안 돌았으면 `AssetDatabase.ImportAsset(path, ForceUpdate)` + `CompilationPipeline.RequestScriptCompilation()` |
| **플레이 모드에서는 에셋 갱신·저장이 막힌다** | `refresh_unity`가 30초 타임아웃. 에디터 상태의 `is_playing`을 먼저 볼 것. 플레이 중엔 Unity를 안 건드리는 작업(분할·측정·검산)을 먼저 |
| **플레이 모드에서 만든 오브젝트는 나가는 순간 사라진다** | 씬 편집 전 `Application.isPlaying` 확인. `SerializedObject`로 씬 저장 시 `This cannot be used during play mode` |
| **라벨 넘침을 rect 폭으로 판단하면 틀림** | TMP는 자동 개행이라 넘치면 밖으로 안 나가고 두 줄이 되어 아래를 침범한다. `textBounds`는 개행 후 값이라 항상 rect 안. **`GetPreferredValues`로 재고 `textInfo.lineCount`가 1인지** |
| 에디트 모드에서 뷰 `Show()` 미리보기 → 그대로 씬에 저장됨 | TMP 문자열·색이 직렬화된다. 찍어 본 뒤 `Show(null)`로 되돌리고 저장 |
| **`TMP_Text.fontMaterial` getter가 인스턴스 머티리얼을 만든다** | 진단 스크립트에서 `.fontMaterial`을 읽기만 해도 그 TMP에 로컬 머티리얼이 붙고, 다음 `SaveScene`에 `m_fontMaterial`이 공유 머티리얼 링크를 끊은 채로 직렬화된다(라벨 여럿에 번져 씬 diff가 커진다). 읽기는 `.fontSharedMaterial`로. 이미 번졌으면 씬을 `git checkout` 후 의도한 변경만 다시 적용 |
| **성급 배수는 공격력·공격속도에 동시에 곱해져 DPS가 제곱으로 커진다** | `{1, 1.6, 2.25}`면 DPS ×1 / ×2.56 / ×5.06. 배수를 만질 때는 항상 제곱해서 볼 것 |
| **`spriteAlignment`는 `TextureImporter`에 없다** | `TextureImporterSettings`에 있다. MCP `manage_asset`으로 넘기면 성공 반환 후 조용히 무시(피벗이 Center로 남아 유닛이 슬롯에서 뜬다). `ReadTextureSettings`→수정→`SetTextureSettings`→`SaveAndReimport`, 메타 되읽어 `alignment: 7` 확인 |
| 스프라이트 전환 전 텍스처 높이를 읽으면 NPOT 값 | 407×749인데 `tex.height`가 512. `textureType = Sprite` + `npotScale = None` 먼저 걸고 리임포트 후 다시 읽는다 |
| **`.meta`를 지우고 이미지를 덮어쓰면 GUID는 살아도 임포트 설정이 초기화** | 참조는 안 끊겨 조용하다. `textureType` Default·PPU 100·NPOT 켜짐으로 돌아간다. `.png`만 덮고 `.meta`는 둔다. 덮었으면 메타에서 `textureType: 8`·PPU 확인 |
| **채팅 미리보기의 흰/검은 배경은 투명 여부를 말해주지 않는다** | 파일을 직접 읽어 알파 채널을 확인하기 전까지 배경 판정 보류 |

### 밸런스 측정

| 함정 | 대응 |
|---|---|
| **사거리는 자유롭게 못 내린다** | 가운데 세 칸(6·7·8)이 트랙에서 2.40 떨어져 그 아래면 아무것도 못 때린다. 게임 최단이 2.5인 이유. 크기는 덮는 트랙 비율로 잰다 — 15칸 평균이 2.5에서 24.0% / 3.0에서 36.6% / 3.5에서 51.6% / 4.0에서 66.3% |
| **사거리와 관통 길이를 따로 보정하면 어긋난다** | 둘 다 "한 발이 몇 기를 때리느냐"에 걸린다. 명목 DPS로 맞추지 말고 **유효 피해량**(스폰 HP − 잔여 HP, 오버킬 제외)으로 맞춘다 |
| **오버킬은 명목 DPS가 아니라 ATK의 절대 크기에 달렸다** | 패턴 비교 시 명목 DPS를 낮게 정규화하면 오버킬이 안 일어나 반대 결론이 난다. 실제 유닛 ATK 대역(5~310)에서 잰다 |
| 전투 성능을 클리어 시간으로 재면 스폰 바닥에 걸린다 | 마지막 스폰 시각보다 빨리 못 끝나 강한 보드끼리 같은 값. 제한시간 동안의 유효 피해량으로 잰다 |
| 재현 실행이 필요한 검증 | `Time.timeScale`을 올리고 `EditorApplication.update`에 자동 진행 콜백(끝날 때 스스로 해제). `BalanceSimRunner.cs`(Editor 전용)가 라운드 루프를 끝까지 자동으로 돈다 |
| **회전 대칭 수치가 좋다고 "축이 없어 보인다"는 뜻이 아니다** | 4중 대칭 도형은 90도 회전에 겹쳐 회전-차이 수치가 잘 나온다. "상하좌우가 유독 길어 보이는가"는 눈으로 따로 확인 |

### 생성형 아트 (유닛·적 완료, 배경·트랙에 일부 적용)

| 교훈 | 근거 |
|---|---|
| **비교급 형용사·부정 지시는 약하다 — 원인이 되는 구조·서술 자체를 바꿔야 먹힌다** | "더 둥글게"로 스웜을 그런트와 못 갈랐고(→ 다른 종으로 못박음), "NOT feather-shaped"로 템페스트 머리를 못 고쳤고(→ 길이 자체를 줄임), "낡은 망토"는 거지꼴로 나왔고(→ 손상 서사 폐기, 디자인 모티프로), "sceptre-rifle"은 계속 지팡이로(→ 총기 해부학 용어로만) |
| **상대 지시("다른 칸과 같은 자세로")는 잘 안 먹힌다** | 에이스 후면 팔을 7번 고쳐도 매번 다른 방식으로 어긋났다. 격자선으로 두 컷을 픽셀 비교해야 원인이 보인다. 같은 부위가 3번+ 어긋나면 문구를 더 파지 말고 자세 자체를 관절 자유도가 낮은 형태로 바꾼다(한 손 들기 → 양손 받쳐 들기) |
| **몸통에 건 지시(굵다·넓다·각지다)가 얼굴에도 샌다** | 몸은 크게 / 얼굴은 갸름하게처럼 상반된 지시는 "얼굴은 몸과 달리 ~하다"로 명시적으로 갈라 쓴다 |
| **4방향 시트: 좌우가 뒤바뀌어 나오고, 방향마다 캐릭터 크기가 다르고, 캔버스 정중앙으로 자르면 안 된다** | 프롬프트에 `BOTTOM-LEFT = LEFT`라고 박아도 반대로 나온다(마크스맨). 자르기 전에 **실제 분리 행/열**을 찾고(중앙선 통과 픽셀 카운트), **무기 띠 x중심 vs 다리 x중심**으로 방향을 판정한다. 뒤바뀌었으면 다시 뽑지 말고 엇갈려 배정. 높이 편차가 크면 부츠 비율로 "몸이 진짜 작은지 vs 포즈 차이인지" 가른다 |
| **셀 경계가 안 비는 유기적 형태(촉수·머리카락·날개)는 연결 성분 검사로 고립 조각을 확인** | 레이스 촉수가 상하 셀 경계를 스쳐, 자른 뒤 인접 셀 조각이 본체와 분리돼 붙어 나왔다. BFS로 가장 큰 덩어리만 남긴다 |
| **총구 좌표: 몸 중앙 가정 금지, 방향마다 실측, 찾은 좌표를 그림에 찍어 재검증** | 어깨에 멘 총은 정면·후면 x=0으로 두면 뒤통수에 화염(마크스맨 21.6px, 스카우트 23px). 좌우를 대칭으로 묶을 땐 한쪽 실측값이 아니라 **양쪽의 중점**(보머 좌우 차 5.5px). 금색 링 자동 검출은 근처 금색 장식에 오염되니 좌표를 점으로 찍어 육안 재검증. 무기가 몸에 가려 안 보이는 방향은 정면 보어를 좌우 반전 |
| **화면에 고정된 좌표는 반동을 안 따라간다** | 아트는 발끝 피벗으로 커지는데 화염 좌표가 슬롯 로컬 고정이면 어긋난다. 하필 화염이 보이는 0.06초가 반동 최대 구간. 아트에 스케일을 걸면 거기 붙는 것도 같은 배율을 받아야 한다 |
| **환경 링·프레임은 캔버스 채우기가 아니라 정합 기준선을 수치로 못박아야 하고, "깨끗한 가장자리"는 부정어로 막아야 한다** | 트랙 루프를 "외곽 ~95%"로 주니 고정 두께 밴드가 안쪽으로 밀려 중심선 비율이 목표(가로:세로 1.417)에서 1.67→1.53으로 어긋났다(3회 재생성). 게임이 정합하는 건 중심선이라 중심선 비율·픽셀을 직접 적어야 한다. 슬롯 하이라이트는 "soft bloom"을 낡은 양피지 가장자리로 해석 — `NO torn/frayed/grunge edge`를 넣어야 매끈해졌다(2회). 비율이 조금 어긋난 링은 왜곡 없는 균등 스케일로 흡수 가능(한 축을 맞추고 나머지 축 오차를 두꺼운 밴드가 덮게) |

---

## 이력

### 2026-08-30 — 화면 상단 레이아웃 패스 1차 (DESIGN §7-4 일부)

M11 5단계가 남긴 슬롯·트랙 시각 이슈 중 큰 지오메트리 변경 없이 되는 것만 처리. 카메라 리프레임은 안 함(밸런스 재검증 이후로 미룸).

- **성급 배지 위치:** 캐릭터 정강이(로컬 y −0.345) → **머리 위(`StarTopY = 0.40`)**. `UnitSlotView.Awake`에서 `sortingOrder`와 같이 지정. 정강이에 겹치면 캐릭터 무늬처럼 읽혔다.
- **슬롯 타일 금색 톤 다운:** `SlotTile.png`의 금색 픽셀만 채도(−55%)·명도(×0.82) 낮춤 — mean (177,141,89) → (145,131,110). 돌 판·회색은 안 건드림. `Docs/tools/slottile_tone.py` 신규(카드 툴과 같은 패턴, 원본은 git). 15칸 깔았을 때 금색이 시끄럽던 것 완화.
- **상세 문구 이동:** `DetailPlate`·`PendingLabel`을 화면 top(anchoredPos y 862, 스크린 y≈1822 — 트랙 상단 밴드 + HUD와 겹침)에서 **그리드 바로 아래(y 220, 스크린 y≈1180)**로. RectTransform 2개만.
- **트랙 명도:** `TrackLoop` SpriteRenderer 색 흰색 → (0.78, 0.78, 0.80). 돌 밴드가 살짝 물러나 위에 얹히는 라벨 대비가 나아지고 보드가 조금 더 분리돼 보인다.
- **`TrackMargin 0.9`는 안 건드렸다** — 가운데 칸이 트랙에서 정확히 2.4, 최단 사거리 2.5가 여기서 나온다(반복 함정 "사거리는 자유롭게 못 내린다"). 줄이면 전 유닛 사거리 유효성이 바뀌어 밸런스 재검증 대상. "15칸이 링 안 작은 섬" 인상은 수용.
- **미해결:** HUD(라이프·라운드)가 여전히 트랙 상단 밴드 위 — 실제 렌더(어두운 톤)에서 읽히는지 확인 필요, 안 되면 라벨에 아웃라인. 카메라 리프레임(보드를 화면에서 내려 상단 공간 확보 + 라벨 5그룹 재배치)은 밸런스 확정 후 §7-4 본 패스에서.
- **검증:** 컴파일 0건, EditMode 219/219, 플레이 모드에서 8유닛 배치해 배지·톤·문구 위치 스크린샷 확인. 씬 diff 3줄(anchoredPos ×2, color ×1).

### 2026-08-30 — M11 5단계: 배경·트랙 아트 4장 + 동반 코드 완료 (M11 종료)

- 배경 / 트랙 루프 / 빈 슬롯 타일 / 슬롯 하이라이트 4장을 임포트하고 씬에 얹었다. **M11 아트 교체가 전부 끝났다.**
- **씬 신규 오브젝트 2개:** 루트 `Background`(풀스크린 스프라이트, `sortingOrder -100`, 카메라 원점) / `Board` 자식 `TrackLoop`(`sortingOrder -10`, 로컬 원점). 적 위치가 `Board` 기준(월드 0, 3.4)이라 트랙은 자식으로 둬야 동기된다. 카메라 단색 clear는 폴백으로 유지.
- **슬롯 15칸:** `Background` 자식 → `SlotTile`, `Highlight` 자식 → `SlotHighlight`. 두 자식 `localScale` 0.92 → 1.06(새 스프라이트가 6% 투명 여백을 물어 칸이 맞닿게).
- **`UnitSlotView`:** `EmptyColor` 상수 + `background` 필드 + `background.color` 대입 2곳 제거(타일이 항상 같은 그림이라 틴트가 불필요, 오히려 어둡게 깔렸다). `highlight`는 `PlaceableColor`(흰 α0.22)·`SelectedColor`(금 α0.45) 틴트를 그대로 곱한다 — 하이라이트 스프라이트를 중립 니어화이트로 뽑은 이유.
- **임포트:** 배경 PPU 126(불투명, 1080×1920의 87% 해상도지만 비율 정확). 트랙 PPU 170 — 제출본 중심선 비율이 1.53(목표 1.417)이라 왜곡 없는 균등 스케일로 흡수(좌우 도로는 정중앙, 상·하는 적이 도로 중앙보다 0.13월드 바깥 — 두꺼운 밴드가 덮음). 타일·하이라이트 PPU 1275(512로 다운스케일), 밉맵 켬.
- **후속 수정(플레이 확인 후):** ① 성급 `★`이 아트에 가려짐 — `starLabel.sortingOrder`가 씬 값 2(아트 3보다 아래)라 항상 묻혔다. `Awake`에서 6으로 올림. ② 폭 넓은 유닛(롱바렐·보머 측면 뷰 ~1.08월드)이 `ArtScale 0.85` 고정이라 타일 금테를 넘어 이웃 칸에 걸침. `FitArtToCell()` 추가 — `artFitScale = Min(ArtScale, 0.9 / 아트월드폭)`, 스프라이트 바뀔 때마다 재계산. 좁은 유닛은 0.85 그대로, 넓은 5종만 2~6% 축소해 정지 시 금테 안. 반동 스파이크(~15ms)엔 셀(1.0) 안에서 잠깐 금테를 스침. 총구 화염 배율도 `artScale / artFitScale`로 맞춤.
- **미해결(전부 DESIGN §7-4로):** ① 트랙 여백(`TrackMargin 0.9`)이 커서 15칸이 넓은 돌 링 안의 작은 섬처럼 보임 ② 슬롯 타일 금색 장식이 15칸 합치면 다소 시끄러움(톤 다운은 원본 후처리) ③ 하단 상세 문구(화면 y≈1800)가 트랙 상단 밴드와 겹침 — `detailPlate`가 뜰 땐 가려지나 안내 문구만 뜰 땐 돌 위에 얹힘 ④ 성급 배지 위치가 캐릭터 정강이(로컬 y −0.345)라 어색 — 배지로 옮길지는 씬 레이아웃 패스에서.
- **검증:** 컴파일 0건, EditMode 219/219(2회), 플레이 모드에서 롱바렐·킹·스카우트 강제 배치해 정지·반동 아트 폭(0.89·0.89·0.46월드)과 `★` 표시 확인. 씬 저장 때 TMP 폰트 아틀라스가 또 부풀어 `git checkout`으로 되돌림(두 번째 저장엔 재churn 없음).

### 2026-08-30 — M11 4단계: 카드 동반 코드 완료

- 임포트만 해 둔 카드 스프라이트 5장을 코드가 쓰게 배선했다.
- **`CardVisualSet` SO 신설** (`namespace PokerDefense.Game`, `Scripts/Game/Data/` — 스프라이트를 든 SO라 `UnitDefinition`·`EnemyDefinition`과 같은 관례). `Front` / `Highlight` / `SuitOf(Suit)`. 에셋 1개에 6장 배선.
- **`CardView`: `suitLabel`(TMP) → `suitIcon`(`Image`).** 무늬 색 틴트 제거(PNG에 구워짐), rank 색만 `IsRed` 유지. 세트는 `RoundScreen`이 `Awake`에서 `Bind(CardVisualSet)`으로 손패 5장에 물려준다.
- **씬:** 루트 `Image` → `CardFront`+Sliced, `SelectionFrame` → `CardHighlight`+Sliced, `Suit` 자식의 TMP 제거 후 `Image` 붙여 `SuitIcon`으로 리네임. `CardText.SuitOf`는 그대로(키카드 줄·전투 라벨에서 계속 씀 — `◆` 대체 유지).
- **검증:** EditMode 219/219, Play Mode에서 렌더·선택 토글 스크린샷 확인, 콘솔 0건.
- **잔여 시각 폴리시(막지 않음):** `CardFront` 4모서리 금색 플로리시가 중앙 쪽으로 다소 침범. 더 나은 테두리가 나오면 `Docs/tools/cardfront_composite.py`를 원본에 다시 돌린다.

### 2026-08-28~29 — M11 4단계: 카드 아트 5장 완료

- 카드는 52장을 개별 생성하지 않고 프레임 + 무늬 아이콘 + rank(Maplestory TMP)를 런타임 합성한다. 실제 이미지는 `CardFront` / `CardHighlight` / 무늬 `♠♥◆♣` = 5장. **뒷면은 보류** — 덱 더미·페이스다운·플립 연출이 없어 어떤 코드도 뒤집힌 카드를 안 그린다.
- **`CardFront.png` — 생성 3회 반려, 후처리로 마감.** 생성기가 "평탄 불투명 크림 면"을 세 번 다 못 뽑았다(알파 그라데이션 / 속 빈 액자). 금 테두리·플로리시만 남기고 안쪽을 파이썬으로 평탄 크림 `#F3EEDF`·알파 255로 덮었다(`Docs/tools/cardfront_composite.py`). **교훈: 이미지 생성기는 "flat opaque fill"을 신뢰성 있게 못 만든다 — AI엔 테두리·장식만, 평면 채움은 코드로.**
- **무늬 4종** — 2×2 시트 1장 → 파이썬 4등분(`Docs/tools/suit_sheet_crop.py`). 하트만 bbox 높이가 12% 작아 네 심볼 높이를 209px로 통일. 색 `♠♣ #1E1E24` / `♥◆ #CC2626`.
- **임포트:** CardFront Sprite/Single·FullRect·9-slice `spriteBorder 240`·maxTextureSize 1024, 무늬 4장 Center pivot·maxTextureSize 256.

### 2026-08-23~27 — M11 3단계: 적 7종 아트 완료

- **컨셉:** 몬스터/크리처. 유닛의 "카드 군단"(붉은 군복 + 흰·금)과 대비되는 "그림자 무리"(어두운 몸통 + 종류별 글로우 악센트). 글로우 색은 `placeholderColor`를 물려받는다.
- **`EnemyDefinition.visualScale` 추가** — `CombatScreen.CreateView`의 `BodyScale`에 곱한다. 그런트 1.0 / 스웜 0.75 / 브루트 1.35 / 레이스 1.5 / 베히모스 1.8 / 아바돈 2.3.
- **적의 "방향"은 트랙 이동 방향**(유닛은 조준 방향). 트랙 4구간(상단=우향/우측=하향/하단=좌향/좌측=상향)이라 결정적이다. `GridBoard.TrackDirection(progress)` + `EnemyDefinition.artDown/Up/Left/Right` + `ArtFor(direction)`. `CombatScreen`이 매 프레임 스프라이트를 갈아 끼운다. 유닛과 같은 2×2 시트 방식.
- **7종을 실루엣이 안 겹치게 구조로 갈랐다:** 직립 임프(그런트) / 스프린트 임프(러너) / 네발짐승(스웜) / 오우거형(브루트) / 다리 없는 부유형(레이스) / 전갈형(베히모스) / 유익종(아바돈). 방향 판정은 눈 위치 비율이 역동적 포즈에서 안 통해 사지·머리·꼬리 위치를 직접 봤다.
- **`displayName`을 고유명으로:** MiniBoss→`Wraith`, Boss→`Behemoth`, FinalBoss→`Abaddon`(화면엔 안 뜨는 값, `id`·에셋 파일명은 웨이브 시스템이 참조하는 슬롯 타입이라 유지).
- **검증:** EditMode 219/219. 그런트만 실플레이(W1에서 트랙 도는 것 확인), 나머지는 방향 로직이 그런트에서 검증됐으므로 리플렉션(`HasArt`·`VisualScale`·4방향 연결)으로 대체. 이로써 `CombatScreen`의 적 아트-없음 분기는 실사용되지 않으나 코드는 남겨 뒀다(신규 적 대비).

### 2026-08-13~23 — M11 1·2단계: 유닛 13종 아트 완료

- **스카우트 파일럿으로 절차 확정** → 나머지 12종에 적용. 생성 단위는 **유닛별 4방향 턴어라운드 시트**(여러 종을 한 장에 담으면 캐릭터끼리 붙어 자동 분할이 깨진다). 2프레임 애니메이션은 몸통·다리가 통째로 달라져 버리고 **코드 반동**으로 갔다.
- **구조 숫자가 있는 여섯**(1·2·2+2·3·3+2·4)은 실루엣으로 읽히게, **없는 일곱**(계단·산·같은 무늬 반복·원형 회전·레일·왕좌·장총)은 컨셉과 색 블록으로 갈랐다 — 종횡비는 판별에 거의 안 쓰였고 매번 실제로 가른 건 색·머리·복장.
- **총구 좌표 모델이 세 번 넓어졌다:** 방향별(어깨에 멘 총) → 쌍무기(둘) → 방향별 목록(배터리 5총구, 좌우 반전 시 정면·후면 두 점이 한 자리로 붕괴). 최종 `muzzlesSide/Facing/Back` 목록 + 발사 방식(`muzzlesFireTogether`·`muzzleRandomOrder`).
- **정렬 절충:** 무기가 한쪽으로 길면 "몸 튐"과 "무기 돌출"의 맞바꿈. 무기 끝을 ±0.46에 닿게 두고 남는 만큼만 몸을 민다(캔버스 비대칭 패딩으로 계산하는 게 재현이 쉽다). 레일거너는 자세 설계 단계에서 무기를 옆구리에 붙여 이 문제를 피한 첫 사례.
- **스트레이트를 랜서(창) → 롱바렐(장총)로 재기획.** 찌르기 궤적 이펙트가 총기에 훨씬 잘 맞았다. 판정(Pierce)·수치는 그대로. §10.4의 "유닛마다 고정 심볼 하나"에 예외 둘(스트레이트·스트레이트 플러시 — 무늬 통일성이 곧 족보). 롱바렐 반동을 크기 반동으로 되돌리며 `RecoilStyle` 분기 자체를 삭제.
- **검증:** 각 유닛 EditMode 전량 통과, 124px 판별 확인, 화염 위치를 반동 최대 순간의 아트에 점 찍어 검산.

### 2026-08-13~17 — M11: 이펙트 6장 완료

- **공용 2장**(`MuzzleEffect` 총구 화염 0.06초 / `HitEffect` 타격 스파크 0.12초) — 유닛별이 아니라 13종 공용, 스카우트 레퍼런스로 먼저 잡았다. 날아가는 총알은 안 만들었다(§5.5 즉시 히트). 총구 위치는 눈대중(가슴 +0.05)으로 30px 어긋나 스프라이트에서 직접 재서 닫았다.
- **롱바렐 전용 3장**(`BeamMuzzleEffect` 은백 8방 / `PierceHitEffect` 관통 자국 / `PierceTrailEffect` 빔 궤적) — 골격(흰 코어 + 방사)은 공용과 같고 색(불→강철)·형태(구름→바늘)·고리(원 금지, 원은 Splash로 읽힘)만 뒤집었다. 빔 궤적은 방사 대칭이 아니라 상하 대칭 + 길이 방향 균일이 조건(코드가 회전·신축시킨다). 타격 쪽 색은 무기가 아니라 **적을 보고 고른다**(진홍은 붉은 적 위에서 묻힌다 → 금·호박빛).
- **`SplashEffect` 3회 시도:** 1차 실사풍 반려(수치는 통과 — 수치 통과가 스타일 통과를 보장 안 함), 2차 십자형 축·`MuzzleEffect`와 실루엣 겹침 반려, 3차 채택(경미한 대각선 편향은 실사용 크기에서 안 보임). 임포트 PPU 1254(캔버스 폭과 같게 두면 `bounds.size`가 정확히 `(1,1)`이라 `fullScale = splashRadius × 2`가 그대로 성립).

### 2026-08-23 — 웨이브 20→50 확장 + 상위 유닛 재조정 + Splash→Multi + 유닛 에셋 rename

- **웨이브 20→50 (순수 데이터).** DevMode 실측 결과 소버린 한 유닛으로 1~20을 전부 클리어 가능했다. 원인: 상위권 유닛 불균형 + DESIGN §7의 진단(유닛은 머지로 배수 성장, 웨이브는 덧셈). 1~19는 그대로, 21~50을 공식으로 생성 — 적 HP는 공용 에셋이라 못 올려서 **개체 수 + Brute 비중 + 스폰 밀도**를 후반부일수록 가파르게(Act3 20→30%, Act4 30→40%, Act5 42→54%). 보스 웨이브는 트래시 대신 Boss/FinalBoss를 직접 쌓았다. 보스 주기 5→10, 특전 10·20·30·40 (4회째는 남은 3종 전부 제시라 선택 의미 없음 — 특전 확장은 별도 작업).
- **상위 유닛 4종 재조정** (성급 공식 `{1, 1.6, 2.25}`은 유지): 마운틴 킹 ATK 156→176, 템페스트 68→54 + `multiSpread` 4→2.2, 레일거너 128→118, 소버린 35.8→25. 쿼드캐논이 anchor. ★1 단일 DPS 곡선(8~13번)이 47.5 → 88 → 108 → 118 → 124 → 150 단조 증가로 정리됨.
- **소버린 Splash → Multi.** 아트가 자동소총으로 확정되며 "연사"에 맞췄다. 단일 대상 DPS(214.8)는 보존하고 배분만 초당 6발·3타겟으로. 앞서 **배터리도 같은 이유**(초당 2.5회 폭발이 "난사")로 Splash → Multi(3타겟). 지금 Splash는 보머 하나.
- **`multiSpread` 추가** — Multi가 사거리 넓은 유닛에서 트랙 반대편 적까지 한 발에 때리는 문제. 첫 타겟에서 이 거리 안의 적만 후보. 기존 3종은 자기 사거리와 같은 값으로 둬 동작 유지, 소버린만 1.5.
- **유닛 에셋 레거시 이름 정리** — `Unit_Champion`→`Unit_QuadCannon` 등 11종을 아트 폴더 이름에 맞춰 `git mv`(내부 `m_Name`까지). GUID 보존이라 참조는 코드 변경 없이 유지.
- **DevMode 웨이브 스킵 창** 추가 — 실제 전투 결과 처리 경로(`GameFlowController.AdvanceRound`)를 재사용, 특전 대기·게임 종료를 만나면 멈춘다.
- **밸런스 자동 시뮬레이션 (9회).** `BalanceSimRunner.cs`로 전략 3종(no_exchange / greedy / greedy_summon) × 조커 사용. 결과: greedy 평균 사망 웨이브 ≈34, greedy_summon은 편차 큼(18~완주 1/7). 사망 구간은 대체로 Act3 후반~Act4로 설계 의도와 맞음. **소버린 단독 캐리 패턴은 재현되지 않았다.** 정확한 합격선은 사용자가 직접 플레이테스트로 정하기로 함. `PerkTests`의 보스 주기 기댓값 `{5,10,15}` → `{5,10,15,20,30,40}` 갱신, EditMode 219/219.

### 2026-08-24 — 유닛 슬롯의 "아트 없음" 폴백 제거

- 유닛 13종이 전부 아트를 갖춘 뒤 `UnitSlotView`의 플레이스홀더 경로(색 사각형 + 이름 라벨)가 죽은 코드가 됐다. 항상 아트를 그리고, `HasArt == false`면 `Debug.LogWarning`만.
- `nameLabel` 필드와 `UnitDefinition.PlaceholderColor`(+ 백킹 필드)도 제거. 씬의 15개 `NameLabel` 자식 오브젝트는 안 쓰이는 채로 남아 있다(완전히 치우려면 별도 요청).
- 적 쪽(`EnemyDefinition.PlaceholderColor`, `CombatScreen` 분기)은 신규 적 대비로 남겨 뒀다.

### 2026-08-12 — M10: 밸런싱 (아트와 분리)

- **M10을 밸런싱, M11을 아트로 나눴다** — §10.4가 구조 숫자를 고정해 밸런싱 결과가 아트 사양을 안 바꾼다. 아트가 먼저면 밸런싱 한 번마다 렌더링 검증이 따라붙는다.
- **성급 배수 `{1, 2, 4}` → `{1, 1.6, 2.25}`.** 기존 배수는 공격력·공격속도에 동시에 곱해져 ★3 = 16배 — `하이카드 스카우트 ★3`(DPS 320)이 `로열 스트레이트 플러시 소버린 ★1`(162)을 이겨 §10.2가 통째로 뒤집혀 있었다. 새 배수는 DPS ×1 / ×2.56 / ×5.06.
- **유지 보너스 상한 4→3, 지원 소환 라운드당 2→1회.** 걱정은 "좋은 패가 Chip까지"였는데 실측은 반대 — 교체하면 Chip이 0이 되어 지원 소환이 20회→1회로 무너진다(Chip 획득처가 유지 보너스 하나뿐). 지원 소환 2회일 땐 유입 40기로 ★3이 10기, 파이프라인이 포커 축을 지배했다.
- **★3 목표를 1~3기 → 4~9기로.** 개수를 정하는 건 배수도 경제도 아니라 유입 유닛 수(20라운드 × 포커 20기, 손패 92%가 2종). 웨이브 20으로 늘린 이상 전제다. §12에 "보드가 ★3으로만 차지 않는가"를 함께 본다.
- 스카우트·건슬링어 사거리 2 → 2.5(중앙 3칸이 트랙에서 2.4). 시작 라이프 15 → 12(15는 11을 남기고 끝나 "지지 않는 판").
- **M10 검증 4조건 통과** (자동 진행 11회, 교체 0장/전부 교체): 잔여 라이프 9·8(목표 3~10), ★3 9·7기(목표 4~9), ★1·★2 잔존 4·6칸(목표 3+), 특전 3회 수령. 자동 진행은 판마다 ±2 흔들린다(덱·특전이 매 판 새 시드).

### 2026-08-09~10 — M6~M9: Chip 경제 / 웨이브·보스·Joker / 유닛 패턴·적 타입 / 딜러 특전

구조는 전부 DESIGN.md에 있다. 이때 정한 것 중 코드에 안 드러나는 것:

- **M6 Chip 경제.** 유지 보너스가 핵심 — `ExchangeableCount`가 곧 미사용 교체권이라 새 상태 없이 계산. `StageController` 분리(라이프·Chip·웨이브는 한 판 유지되는 같은 층위). 판매가는 성급으로만(1/2/4). **M6 검증:** 스카우트 4기 확보까지 평균 5.3라운드(M3의 13 → 목표 4~7 한가운데).
- **M7 웨이브·보스·Joker (에셋만, 코드 0줄).** 라이프 피해 상한(§5.4), 보스는 한 기만 남아도 상한만큼. 최고 족보는 `HandRarity`, 최고 유닛은 DPS. `RunStats` 분리(규칙에 관여 안 함). 재시작은 씬 리로드.
- **M8 공격 패턴 5종 + Runner·Tank.** 패턴이 바꾸는 건 "몇 기를 때리는가"뿐, 겨냥 대상은 항상 사거리 내 가장 앞선 1기. Rapid↔Heavy는 코드가 같고 오버킬 낭비로만 갈린다. Pierce는 트랙 기준, Splash는 착탄 지점 기준. `EnemyType`은 뒤에 붙였다(정수 고정). 스탯은 기준 DPS 유지하고 배분만 옮김.
- **M9 딜러 특전.** 6종이 건드리는 곳 5개를 `PerkSet` 메서드 5개로 고정, 호출부는 "지금 값이 얼마인가"만 묻는다. `PerkSet`은 `StageContext`가 소유(규칙을 바꾸므로). `AttackPowerOf`를 `UnitInstance`에 안 넣음(유닛은 스테이지를 모르는 불변, 전투 피해와 상세 표기가 반드시 같은 수여야 함). 특전마다 SO 없이 전역 `PerkTable` 1개. 화면 이름은 한글 하나(뚝심·위로금·흥정·이자·신병 훈련·고참), `PerkId`는 영문.

### 2026-08-06~09 — M0~M5: 포커 코어 / 카드 UI / 그리드·머지 / 전투·웨이브 / 라운드 루프

구조는 DESIGN.md. 이때 정한 것 중 코드에 안 드러나는 것:

- **M0 asmdef 전면 폐지.** "불가능"이라던 판단이 틀렸다 — Test Runner는 asmdef가 아니라 `nunit.framework`를 참조하는 로드된 어셈블리를 스캔하고, 필요한 참조 방향(`Assembly-CSharp-Editor` → `Assembly-CSharp`)은 predefined끼리 자동 성립. 폴더명만 `Editor`로. 포기: `[UnityTest]`·`LogAssert`·PlayMode 테스트·컴파일러의 레이어 강제.
- **M2 교체는 자리 단위로 잠근다** — 한 라운드에 여러 번, 같은 자리는 한 번. 잠긴 자리가 섞인 요청은 손패를 한 장도 안 건드리고 통째로 거부.
- **M3 설계 구멍:** 머지가 "소환 유닛 놓을 때"만이면 ★3에 영원히 도달 못 한다(소환은 항상 ★1). `TryMergeSlots(from, to)` 추가, UI는 2탭. `GridSlot`·`MergeResolver` 클래스는 안 만들었다. **저등급 편중 실측: 스카우트 4기 = 13라운드** (이후 마일스톤의 baseline).
- **M4 보드를 월드 스페이스로.** "기존 87개 테스트 무수정 통과"가 검증 조건이었다(표현만 바꾸고 규칙 코드는 안 건드림). 전투는 순수 C# `CombatContext.Tick(deltaTime)` — 고정 틱을 강제 안 하면서 테스트에서 전투 한 판을 시뮬레이션. 트랙은 `GridBoard`가 계산하는 닫힌 사각 루프, 적 위치는 진행도 하나.
- **M5 `GameFlowController`.** 자동 구간만 맡는다. 새 자동 전이는 `Result` → 다음 `Draw` 하나. `새 라운드` 버튼 제거(루프가 자동이면 어긋남만). `Combat` 진입은 `Place` 페이즈 + 배치 대기 유닛 없음 둘 다 만족해야.
- **한글 폰트.** Maplestory TTF, TMP Dynamic 아틀라스(한글 11,172자라 정적 불가). 커버리지: `♠♥♣★☆◆◇` 있음 / `♦`·`—`·`–` 없음. `CardText` 분리(키카드가 `4h`로 나와 앞면 `4♥`와 어긋났다).
- **전투 중 배치 변경을 정식 규칙으로.** 제보(포커 스킵)는 재현 안 됐으나 전투 중 보드가 전부 열려 있었다(DESIGN §1과 어긋남 → 규칙으로 올림). 먼저 막을 구멍: 이동이 공격 쿨다운을 초기화(슬롯 인덱스로 들어서 — `Dictionary<UnitInstance, float>`로). 제보 증상의 실체는 UI(고른 채 웨이브 종료 시 `selected`가 남음 → `PhaseChanged` 구독).
