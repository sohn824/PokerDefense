# 작업 히스토리

## 이 문서의 관리 규칙

- **최신 항목이 위로.** 각 항목은 `날짜 — 제목` / 결정과 이유 / 검증 순으로 적는다.
- **레거시 정리:** 이후 작업으로 완전히 대체돼 현재 코드·설계와 무관해진 항목은 삭제한다. 뒤집힌 이유가 지금도 의미 있으면 최신 항목에 한 줄로 흡수시킨다.
- **현재 상태 요약**과 **반복 함정**은 항상 최신이어야 한다. 이력을 읽지 않아도 이 둘로 상태를 파악할 수 있어야 한다.
- 설계 내용은 여기 쓰지 않는다. [DESIGN.md](DESIGN.md)가 유일한 설계 출처다.
- 완성도·게임필 개선 아이디어는 [POLISH.md](POLISH.md)에 백로그로 둔다. 착수·완료 시 이 문서에 기록하고 POLISH.md에서 지운다.



---

## 현재 상태 요약

2026-09-13 기준. Claude 인수인계와 코드 지도는 [HANDOFF.md](HANDOFF.md), 현재 설계는 [DESIGN.md](DESIGN.md)를 따른다.

- **방향:** 직접 손패를 맞추는 재미가 중심. 매 손패 선택 교체 1회(일반 교체한 자리 하나, 후보 3장, 공개 시 소진, 이월 없음).
- **제거:** Chip·유지 보너스·판매 수입·상점·보유 카드·랜덤 소환. M12 경제 튜닝 완료 여부는 현행 목표가 아니다.
- **루프:** 5장→교체→확정/1기 소환→배치/합치기/확인 후 교체 또는 소환 포기→전투→결과 비트→새 손패. 50웨이브, 승급권 유지.
- **UX:** 고정된 일반/선택 교체/확정 버튼, 실제 남은 카드 기반 목표 안내, 보드 교체·퇴장·포기 확인, 독립 안내 3종, 메뉴·옵션·회고·위협 안내, 둥근 트랙 구현.
- **씬/구조:** Boot→Title→Game. URP 2D, 모바일 세로, 월드 보드와 uGUI. Poker→Game→UI, asmdef 없음, 테스트는 Scripts/Tests/Editor.
- **데이터/오디오:** 유닛 13종·적 7종·50웨이브·Stage_1. Economy 에셋 없음. 채택 BGM Arena Breaks와 현행 총성 유지; AUDIO_REQUEST 참조.
- **최근 검증:** 직전 구현 작업 EditMode 201/201, Editor Play에서 후보 초기화·W5→W6·가득 찬 보드 확인/취소·메뉴 중첩·360×800 목표 표시 확인. Windows 빌드 성공. 이번 문서 갱신에서 재실행한 결과는 아니다.
- **미완료:** 현행 공급량 기준 정상 50웨이브 사람 런·밸런스 검증, 기기 UX/오디오 QA, TMP Light 폰트 임포터 문제. 런 저장/이어하기는 미구현. 다음 순서는 POLISH 참조.
- **문서 정리:** 과거 §14~17은 LEGACY_LOOP_PLAN으로 분리하고 현행 설계·백로그를 갱신했다. 아래 날짜별 과거 검증은 당시 규칙에만 해당한다.

---

## 반복 함정

같은 곳에 두 번 이상 걸렸거나, 걸리면 원인 찾기 어려운 것들.

### Unity / 코드

| 함정 | 대응 |
|---|---|
| **폰트에 없는 글자** — `♦`(U+2666), `—`, `–`이 Maplestory에 없어 두부(□) | `◆`, `-`로 대체. 새 기호 전에 `HasCharacter(c, false, true)`로 확인(Dynamic 폰트는 인자 없는 `HasCharacter`로 판정 불가) |
| 화면 표기에 `ToString()` | `Card.ToString()`("4h")·`UnitInstance.ToString()`(★)은 로그·테스트용. 화면은 `CardText`·`BoardScreen.Describe()` |
| **에디터 포커스를 잃으면 플레이어 루프가 거의 멈춘다** | `PlayerSettings.runInBackground = true`. MCP로 실시간 동작 볼 때 필수 |
| `manage_build`로 씬 목록 바꿔도 디스크에 안 써짐 | `File/Save Project` 실행해야 `EditorBuildSettings.asset`에 반영 |
| **TMP 폰트 에셋 아틀라스가 리로드·씬 저장 때 부푼다** | 다이나믹 폰트라 `Maplestory Light/Bold SDF.asset`이 1×1 → 1024² 커밋 상태로 바뀐다. 무관한 churn이면 `git checkout`으로 되돌린다. 폰트를 새로 만들 땐 `CreateFontAsset` 후 **서브에셋으로 추가** |
| Input System 전용(`activeInputHandler: 1`)이라 기본 입력 모듈이 터짐 | `EventSystem`에 `InputSystemUIInputModule` 부착 |
| 에디터 켠 채 패키지 제거 → `Library/ScriptAssemblies`에 고아 DLL | 수동 삭제 후 리프레시 |
| 컨트롤러 안 거치고 `GridBoard` 직접 변경 → 화면 굳음 | `PlacementController` 이벤트가 안 나가서다. 검증 스크립트에서 자주 걸린다 |
| **슬롯 인덱스로 든 전투 상태는 유닛이 움직이면 어긋난다** | 공격 쿨다운이 그랬다 — 빈 칸으로 옮기면 그 칸의 0을 물려받아 연사(10배). **유닛을 키로** 들 것 |
| **기존 에셋에 `[SerializeField]` 필드를 새로 넣으면 C# 초기값이 아니라 0** | YAML에 키가 없으면 `default(T)`. 필드 추가 시 기존 에셋에 값을 명시적으로 써넣고, **비어 있지 않은지 검사하는 에셋 가드 테스트**를 붙인다(`visualScale`·`multiSpread`가 그랬다) |
| **`Awake`에서 만든 것을 다른 컴포넌트의 `Awake`가 읽으면 순서에 걸린다** | 실행 순서는 씬 설정에 달려 코드만 봐선 안 보인다. **처음 묻는 쪽이 만들게**(지연 생성) |
| **enum 값을 중간에 끼우면 기존 에셋이 조용히 어긋난다** | 에셋은 enum을 정수로 든다. `EnemyType`은 뒤에만 붙이고(Normal 0 / Swarm 1 / Boss 2 / Runner 3 / Tank 4) 정수 값 고정 테스트 |
| `Tick(0.05f)`를 N번 돌려도 정확히 N×0.05초가 안 됨 | 부동소수 누적이라 살짝 못 미쳐 제한시간 판정을 안 지난다. 한 스텝 더 돌린다 |
| 스크립트로 씬 `RectTransform` 옮길 때 `localPosition` | 직렬화되는 건 `m_AnchoredPosition`. `anchoredPosition`에 쓰고 `RectTransform`도 `SetDirty` |
| **"콘솔에 오류 없음" ≠ "컴파일됨"** | `refresh_unity`가 파일 변경을 못 잡으면(`refresh_triggered: false`) 컴파일 자체가 안 돈다 — 콘솔은 깨끗하고 `execute_code`는 옛 어셈블리를 본다. **`Library/ScriptAssemblies/Assembly-CSharp.dll` 수정 시각을 소스와 비교.** 안 돌았으면 `AssetDatabase.ImportAsset(path, ForceUpdate)` + `CompilationPipeline.RequestScriptCompilation()` |
| **플레이 모드에서는 에셋 갱신·저장이 막힌다** | `refresh_unity`가 30초 타임아웃. 에디터 상태의 `is_playing`을 먼저 볼 것. 플레이 중엔 Unity를 안 건드리는 작업(분할·측정·검산)을 먼저 |
| **플레이 모드에서 만든 오브젝트는 나가는 순간 사라진다** | 씬 편집 전 `Application.isPlaying` 확인. `SerializedObject`로 씬 저장 시 `This cannot be used during play mode` |
| **라벨 넘침을 rect 폭으로 판단하면 틀림** | TMP는 자동 개행이라 넘치면 밖으로 안 나가고 두 줄이 되어 아래를 침범한다. `textBounds`는 개행 후 값이라 항상 rect 안. **`GetPreferredValues`로 재고 `textInfo.lineCount`가 1인지** |
| 에디트 모드에서 뷰 `Show()` 미리보기 → 그대로 씬에 저장됨 | TMP 문자열·색이 직렬화된다. 찍어 본 뒤 `Show(null)`로 되돌리고 저장 |
| **`TMP_Text.fontMaterial` getter가 인스턴스 머티리얼을 만든다** | 진단 스크립트에서 `.fontMaterial`을 읽기만 해도 그 TMP에 로컬 머티리얼이 붙고, 다음 `SaveScene`에 `m_fontMaterial`이 공유 머티리얼 링크를 끊은 채로 직렬화된다(라벨 여럿에 번져 씬 diff가 커진다). 읽기는 `.fontSharedMaterial`로. 이미 번졌으면 씬을 `git checkout` 후 의도한 변경만 다시 적용 |
| **성급 배수는 공격력·공격속도에 동시에 곱해져 DPS가 제곱으로 커진다** | `{1, 1.6, 2.25}`면 DPS ×1 / ×2.56 / ×5.06. 배수를 만질 때는 항상 제곱해서 볼 것 |
| **`spriteAlignment`는 `TextureImporter`에 없다** | `TextureImporterSettings`에 있다. MCP `manage_asset`으로 넘기면 성공 반환 후 조용히 무시(피벗이 Center로 남아 유닛이 슬롯에서 뜬다). `ReadTextureSettings`→수정→`SetTextureSettings`→`SaveAndReimport`, 메타 되읽어 `alignment: 7` 확인 |
| 스프라이트 전환 전 텍스처 높이를 읽으면 NPOT 값 | 407×749인데 `tex.height`가 512. `textureType = Sprite` + `npotScale = None` 먼저 걸고 리임포트 후 다시 읽는다 |
| **`.meta`를 지우고 이미지를 덮어쓰면 GUID는 살아도 임포트 설정이 초기화** | 참조는 안 끊겨 조용하다. `textureType` Default·PPU 100·NPOT 켜짐으로 돌아간다. `.png`만 덮고 `.meta`는 둔다. 덮었으면 메타에서 `textureType: 8`·PPU 확인 |
| **채팅 미리보기의 흰/검은 배경은 투명 여부를 말해주지 않는다** | 파일을 직접 읽어 알파 채널을 확인하기 전까지 배경 판정 보류 |

### 밸런스 측정

| 함정 | 대응 |
|---|---|
| **사거리는 자유롭게 못 내린다** | 가운데 세 칸(6·7·8)이 트랙에서 2.40 떨어져 그 아래면 아무것도 못 때린다. 게임 최단이 2.5인 이유. 크기는 덮는 트랙 비율로 잰다 — 15칸 평균이 2.5에서 24.0% / 3.0에서 36.6% / 3.5에서 51.6% / 4.0에서 66.3% |
| **사거리와 관통 길이를 따로 보정하면 어긋난다** | 둘 다 "한 발이 몇 기를 때리느냐"에 걸린다. 명목 DPS로 맞추지 말고 **유효 피해량**(스폰 HP − 잔여 HP, 오버킬 제외)으로 맞춘다 |
| **오버킬은 명목 DPS가 아니라 ATK의 절대 크기에 달렸다** | 패턴 비교 시 명목 DPS를 낮게 정규화하면 오버킬이 안 일어나 반대 결론이 난다. 실제 유닛 ATK 대역(5~310)에서 잰다 |
| 전투 성능을 클리어 시간으로 재면 스폰 바닥에 걸린다 | 마지막 스폰 시각보다 빨리 못 끝나 강한 보드끼리 같은 값. 제한시간 동안의 유효 피해량으로 잰다 |
| 재현 실행이 필요한 검증 | `Time.timeScale`을 올리고 `EditorApplication.update`에 자동 진행 콜백(끝날 때 스스로 해제). `BalanceSimRunner.cs`(Editor 전용)가 라운드 루프를 끝까지 자동으로 돈다 |
| **회전 대칭 수치가 좋다고 "축이 없어 보인다"는 뜻이 아니다** | 4중 대칭 도형은 90도 회전에 겹쳐 회전-차이 수치가 잘 나온다. "상하좌우가 유독 길어 보이는가"는 눈으로 따로 확인 |

### 생성형 아트 (유닛·적 완료, 배경·트랙에 일부 적용)

| 교훈 | 근거 |
|---|---|
| **비교급 형용사·부정 지시는 약하다 — 원인이 되는 구조·서술 자체를 바꿔야 먹힌다** | "더 둥글게"로 스웜을 그런트와 못 갈랐고(→ 다른 종으로 못박음), "NOT feather-shaped"로 템페스트 머리를 못 고쳤고(→ 길이 자체를 줄임), "낡은 망토"는 거지꼴로 나왔고(→ 손상 서사 폐기, 디자인 모티프로), "sceptre-rifle"은 계속 지팡이로(→ 총기 해부학 용어로만) |
| **상대 지시("다른 칸과 같은 자세로")는 잘 안 먹힌다** | 에이스 후면 팔을 7번 고쳐도 매번 다른 방식으로 어긋났다. 격자선으로 두 컷을 픽셀 비교해야 원인이 보인다. 같은 부위가 3번+ 어긋나면 문구를 더 파지 말고 자세 자체를 관절 자유도가 낮은 형태로 바꾼다(한 손 들기 → 양손 받쳐 들기) |
| **몸통에 건 지시(굵다·넓다·각지다)가 얼굴에도 샌다** | 몸은 크게 / 얼굴은 갸름하게처럼 상반된 지시는 "얼굴은 몸과 달리 ~하다"로 명시적으로 갈라 쓴다 |
| **4방향 시트: 좌우가 뒤바뀌어 나오고, 방향마다 캐릭터 크기가 다르고, 캔버스 정중앙으로 자르면 안 된다** | 프롬프트에 `BOTTOM-LEFT = LEFT`라고 박아도 반대로 나온다(마크스맨). 자르기 전에 **실제 분리 행/열**을 찾고(중앙선 통과 픽셀 카운트), **무기 띠 x중심 vs 다리 x중심**으로 방향을 판정한다. 뒤바뀌었으면 다시 뽑지 말고 엇갈려 배정. 높이 편차가 크면 부츠 비율로 "몸이 진짜 작은지 vs 포즈 차이인지" 가른다 |
| **셀 경계가 안 비는 유기적 형태(촉수·머리카락·날개)는 연결 성분 검사로 고립 조각을 확인** | 레이스 촉수가 상하 셀 경계를 스쳐, 자른 뒤 인접 셀 조각이 본체와 분리돼 붙어 나왔다. BFS로 가장 큰 덩어리만 남긴다 |
| **총구 좌표: 몸 중앙 가정 금지, 방향마다 실측, 찾은 좌표를 그림에 찍어 재검증** | 어깨에 멘 총은 정면·후면 x=0으로 두면 뒤통수에 화염(마크스맨 21.6px, 스카우트 23px). 좌우를 대칭으로 묶을 땐 한쪽 실측값이 아니라 **양쪽의 중점**(보머 좌우 차 5.5px). 금색 링 자동 검출은 근처 금색 장식에 오염되니 좌표를 점으로 찍어 육안 재검증. 무기가 몸에 가려 안 보이는 방향은 정면 보어를 좌우 반전 |
| **화면에 고정된 좌표는 반동을 안 따라간다** | 아트는 발끝 피벗으로 커지는데 화염 좌표가 슬롯 로컬 고정이면 어긋난다. 하필 화염이 보이는 0.06초가 반동 최대 구간. 아트에 스케일을 걸면 거기 붙는 것도 같은 배율을 받아야 한다 |
| **환경 링·프레임은 캔버스 채우기가 아니라 정합 기준선을 수치로 못박아야 하고, "깨끗한 가장자리"는 부정어로 막아야 한다** | 트랙 루프를 "외곽 ~95%"로 주니 고정 두께 밴드가 안쪽으로 밀려 중심선 비율이 목표(가로:세로 1.417)에서 1.67→1.53으로 어긋났다(3회 재생성). 게임이 정합하는 건 중심선이라 중심선 비율·픽셀을 직접 적어야 한다. 슬롯 하이라이트는 "soft bloom"을 낡은 양피지 가장자리로 해석 — `NO torn/frayed/grunge edge`를 넣어야 매끈해졌다(2회). 비율이 조금 어긋난 링은 왜곡 없는 균등 스케일로 흡수 가능(한 축을 맞추고 나머지 축 오차를 두꺼운 밴드가 덮게) |

---

## 이력

### 2026-09-13 — 손패 중심 루프로 구조 개편

- 선택 교체를 매 손패 1회로 옮기고 Chip·유지 보너스·상점·보유 카드·랜덤 소환·판매 경제를 제거했다. 공개 전 취소/공개 후 선택 의무는 유지했다.
- 한 장으로 노릴 수 있는 족보와 보드 합치기 목표를 표시한다. 새 유닛으로 기존 유닛 교체 및 퇴장/소환 포기는 확인 후 실행한다. 희귀 유닛 성장용 승급권은 유지했다.
- 순수 규칙 201/201 통과. Play Mode에서 매 손패 기회 초기화, W5→W6 상점 없이 진행, 15칸 보드 교체 취소/확인, 메뉴 중첩 정지 복원, 승급권 사용을 검증했다.
- Windows 개발 빌드 갱신 성공 (`Builds/LoopReview/PokerDefense.exe`). 1080×1920/360×800 목표 안내와 교체 확인 창을 검사했다. 기존 TMP Light 폰트의 NativeFormatImporter 일관성 메시지는 남아 있으며 게임 스크립트 컴파일 오류는 없다.
- 웨이브 수치는 이번 구조 변경과 섞어 자동 조정하지 않았다. 새 공급량 기준 난도 측정·사람 플레이테스트가 남는다. 현재 규칙은 DESIGN §18.

## 2026-09-13 — 일반·추가 교체를 공통 행동 바로 통합

- 두 교체의 위치가 헷갈린다는 피드백으로 일반 교체 / 추가 교체 / 손패 확정을 같은 하단 줄에 배치했다. 고정 위치·조건별 비활성 사유·2줄 버튼 문구와 상태 안내로 관계를 구분한다.
- 추가 교체 대상이 하나면 자동 선택하되 후보 공개/비용 소비는 별도 확인으로 유지한다. 버튼 높이를 140으로 늘렸다. 카드 확률과 경제 규칙은 그대로다.
- 검증: Play Mode에서 실제 카드 버튼→일반 교체, 추가 교체 조건별 활성화, 한 장 자동 선택/복수 대상 직접 선택, 공개 전 무료 복귀, 공개 시 기회 1회 차감, 메뉴 중첩 후 입력 복구, 확정 후 교체 UI 숨김을 확인했다. 1080×1920·360×800에서 문구 overflow 없음. Windows 개발 빌드 갱신 완료 (`Builds/LoopReview/PokerDefense.exe`). 빌드는 성공했으나 기존 동적 TMP Light 폰트의 NativeFormatImporter 일관성 메시지는 재임포트 후에도 남아 별도 확인 대상으로 기록한다.

## 2026-09-13 — 안내 팝업 분리와 추가 교체 설명 개선

- 기존 UI와 안내가 섞여 보인다는 플레이 피드백을 반영했다. `OnboardingGuide`의 네 안내를 차광막·독립 창·행동 버튼으로 분리하고, 표시 중 전투 및 뒤쪽 입력을 멈춘다. 런당 한 번 표시한다.
- ‘승부 교체’를 ‘추가 교체’로 변경했다. 일반 교체했던 한 장을 다시 바꾸는 목적, 3단계 순서, 후보 카드 자리, 대상이 없을 때의 다음 행동, 공개 비용과 선택 의무를 표시한다. 카드·경제 규칙은 유지했다.
- 메뉴 중첩 시 기존 정지 상태를 복구하도록 수정했다. `GameLoopSetup.UpdateGuideAndAssist`로 해당 UI만 재설치하고 기존 타이틀/메뉴 배치는 유지한다.
- 검증: Windows 개발 빌드 성공(오류 0, `Builds/LoopReview/PokerDefense.exe`). Unity 컴파일 및 콘솔 오류 0. Play Mode에서 첫 손패·배치·합치기·상점 안내, 메뉴 중첩 정지/입력 복구, 전투 시간 정지, 차광막 Raycast, 후보 공개→선택→확정 및 메뉴 복귀 후 후보 유지 확인. 1080×1920 및 360×800 화면 검사에서 표시 문구 overflow 없음. 합치기/상점 발생은 개발 조작으로 재현했으며 정상 런 이해도 재검증은 사람이 진행한다.

## 2026-09-13 — 타이틀·공통 메뉴·트랙·승부 교체 구현

- DESIGN §17 순서에 따라 LOOP-01·TRACK-01과 HAND-01 비교 프로토타입을 코드/씬에 연결했다. `Poker→Game→UI` 참조 방향, System.Random 셔플, 순수 Context 규칙과 Controller 이벤트·씬 직렬화 UI 패턴을 유지했다. 재설치는 `Tools/Poker Defense/Install Game Loop` 메뉴에서 가능하다.
- 새 Title 씬과 Boot 진입, 공통 옵션/정지/포기 확인, 결과 타이틀 복귀를 추가했다. 사용자 음량은 기존 믹스를 보존하는 배율로 저장한다. PC 기본 창은 세로 540×960으로 설정했다.
- 실제 발 위치와 배경을 대조해 둥근 공통 경로를 적용했다. 길이·사거리 체류 비율 변화는 DESIGN §17.8에 기록했다. 관통 테스트의 옛 경로 전제를 새 경로의 사거리 안/밖 명시적 검증으로 바꿨다.
- 승부 교체 후보 공개/선택과 재화 불변식, 보유 카드 제외, 한 라운드 한 번 제한을 추가했다. 후보 선택 중 메뉴 정지/복귀에서도 카드가 유지된다. 플레이 비교 기록은 로컬 JSON이며 저장/이어하기 기능은 아니다.
- 검증: EditMode 241/241. 에디터 Play Mode에서 시작/최종 결과 복귀, 설정 유지, 후보 프리뷰·선택·정지 복귀, 모달 뒤 입력 차단, 전투 시간·적 위치 정지 확인. Windows 개발 빌드 성공. 360×800 긴 화면 옵션/승부 교체 표시, 중첩 메뉴의 CanvasGroup 잠금/복원 확인. 빌드의 headless 초기화 smoke에서 게임 초기화 예외 없음(그래픽·실제 조작 검증은 에디터에서 수행). 정상 사람 런·모바일 터치·SAVE-01은 남아 있다.

- 마지막 CanvasGroup 입력 보완은 컴파일/Play Mode 검증 완료. 이후 에셋 정리의 Refresh에서 Unity MCP ping이 응답하지 않아 **해당 입력 보완을 포함한 최종 재빌드 확인은 보류**했다. 이전 Windows 빌드 성공과 구분한다. 현재 파일 기준 씬 내부 참조/중복 ID 및 diff 검사 통과.

### 2026-09-13 — 전체 게임 루프 개선 계획

- 코드·씬·빌드 목록·경제 데이터를 대조해 DESIGN §17에 시작/옵션/중단/종료, 배경과 이동 경로 정합, 상위 족보 보조의 개발 순서와 완료 기준을 추가했다. 기존 §16 실행 순서와 POLISH 백로그를 연결했다.
- 족보 보조는 남은 덱 후보 3장 중 1장을 고르는 안과 무작위 추가 교체 안을 비교하도록 설계했다. 지급량·비용·채택 여부는 실험 가설이며 현재 게임 규칙이나 데이터를 변경하지 않았다. 상세 규칙은 DESIGN에서만 관리한다.
- UX-09·10 구현을 재개발 대상으로 세지 않고 회귀·사람 관찰 대상으로 유지했다. 트랙 수정은 시각 위치뿐 아니라 사거리·관통·이동 시간에 영향을 주므로 밸런스 측정보다 앞에 뒀다.
- 검증: 문서와 현재 소스/에셋 정적 대조, 문서 diff 검사. 이번 작업에서 게임 실행·실기기 테스트·오디오 변경은 수행하지 않았다.

### 2026-09-12 — UX-10: 첫 플레이 안내 (미커밋)

- **강제 튜토리얼 대신 상황 카드 위 한 줄 안내 4개.** `OnboardingGuide`(신규)가 DESIGN §16.4의 네 순간을 판정한다 — 첫 손패(라운드 1 교체 중) · 첫 배치 대기 · 첫 머지 가능 쌍 · 첫 상점. 각각 세션당 한 번만 뜨고 자연스러운 종료(확정 · 배치 · 머지 · 구매)나 줄 탭으로 사라진다. 족보·확률·상점 가격·보상은 그대로 — 표시만 얹었다.
- **판정은 상태 조건, 종료는 이벤트.** `IsHandHintActive` 등은 `RoundNumber`·`Phase`·`Pending`·보드의 `HasMergePartner`를 매 프레임 다시 읽어 안내를 켠다(ThreatPreview·RoundRecap과 같은 패턴). 종료는 `PlacementController.Placed`(배치/머지)와 보유 카드 수 증가(구매)로 판정해 한 번 닫히면 그 세션에서 다시 안 뜬다.
- **상점 안내는 상점 UI 자체에.** `SituationCard`는 상점 단계에서 꺼지므로(`ActionBarController`) 상점 전용 줄(`ShopHintRow`)을 `ShopPanel`에 따로 뒀다 — `HeldLabel`과 `BuyButton` 사이 빈 공간(y −122, 여백 95px) 사용.
- **"상세 도움말에서 다시 읽기"는 이번 범위에서 뺐다.** POLISH §8의 "족보 → 유닛 참조 패널"(상시 접이식)과 중복 설계가 될 것 같아, 재열람 기능은 그쪽에서 함께 다루기로 미뤘다.
- 검증: EditMode 233/233. 플레이 모드 실측 — 라운드 1 교체 중 손패 안내 → 확정 시 사라짐 → 첫 배치 대기 안내 → 배치 시 사라짐 → `HandCheatTable`로 같은 족보를 두 번 강제해 머지 쌍을 만들어 머지 안내 확인 → 머지 시 사라짐 → 웨이브 5 첫 상점 안내 → 구매 시 즉시 사라짐 → 웨이브 10 두 번째 상점에서는 다시 안 뜸.

### 2026-09-11 — UX-09: 직전 전투 회고 · 이번 적 특성 (미커밋)

- **불변 스냅샷.** `CombatContext.CollectUnresolved()`가 전투 정리 전에 종류별 미해결 적을 센다 — 트랙에 살아 있는 수(Live)와 제한시간 안에 못 나온 수(Pending)를 나눠 담고, 하나도 없는 종류는 뺀다. `UnresolvedBosses`와 같은 순회 방식.
- **RoundSummary 확장.** `BossLeft`·`Remnants`(스냅샷 리스트) 추가. `GameFlowController.OnCombatFinished`는 게임 오버 경로에서도 요약을 만들어 `LastRound`에 남긴다(결과 비트만 건너뛴다). `LastRound`는 다음 전투가 끝날 때 교체된다.
- **표시 세 곳이 같은 스냅샷을 읽고 재계산하지 않는다.** 결과 비트(`RoundBreakScreen`)는 시간 초과 시 `남은 적: 러너 3, 탱커 1 (미등장 2)`를, 상황 카드의 새 `RecapLabel`(`RoundRecap`)은 다음 준비 내내 접힌 `직전 전투 · 웨이브 N …  ▼` 한 줄을 탭 펼침으로, 최종 패배 화면(`ResultScreen`)은 `마지막 웨이브   남은 적 …`을 보여준다. 새 전투가 시작되면 회고 줄은 접힌다.
- **이번 적 특성.** `ThreatBar`를 탭하면 `러너: 빠름 · 체력 낮음`처럼 종류별 사실 한 줄을 펼친다. 특수 종류(무리·러너·탱커·보스)가 하나도 없으면 토글 자체를 숨긴다. 수량 줄은 그대로.
- **명칭 통일.** 종류명·특성 문자열을 `EnemyTypeNames`로 모으고 `ThreatPreview`도 이걸 쓴다. 스웜 → **무리**로 표기 변경(POLISH §3 예시에 맞춤).
- 검증: EditMode 233/233. 플레이 모드 실측 — 웨이브 1 무유닛 시간 초과 시 `LastRound`에 `Normal live 4`, 회고 줄·결과 비트 문구 일치. 웨이브 31(러너 58·탱커 24) ThreatBar 펼침에서 두 특성 줄 표시.

### 2026-09-11 — Arena Breaks 게임 BGM 채택·적용

- 사용자가 이번 후보로 교체를 요청해 끝 페이드 없는 `Arena_Breaks_loop.wav`를 `bgm_combat.wav`에 설치했다. 파일 해시 일치, 오디오 폴더에서 해당 WAV만 변경, meta GUID 유지 확인.
- 이전 WAV/meta는 `Artifacts/Audio/BeforeArenaBreaksInstall/<이전 SHA256>`에 백업했다. 생성기는 후보 렌더 역할을 유지하고 README의 최종 설치 대상을 최신화했다.
- Unity 강제 임포트로 77.41936초·2채널·3,716,129 samples 확인. 준비·전투·보스·결과 4개 직렬화 슬롯이 모두 새 클립을 참조한다. 씬·SFX·볼륨 변경 없음.
- 기존 곡의 고정 샘플 수를 검사하던 테스트를 채택곡의 124 BPM·40마디 계산식으로 갱신하고 48kHz 검증을 추가했다. 오디오 EditMode 테스트 13/13 통과.

### 2026-09-11 — 더 경쾌한 Arena Breaks 후보

- 사용자가 Arena Drive를 이전보다 긍정적으로 평가하고 alexguz 펑크/브레이크비트 MP3를 추가 제공했다. 로컬 분석: 124.84초, 템포 약 123~127 BPM 후보, 중간 완화와 후반 에너지 상승. 직접 청취나 채보로 확정한 값은 아니다.
- 124 BPM / 77.42초의 별도 후보. 짧은 마이너7/9 기타 코드, 옥타브 응답 베이스, 고스트 스네어·엇박 킥, 브레이크→빌드→재진입. 독립 주선율 없이 리듬을 중심에 두었다. 이전 후보의 악기 함수는 재사용하되 악보는 새로 작성했다.
- `compose_arena_breaks.py` 추가. `Artifacts/Audio/ArenaBreaks`에 전체 청취본·루프·30.97초 하이라이트와 수치/악보/소스 기록 생성. 첨부 녹음은 출력에 포함하지 않았다.
- PCM 검사: RMS -15.90 dBFS, sample peak -1.50 dBFS, 클리핑 0, 루프 끝점 차이 0. 이전 후보와 게임 음원은 유지하며 런타임/메타 해시 불변 확인. 사용자 청취 비교 대기.

### 2026-09-11 — 첨부 록 음원 분석과 Arena Drive 후보

- 사용자 첨부 `magpiemusic-action-race-rock-music-513682.mp3`를 로컬 디코딩해 분석했다. 126.09초 / 44.1kHz stereo, 구간 에너지와 스펙트럼 온셋을 측정. 템포 후보는 약 100/133 BPM으로 모호하며 직접 청취나 정확한 채보를 했다는 의미는 아니다.
- 100 BPM / 76.8초의 리프 중심 후보를 별도 작곡. 이전 주선율 배열 없이 저음 기타 파워코드·드럼·베이스, 브레이크→빌드→후반 고조를 구성했다. 원곡 녹음은 출력에 포함하지 않았고 기존 CC0 기타/드럼 소스와 합성 베이스·타악을 사용했다.
- `analyze_rock_reference.py`, `compose_arena_drive.py` 추가. `Artifacts/Audio/ArenaDrive`에 루프·전체 청취본·28.8초 하이라이트, 수치 검증과 소스 해시를 저장했다. MP3 디코더는 로컬 Artifacts 의존성이며 Unity에는 필요 없다.
- 검증: 출력 PCM48kHz stereo16bit, RMS -14.50 dBFS, sample peak -1.58 dBFS, 클리핑 0, 루프 끝점 차이 0, 게임 오디오/메타 해시 불변. 기존 BGM 미교체, 사용자 청취 판단 대기.

### 2026-09-10 — 반주 없는 주선율 3안

- Iron Oath도 이전에 싫어했던 멜로디와 비슷하게 들린다는 사용자 피드백에 따라 편곡을 멈추고 선율만 비교한다. A 절제된 비장함(낙하·쉼), B 전투감(동음 반복·엇박 도약), C 결의(긴 상승 아치)로 독립 작성했다.
- 같은 CC0 하프·112 BPM·17.74초·RMS -20 dBFS. 반주·드럼·기타·추가 리버브 없음. 악기 최종 채택이 아닌 선율 비교용이며 기존 곡 배열을 재사용하지 않았다.
- `audition_melody_sketches.py` 추가, `Artifacts/Audio/MelodySketches`에 WAV 3개·음표 목록·검증 보고서 출력. PCM 클리핑 0, 게임 오디오/메타 해시 불변. 주관적 선호는 사용자 청취 후 결정한다.

### 2026-09-10 — 비장한 전투 BGM 후보 Iron Oath

- 사용자 레퍼런스: 파랜드 택틱스 2 「폭풍 PART2」. 영상/음원 접근 실패로 직접 청취·채보하지 못했고, 진지하고 비장한 분위기 요청에 따라 별도 원곡을 작곡했다.
- 128 BPM / D minor / 48마디·90초. 샘플 기타의 저음 리프·주제 A/B, 드럼·베이스, 낮은 현 배경과 드문 하프 음. 도입→주제→숨 고르기→빌드→재현 구조로 새로 구성했다.
- `Tools/Audio/compose_iron_oath.py`와 재생성 안내 추가. `Artifacts/Audio/IronOath`에 루프 WAV·전체 청취본·30초 발췌 및 검증 보고서를 출력했다. 기타/오케스트라 소스는 기존 CC0 샘플이며 음성·레퍼런스 음원은 사용하지 않았다.
- 측정: 48kHz stereo PCM16, 90초, RMS -16.62 dBFS, sample peak -1.80 dBFS, 렌더 루프 끝점 차이 0. 런타임 오디오/메타 해시 불변 확인. 음악적 품질과 방향 채택은 사용자 청취 판단이며 게임에는 미적용.

### 2026-09-08 — 투기장 UI 비주얼 패스

- Game 씬의 HUD·위협 바·상황 카드·손패 판·하단 바·상점/결과·랜덤 소환 판을 흑철색과 절제된 황동 테두리로 통일했다. `ArenaPanelGraphic`은 절삭 모서리·얇은 안쪽 선을 UI 메시로 그린다. 넓은 면의 강한 그라데이션은 화면 검토 후 제거했다.
- `ButtonSkin`에 프레임 참조와 눌림/놓음/영역 이탈 피드백을 추가했다. 주 행동은 낮은 채도의 녹색, 판매는 적갈색, 비활성은 중립색. 버튼 크기·클릭 이벤트·게임 규칙은 유지했다. 장식의 raycast는 모두 껐다.
- 족보 제목을 56으로 조정하고 본문/제목/보조 글자색 및 버튼 내부 여백을 정리했다. 라운드 결과 판 폭을 본문보다 넓게 맞췄다. `ArenaUiVisualSetup` 메뉴로 재적용 가능하며 씬에 저장했다.
- 검증: 컴파일 오류 없음, EditMode 233/233 통과. Play Mode 교체·배치·개발용 웨이브 점프로 상점 캡처, 샘플 RoundSummary로 결과 표시를 확인했다. 교체/상점 활성 TMP overflow 없음, 장식 raycast 없음, 버튼 눌림 색 변화/복귀 확인. 전체 런·실기기·모달 입력 회귀 완료를 뜻하지 않는다.
- 캡처: `Artifacts/UIVisual/final-exchange.png`, `final-shop-1.png`. 에디터를 Play Mode가 아닌 상태로 복귀했다. 미리보기에서 생긴 동적 폰트 캐시 변경은 제외했다.

### 2026-09-08 — UX 적용 상태 대조 및 후속 기획 최신화

- Git HEAD `2d08e37`과 UI·GameFlowController·RunStats·직렬화된 Game 씬을 대조했다. 점검 시작 시 작업 트리는 깨끗했다. 아래 과거 작업의 “커밋·푸시 없음”은 작성 당시 기록이며 현재 미커밋 상태를 뜻하지 않는다.
- DESIGN §14·15의 전부 미구현 표기를 부분 구현 상태로 수정하고 개편 전 진단을 명시했다. 실제 결과 비트 1.8초와 제거한 Act 진행 바 결정을 반영했다.
- POLISH를 구현 상태표와 다음 실행 백로그로 분리했다. HISTORY 현재 요약의 테스트·오디오·루프 상태를 갱신했다.
- DESIGN §16에 터치/모달 회귀, 직전 전투 회고, 첫 플레이 안내, 단계별 시간과 Act 검증의 범위·수용 기준·착수 순서를 추가했다. 게임 규칙·코드·씬은 변경하지 않았다.
- 검증: 소스·씬 참조·Git 이력 및 문서 diff 대조. 런타임 테스트·실기기·사람 관찰은 이번 작업에서 수행하지 않았으며 기존 233/233 기록과 구분한다.

### 2026-09-08 — UX-08: 결과 비트 · Act 예고 (커밋 2d08e37)

- **라운드 전환 비트.** 지금까지 전투 종료 → 즉시 다음 손패(또는 상점)라 결과를 읽을 틈이 없었다. `GameFlowController`가 전투 종료 시 `RoundSettled(RoundSummary)`를 쏘고 `AdvanceRound`를 콜백 하나에 담아 대기 → `DismissBreak()` 한 번으로만 다음 단계로 넘어간다("정산·전이 1회"). 딜 사운드를 트리거하는 `StartNextRound`가 비트 뒤에만 돌아 딜음 중첩도 자연 해소.
- **`RoundBreakScreen`(신설).** `웨이브 N 클리어` / `웨이브 N 시간 초과` + (클리어: `적 전멸` 또는 `적 전멸 · 조커 +N` / 시간초과: `M마리 남음 · 라이프 -K`). **Chip·유지 보너스는 표시 안 함**(확정 때 이미 지급). ~1.8초 자동 진행 + 아무 데나 탭하면 즉시 진행. 텍스트 뒤 어두운 판(경기장 위 가독성).
- **Act 예고.** 다음 라운드가 11·21·31·41이면 결과 비트에 `다음 · ACT N · {초점}` 한 줄 덧붙임(DESIGN 14.4의 "결과 전환 안에 넣는다" — 별도 배너·닫기 없음). Act 2~5 한마디: 여러 적이 섞여 온다 / 단단한 적이 늘어난다 / 물량이 빽빽해진다 / 최대 밀도, 총력전. 상점 웨이브(10·20·…) 직후라 순서는 `결과 비트 → 상점 → (다음 라운드 시작) ACT 줄`.
- **진행 바는 넣었다가 뺐다.** `구간 N/5` 라벨 + 5칸 부분 채움 세그먼트까지 만들었으나 사용자 판단으로 제거(`ActProgressBar` 스크립트·`ActBar` 오브젝트·`GameFlowController.CurrentAct` 삭제). Act 위치는 결과 비트의 `다음 · ACT N` 줄로만 전달.
- **게임 종료.** `IsGameOver`/`IsAllWavesCleared`면 결과 비트 없이 기존 최종 `ResultScreen`으로 직행.
- 검증: EditMode 233/233. Play Mode 실전 전투로 클리어/시간초과 각 케이스의 비트 문구·자동 진행·전이 1회(`RoundNumber` 1→2), 탭 스킵, Act 줄(웨이브 10 → `다음 · ACT 2 · 혼합 대응`)을 스크린샷·리플렉션으로 확인. 씬·`GameFlowController` 변경 + `RoundBreakScreen` 신설. 커밋·푸시 없음.
- 남은 것: 상점 웨이브 순서 실플레이 확인, 비트/Act 배너 연출 마감(현재 텍스트+판), 실기기 확인.

### 2026-09-08 — UX-07: 위협·사거리·정보 연결 (커밋 46fe6a2)

- **이번 적 미리보기.** `ThreatPreview`(신설, `RoundScreen`에 부착) — HUD 바 아래 `ThreatBar` 한 줄에 이번 웨이브 적을 종류별로 묶어 `이번 적 · 무리 12 · 탱커 1 · 보스` 형태로. `StageContext.CurrentWave.Entries`를 `EnemyType`으로 집계(Normal→일반 / Swarm→무리 / Runner→러너 / Tank→탱커 / Boss→따뜻한 붉은색). 전투 중·상점·게임 종료엔 숨긴다(전투 중엔 남은 적 표기가 따로 있음). 컴포넌트는 항상 켜진 `RoundScreen`에 두고 바만 껐다 켠다.
- **역할 우선 + 머지 안내.** 상황 카드 상세 첫 줄을 `{이름} ★N · {역할}`로 바꾸고, 배치 대기/선택 유닛에 머지 안내 한 줄 추가 — 대기: `같은 유닛·성급 칸에 겹치면 ★{N+1}`, 선택: `HasMergePartner`면 `★{N+1}로 머지할 상대가 있습니다`, 아니면 `같은 유닛 ★N 둘이면 ★{N+1}`, 최대 성급이면 `최대 성급 - 더 못 올림`.
- **수치는 탭으로 펼침/접힘.** 공격력·초당·DPS·사거리는 기본 접힘. 상황 카드(=`DetailPlate`에 `Button`, 카드 위 라벨은 `raycastTarget` 끔)를 탭하면 `detailExpanded` 토글 → 첫 줄 끝에 `▼`/`▲`. DPS는 `명목 DPS`로 표기(실전 기여로 오독 방지). 상태는 세션 동안 유지.
- **선택 사거리 링.** `BoardScreen`이 `LineRenderer`로 반지름 1 원을 한 번 만들어 두고(에셋 없음, `Sprites/Default` 머티리얼), 유닛 선택 시 그 칸 월드 좌표에 실제 `Range`로 48각형을 그린다. `Refresh` 끝에서 갱신, 선택 없으면 끈다. 콜라이더 없어 다른 칸 탭을 막지 않는다.
- 검증: EditMode 233/233. Play Mode에서 교체/준비/전투 각 상태의 위협 바 표시·숨김, 상세 접힘/펼침, 머지 안내 분기, 사거리 링 렌더를 스크린샷·리플렉션으로 확인. 보스 웨이브 문자열은 `Describe`를 웨이브 에셋에 직접 호출해 확인(#5 `일반 3 · 보스`, #50 `일반 23 · 탱커 29 · 보스 9`). 씬·`BoardScreen` 변경 + `ThreatPreview` 신설. 커밋·푸시 없음.
- 남은 것: 이번 적을 아이콘으로(현재 텍스트), 실기기 확인, 사거리 링 시각 마감(반지름 2.5는 보드 대부분을 덮음 — 데이터 그대로).

### 2026-09-08 — UX-06: 공통 비주얼 정리 (커밋 8ca1795)

- **공통 토큰 한곳으로.** `UiStyle`(static) 신설 — 패널 바탕(`PanelBg`)·정보판(`Plate`)·모달 막(`Scrim`), 버튼 3역할(`ButtonPrimary`/`ButtonSecondary`/`ButtonDanger`)·비활성 바탕(`ButtonDisabled`), 본문 3단계·재화 금색, 타이포 4단계 상수(제목 72 / 행동 40 / 본문 34 / 보조 30), 간격 배수 8. 씬 세팅 코드와 런타임 위젯(`CardView` 배지)이 같은 값을 참조한다.
- **버튼 통일.** 하단 액션 6종 + 상점 2종 + 결과 1종에 공통 둥근 9-slice 스프라이트(빌트인 `UISprite`) + `ButtonSkin` 컴포넌트. `ButtonSkin`이 역할색을 입히고 `Selectable.IsInteractable()`가 false면 바탕만 어두운 중립색으로 죽인다(라벨은 그대로 둬 비활성 사유 문구가 계속 읽힘 — Linear 색공간에서 `ColorTint` 비활성 틴트가 화면에 잘 안 실려서 명시적 컴포넌트로 대체). 라벨은 Bold + 단계별 크기, `enableWordWrapping = true` / `enableAutoSizing = false`(자동 축소로 겹침 숨김 금지). 주 행동은 초록 채움·큰 글자, 보조는 어두운 바탕·작은 글자.
- **정보판이 돌바닥 위에 뜨지 않게.** `BoardScreen`이 유닛 상세일 때만 켜던 `detailPlate`(+ 필드) 제거 — 상황 카드 배경판을 상시 켜서 `situationCard`와 함께 뜨고 진다(교체·전투·배치 대기 모든 문구가 판 위에 얹힌다). `HandPanel`·`BottomActionArea`에도 같은 배경판(`HandPlate`/`BottomBarPlate`).
- **긴 유닛명 대응.** 상황 카드에 `ContentSizeFitter`(세로 PreferredSize) + 라벨 고정 높이 해제 → 이름이 이름 영역에서 줄바꿈되며 카드·배경판이 같이 자란다. 가장 긴 이름("로열 스트레이트 플러시 소버린 ★★★ …")에서 잘림 없음 확인. 최소 높이 96 보장.
- **패널 바탕 통일.** 상점·결과 = `PanelBg` 불투명, 랜덤 소환 = 스크림 + 안쪽 본문판 `Plate`.
- **Safe Area.** `SafeAreaFitter` 신설 후 `RoundScreen`에 부착(에디터 Game뷰는 `Screen.safeArea`가 전체라 눈에 안 띔 — 실기기 확인 대상).
- 검증: EditMode 233/233. Play Mode에서 준비·유닛 선택·배치 대기·전투·상점 상태를 스크린샷·리플렉션으로 확인. MCP 스크린샷은 Linear 버퍼를 감마 보정 없이 캡처해 실제 기기보다 밝게·연하게 보인다(색 대비는 기기에서 더 강함) — 픽셀 단위 색 검증은 미실시. 씬·`BoardScreen`·`CardView` 변경 + 스크립트 3종 신설. 커밋·푸시 없음.
- 남은 그레이박스: 보조 버튼 테두리/외곽선, 실기기 Safe Area·360~430 폭 확인, 결과 화면 실플레이 확인(구조는 상점 패널과 동일), 눌림·처리 중 상태 시각 피드백.

### 2026-09-07 — UX-05: 상점 선택→구매 흐름 (미커밋)

- **탭 = 즉시 구매를 탭 = 선택으로 바꾸고, 하단 구매 버튼으로만 산다.** `ShopScreen`에 `selectedCard` + `BuyButton`(씬 신설) + `OnBuy()`. 카드 확인만으로는 Chip이 안 나간다. 구매 성공 시 선택을 풀어 연타가 다음 카드로 이어지지 않게 한다. 추가 확인 모달은 없다.
- **보유 카드 실제 표시.** `HeldLabel`(씬 신설)에 `보유 카드 7◆ K◆ 2♥  (빈 1)` — 실제 숫자·무늬 + 빈 칸. 다음 손패 추천은 하지 않는다.
- **구매 완료 상태.** `CardView.CardState.Bought` 추가(배지 "구매 완료", 축소·반투명). 상점에서 고른 카드는 `TrayPick` 재사용(배지 "선택"). `CardView.SetSelected`는 이제 안 쓰여서 제거.
- **실행 불가 구분.** `Chip 부족` / `보유 카드 가득` / (이미 산 카드는 `구매 완료` 배지로 못 고름). 사유는 `infoLabel`과 구매 버튼 라벨에 문구로. 선택·정보 확인은 막지 않고 구매 실행만 막는다.
- 상점 나가기 버튼 문구 `상점 나가기 (다음 라운드)`.
- 검증: EditMode 233/233. Play Mode에서 DevSkip으로 상점(라운드 5) 진입 → 선택→구매(Chip 20→16, 보유 0→1, Slot=Bought), 보유 3/3 가득에서 선택은 되고 구매만 막힘을 리플렉션·스크린샷으로 확인. `Game.unity`에 `BuyButton`·`HeldLabel` 배선. 커밋·푸시 없음. 구매 버튼의 비활성 색상 통일은 UX-06.

### 2026-09-07 — UX-04: 버튼 비용·사유 노출 (미커밋)

- **버튼에 비용·반환액·남은 횟수를 항상 표시.** 랜덤 소환 `랜덤 소환 · {비용} Chip · 남은 {N}회`, 판매 `판매 +{반환액} Chip`, 조커 `조커 ★{N} → ★{N+1} · Joker {보유}` (DESIGN §15.5).
- **비활성 사유를 회색만이 아니라 문구로.** 랜덤 소환: `Chip {현재}/{필요}` / `남은 0회` / `빈 칸 없음`. 조커: `Joker 없음` / `이미 최대 성급`. `BoardScreen.RandomSummonBlockReason()` · `JokerBlockReason()`.
- **상점**: 못 살 때 `infoLabel`에 `· Chip 부족` / `· 보유 카드 가득` 사유를 붙인다. 카드 선택 → 구매 버튼 분리는 UX-05.
- **연타·중복 소비**는 기존 `Try*` 반환값 가드 + 랜덤 소환 패널의 전체 화면 raycast 차단으로 이미 막힘을 확인(별도 "처리 중" 상태 불필요). 조커·판매도 성공 시 `selected`/`Pending`이 즉시 비워져 두 번째 탭이 무효.
- 검증: EditMode 233/233. Play Mode에서 Chip 0/8, 사용 횟수 소진, Joker 유무·최대 성급에 따른 라벨을 리플렉션으로 확인. 씬 변경 없음. 커밋·푸시 없음. 긴 라벨이 좁은 버튼에서 줄바꿈되는 건 UX-06(버튼 높이·타이포 고정).

### 2026-09-07 — UX-03: 보드 조작 예측·실패 처리 (미커밋)

- **누르기 전 결과를 문구로 미리 보여준다 (DESIGN §15.5).** 유닛을 고르면 각 칸에 `이동` / `머지 ★N`(도착 성급) / `교환` / `선택`이 뜬다. `UnitSlotView`에 `actionHint`(starLabel 복제로 폰트 물려받음, `Bind`에서 생성)를 추가하고 `BoardScreen.Refresh`의 선택-상태 분기를 칸별 `HintFor(board, i)`로 세분화.
- **실패 시 선택 유지.** `OnSlotClicked`가 이동/머지/교환 성공 시에만 `selected`를 비운다. 실패하면 선택을 남겨 다른 대상을 고를 수 있게 하고 "그 자리에는 할 수 없습니다"를 알린다.
- **못 놓는 칸 이유 표시.** 대기 유닛이 있을 때 못 놓는 칸도 `SetInteractable(true)`로 열어 두고, `TryPlace` 실패 시 소비 없이 "여기엔 놓을 수 없습니다 · 같은 유닛·성급 칸에만 겹칠 수 있습니다"를 안내.
- **빈 배경 탭으로 선택 해제.** `Update`에서 `OverlapPoint`가 아무것도 못 맞히면(그리고 UI 위가 아니면) 고른 유닛을 해제한다.
- UI 터치가 보드로 통과하는 문제는 기존 `IsPointerOverGameObject()` 가드 + 랜덤 소환 패널의 전체 화면 차단으로 이미 막힌다. 실기기 터치 ID 검증은 미실시(수동 QA 대상).
- 검증: EditMode 233/233. Play Mode에서 이동/머지 ★N/교환/선택 문구, 머지 성공 후 선택 해제, 못 놓는 칸 이유(대기 유닛 유지)를 리플렉션으로 확인. 씬 변경 없음(actionHint 런타임 생성). 커밋·푸시 없음.

### 2026-09-07 — UX-02: 카드 상태 표시 (미커밋)

- `CardView`에 `CardState` 6종(Normal / Pick / Locked / Placed / Target / TrayPick)을 넣고 **색이 아니라 크기·투명도·테두리·아래쪽 배지 문구**로 구분한다 (DESIGN §15.4). Pick·TrayPick은 1.08배 확대 + "교체"/"선택" 배지, Locked·Placed는 0.92배 축소 + 알파 0.5 + "교체 완료"/"배치 완료" 배지, Target은 테두리 + "여기 놓기". 배지는 프리팹이 없어 인스턴스마다 `Awake`에서 코드로 붙인다.
- **교체(유지 보너스 깎임)와 트레이 배치(안 깎임)를 문구로 구분.** `RoundContext.locked`는 둘을 한 플래그로 묶으므로 `RoundScreen`이 `heldPlacedSlots`를 따로 세어 "배치 완료"를 "교체 완료"와 갈라 보여준다. 라운드 진입 시 리셋.
- **보너스 프리뷰 분리.** 확정 버튼은 현재 보너스(`확정 +N`), 상황 카드는 고른 장수만큼 교체 시 예상치("N장 교체 시 유지 보너스 +X → +Y"). "남은 교체 N칸 (자리당 1회)" 상시 문구는 "변경 가능 N장"으로 줄이고 '자리당 1회'는 첫 교체 전에만 안내.
- **트레이 취소.** 배치 모드에서 교체 버튼이 "선택 취소"로 바뀐다(재탭 취소도 유지). 잠긴 자리를 누르면 카드·Chip 소모 없이 "이미 변경했습니다"만 안내.
- 검증: EditMode 233/233. Play Mode에서 6개 상태 전이(고르기 → 교체 완료, 트레이 선택 → 여기 놓기 → 배치 완료) + 취소가 재화·잠금을 건드리지 않음을 리플렉션으로 확인. 씬 변경 없음(배지는 런타임 생성). 커밋·푸시 없음.

### 2026-09-07 — 지원 소환 → 랜덤 소환 개편

- **명칭 전면 변경.** `SupportSummon`→`RandomSummon`, `TrySupportSummon`/`CanSupportSummon`/`SupportSummonCost`/`SupportSummonsUsed`, `EconomyDefinition`의 `supportPool`/`supportSummonCost`/`supportSummonsPerRound`(→`randomPool` 등, `FormerlySerializedAs`로 `Economy.asset` 자동 이전), UI의 `supportButton`/`supportLabel`/`OnSupportSummon`, `BalanceSimRunner`, `EconomyTests`까지. 버튼 문구 "랜덤 소환 6".
- **전투 중 사용 개방.** 모델(`CanRandomSummon`)은 원래 `Phase == Place`만 봐서 이미 전투 중 허용됐고, `ActionBarController`가 Combat 단계에서 버튼을 숨기던 것을 노출로 바꿨다. `CombatContext.Attack()`이 매 Tick `board[slot]`을 라이브로 읽어 전투 중 추가한 유닛이 즉시 사격에 참여함을 확인. 라운드당 1회 제한은 준비+전투 합산 그대로(밸런스 근거 DESIGN §9.3) — 데이터 `randomSummonsPerRound: 1` 유지.
- **자동 배치 → 배치 대기 + 슬롯머신 연출.** `TryRandomSummon`이 첫 빈 칸에 바로 놓던 것을 `Pending`으로 올리도록 바꿨다(포커 확정과 동일 흐름 — 플레이어가 칸을 고름). `PlacementController.RandomSummoned(UnitDefinition)` 이벤트, 새 `RandomSummonMachine`(Canvas 오버레이 패널 `RandomSummonPanel`)이 `[아트][이름]` 한 줄 항목 20개를 세로로 밀어 올리다 감속해 뽑힌 유닛에서 정지 후 페이드. 패널은 전체 화면 배경으로 그 사이 보드 탭만 막고 전투 시뮬은 안 멈춘다. 패널 캐비닛·레버·7-7-7 아트는 그레이박스(UX-06/아트 패스).
- `ActionBarController`: 대기 유닛 존재 시 판매 버튼·안내 라벨을 `stage`와 무관하게 노출(전투 중 랜덤 소환 대기 유닛도 배치 안내가 뜨도록).
- 검증: EditMode 233/233. Play Mode에서 릴이 뽑힌 유닛에서 정지, 닫히면 대기 유닛으로 배치, 전투 중 버튼 노출, 라운드당 1회 소진 후 비활성 확인. `Game.unity`에 `RandomSummonMachine` + `RandomSummonPanel` 배선. 커밋 23d49c8.

### 2026-09-06 — 미채택 오디오 Git 제외

- VOICE_LICENSE는 미채택 남성 가창 MIT 고지. 현재 기타곡은 가창 미사용. 고지 사본/가창 다운로드 도구와 참조 없는 구 BGM plan/boss/result WAV+meta를 정확한 경로로 ignore 추가. 파일은 삭제하지 않음.
- 이전 3곡 GUID의 Assets 참조 없음 확인. 루프 테스트도 채택곡만 대상으로 수정하여 새 클론에서 구 파일이 없어도 검증 가능. 활성 56WAV와 메타·생성 의존 소스·출처 문서는 유지.

### 2026-09-06 — 권총 실제 녹음 적용

- 스카우트/건슬링어에 FirePistol 전용 큐 추가. 공유 Rapid 음은 에이스 등 다른 유닛을 위해 유지. enum 끝에 추가하여 기존 ID 유지.
- CC0 1911 근거리 녹음 2발+채널 배합 변형, 160ms/48kHz/모노/-3dBFS. gain .22, cooldown .075, pitch variation 0. Scene 및 재설정 도구 연결. 현재 33큐/59WAV.
- 출처/재현은 FIREARM_SOURCES.md, 미리듣기 RecordedPistol/Pistol_Preview.wav. BGM/로열 라이플 변경 없음.

### 2026-09-06 — 게임 전체 BGM 연결 수정

- 원인: 전투 WAV는 선택한 기타 루프와 SHA-256 동일했으나 AudioManager.Start는 bgmPlan, 보스/종료는 별도 bgmBoss/bgmResult를 선택. 이전 교체가 일반 전투에만 적용되어 예전 곡이 나옴.
- Game 씬의 준비·전투·보스·결과 4참조를 선택한 bgm_combat로 통일. AudioPolishSetup 재실행 시에도 같은 연결이 유지되도록 수정. 기존 다른 WAV는 보존하되 현재 씬에서 미사용. 동일 클립이므로 상태 전환 때 곡을 재시작하지 않음.

### 2026-09-06 — 기타 반주 버전 전투 BGM 채택

- 사용자 선택에 따라 GuitarRhythmOnly/guitar_loop.wav를 bgm_combat.wav에 그대로 적용. 메인 선율 없는 드럼·베이스·기타 반주, 48kHz/스테레오/16bit/76.8초. 끝 페이드 없는 루프 원본 사용, 게인 변경 없음.
- 전투 WAV만 해시 변경됨을 확인. 메타 GUID/총성/다른 곡 유지. 이전 전투곡 BeforeGuitarRhythmInstall에 백업. 현재 런타임 선택은 기타 반주 버전.

### 2026-09-06 — 메인 선율 없는 기타 반주 후보

- --no-lead로 주선율/옥타브 중복을 제외한 GuitarRhythmOnly/guitar_preview.wav 및 루프 제작. 직전 후보의 드럼·베이스·기타 반주와 마스터 전 배열 동일 검증.
- 76.8초, RMS -17 dBFS, 피크 -1.94 dBFS, 루프 끝점 차이 0. 게임 오디오/메타 유지.

### 2026-09-06 — 기타 메인 선율만 교체한 후보

- 선호한 비트/기타 반주는 유지하고 주선율을 낮은 음역·짧은 엇박 문답형으로 교체. 후반 옥타브 중복 제거. GuitarLeadAlternative/guitar_preview.wav.
- 직전 GuitarDeveloped와 배경 및 기타 반주 배열이 마스터 전 샘플 단위 동일함을 검증. RMS -17 dBFS, 피크 -2.25 dBFS, 루프 끝점 차이 0. 런타임 오디오/메타 변경 없음.

### 2026-09-06 — 기타 후보 멜로디 발전

- 후보 생성기에 --guitar-developed 추가. 저음 리프 4종, 6.4초 주선율 진입, 25.6초 별도 낮은 응답, 38.4초 주제 복귀·후반 옥타브 보강. 주선율은 긴 감쇠와 약한 왜곡으로 반주와 구분.
- GuitarDeveloped/guitar_preview.wav, 76.8초. 드럼/베이스/배경은 직전 후보와 샘플 단위 동일. RMS -17 dBFS, 피크 -2.42 dBFS, 루프 끝점 차이 0, 모든 런타임 오디오·메타 동일. 게임곡 ArenaSmooth 유지, 후보 채택 대기.

### 2026-09-06 — 일렉기타 / 남성 가창 레이어 비교 후보

- 현재 ArenaSmooth 게임곡 유지, 동일 드럼·베이스·배경으로 76.8초 후보 2개 제작. 기타는 CC0 FreePats 실제 녹음의 짧은 리프/왜곡, 합창은 MIT vocobox 남성 1인 가창의 다중 레이어. 실제 합창단 녹음은 아님.
- GuitarChoirCandidates의 두 preview WAV는 RMS -17 dBFS로 일치, 피크 기타 -2.46/합창 -1.85 dBFS, 클리핑 없음·루프 끝점 차이 0. 모든 런타임 오디오/메타 해시 동일 확인. Unity 에셋 변경 없음.
- 출처·재현: Tools/Audio/GUITAR_CHOIR_CANDIDATES.md. MIT 고지문은 소스 폴더와 후보 출력에 동봉. 최종 후보 채택 대기.

### 2026-09-06 — 타격 잡음 후보 제거 및 금관 프레이즈 정리

- 사용자 지적의 탁탁 소리는 직접 청음으로 특정하지 못했으므로 코드상 후보인 금속 틱·고스트 스네어·스네어 필·전환 노이즈·거친 공기 질감·반복 짧은 현악을 제거. 하이햇 음량을 줄이고 고정 고주파 사인 성분 제거. 기본 킥/백비트 유지.
- 짧은 금관 문답을 긴 저음 금관 프레이즈와 쉼표로 교체. 현재 미리듣기 ArenaSmooth, 백업 BeforeArenaSmooth. RMS -14.77 dBFS, 피크 -2 dBFS, 루프 끝점 차이 0. 주관적 오락실 느낌 해소는 청취 확인 필요.

### 2026-09-06 — 폐허 투기장 전투곡

- Stage/Background.png의 무너진 석벽·암회색 바닥을 직접 확인하고 어두운 투기장 방향으로 편곡. 저음 금관의 새 반음 모티프, 긴 저음 현악 질감, 거친 공기·짧은 금속 타격을 배치. 합성 합창 제거.
- 엇박 킥·실제 스네어·세분 하이햇, 짧은 저음 펄스로 현대적 리듬 유지. 조용한 중간부 없음. 150 BPM/76.8초, RMS -14.76 dBFS, 샘플 피크 -2 dBFS. 미리듣기 RuinedArena, 이전 버전 BeforeRuinedArena. 주관적 청취 채택 대기.

### 2026-09-06 — 금관·합창풍 전투 편곡

- 현악 주선율을 제거하고 실제 CC0 호른/트롬본의 짧고 넓은 문답형 주제로 재설계. 현악은 저음 짧은 반주만 담당. 기존 킥·스네어·하이햇 유지.
- 낮은 무가사 합창풍 배경은 직접 모음 합성한 음색이며 실제 사람 합창 녹음이 아님. 금관 소스 추가 후 검증된 샘플 총 114개. 현재 미리듣기 Artifacts/Audio/BrassChoir, 백업 BeforeBrassChoir.

### 2026-09-06 — 드럼 비트와 주선율 강화

- 팀파니 백비트를 실제 CC0 스네어 2테이크로 교체. 매 박 하이브리드 킥(녹음 큰북+짧은 합성 저음), 2·4박 스네어, 고스트 노트와 8분 하이햇 추가.
- 현악 주제 강박 강조, 호른 더블링 확대, 배경 패드 감소로 주선율을 전면에 배치. 현재 미리듣기 Artifacts/Audio/BattleDrums, 백업 BeforeBattleDrums. 전투곡만 변경.

### 2026-09-06 — 비트를 유지하는 전투 테마 후보

- C단조의 강한 반복음·상행 응답으로 새 멜로디 작곡. 조용한 도입/하프 중간부를 없애고 전 구간 큰북·팀파니·8분음표 현악 반주 유지. 짧은 주제 음은 spiccato 샘플로 발음 선명도 보강.
- 150 BPM/76.8초. RMS -14.30 dBFS(직전 -15.36), 샘플 피크 -2 dBFS. 실제 게임 게인 유지. 현재 미리듣기 Artifacts/Audio/BattleAssault, 이전 곡 BeforeBattleAssault.

### 2026-09-06 — 전투곡 멜로디 완전 재구성

- 직전 작업은 음색만 교체한 것이어서 선율이 같았음. 이번에는 기존 A/B·화성 배열을 모두 제거하고 D단조 12마디 현악 주제, 독립 8마디 하프 중간부, 호른 대선율로 새로 작곡.
- 셋잇단 분할의 질주 리듬과 새 베이스 패턴. 도입 0초 → 주제 6.4초 → 중간부 25.6초 → 절정 38.4초 → 확장 57.6초 → 마무리 64초. 150 BPM/76.8초 및 실제 악기 샘플 유지.
- 현재 미리듣기 MelodyRebuilt, 직전 곡 BeforeMelodyRebuilt. 전투 WAV만 교체. RMS -15.36 dBFS, 샘플 피크 -2 dBFS, 양 끝 값 차이 0. 총성 해시·BGM 메타 동일 확인.

### 2026-09-06 — 전투곡 실제 악기 샘플로 음색 개선

- 합성 현악/호른을 CC0 VSCO 2 CE 실제 악기 샘플로 교체. 하프·큰북·팀파니, 현악 복수 짧은 테이크, 미세 강약/타이밍과 홀 반사 적용. A/B 구성 및 150 BPM/76.8초 유지.
- 샘플 81개를 고정 커밋과 Git blob 해시로 검증. 출처와 재생성은 Tools/Audio/ORCHESTRA_SOURCES.md. 원본은 Artifacts 아래 Git 제외; 실행은 최종 WAV만 필요.
- 전투 WAV 1개만 교체. RMS -15.43 dBFS, 샘플 피크 -2 dBFS, 루프 양 끝 값 차이 0. 현재 미리듣기 OrchestralPolish, 이전 합성곡 BeforeOrchestralPolish. 최종 음색 청취 채택 대기.

### 2026-09-06 — G36 효과음 후보 / 판타지 멜로디 교체

- 로열 총성 3개를 CS279의 CC0 G36-E Fire 공개 OGG에서 편집. 0.18초, -3 dBFS, 기존 게인·연사 설정 유지. 출처·압축 원본 한계·정규화 디코딩과 해시를 FIREARM_SOURCES.md에 기록.
- 전투 BGM은 4마디 반복에서 독립 8마디 A/B로 변경. 호른 주제와 현악 응답, 쉼표와 긴 음, 새 화성을 적용. 150 BPM/76.8초, RMS -15.21 dBFS/샘플 피크 -2 dBFS.
- 참고 영상은 모르반·크로우 크루아흐 제목 확인까지이며 직접 청음·채보·샘플링하지 않음. 미리듣기 FantasyMelodyRework, 이전 M4/전투곡 BeforeFantasyMelodyRework 모두 Git 제외. 기존 WAV 4개만 갱신, 메타 GUID 유지.
- 검증: Unity 에셋 갱신 및 AudioPolishTests 10/10 통과. 메타 4개 동일·준비/보스/결과 BGM 3개 해시 동일·런타임 WAV 56개 확인. 전체 git diff의 기존 폰트/씬 공백 경고는 이번 오디오 변경 범위 밖으로 유지.

### 2026-09-06 — M4 계열 사격음 / 판타지 전투 편곡

- 로열 전용 총성을 CC0 라이브러리의 AR-15/M4 표기 D_32P 녹음으로 교체. 서로 다른 두 발과 첫 발의 마이크 배합 변형을 3종으로 사용한다. 기존 AK 에셋은 BeforeFantasyRework에 백업했다. 출처와 새 원본 해시는 FIREARM_SOURCES.md 갱신.
- 전투 BGM을 현악·호른·류트풍 발현·가죽 북·홀 잔향의 코드 합성 편곡으로 바꿨다. 전자 킥·하이햇·FM·박자 딜레이·사이드체인 제거. 150 BPM/76.8초, 테마·빌드·복귀 유지. 마스터 RMS -14.95 dBFS, PCM 피크 -2 dBFS.
- 검증: Unity 임포트 후 오디오 EditMode 10/10 통과, WAV 피크·유한값·전투 루프 경계 검사. 준비/보스/결과곡 해시 동일. 현재 미리듣기 FantasyRework와 원본·백업은 모두 Git 제외. 게임 규칙·코드·씬 배선은 변경하지 않았다.
- 합성 악기의 최종 음색 및 기존 준비/보스곡과의 전환 청감은 사용자 확인 대기.

### 2026-09-06 — 총성 녹음 전환 / 전투 테마 재작곡

- CC0 The Free Firearm Sound Library의 AK-47 단발 녹음에서 세 발을 골라 로열 전용 0.18초 효과음으로 편집했다. 출처·저작자·라이선스·원본 해시를 Tools/Audio/FIREARM_SOURCES.md에 남겼다. 유튜브 음원은 사용하지 않았다.
- 전투 BGM에 4마디 문답형 테마와 6구간 구조를 도입했다. 25.6초 숨 고르기, 32초 빌드, 38.4초 테마 복귀. 150 BPM/76.8초와 RMS -14 dBFS 유지, 기존 준비·보스·결과 BGM은 해시 대조로 동일함을 확인했다.
- 검증: Unity 재임포트, 오디오 EditMode 10/10 통과, 새 WAV 유한값·피크 및 전투곡 루프 경계 확인. 단발/6·9.6·13.5발 연사와 전체 테마·혼합 미리듣기 생성. 오프라인 미리듣기는 실제 Unity 전투 녹음이 아니며 청취 채택은 사용자 확인 대기.
- 이번 런타임 변경은 기존 WAV 4개(전투곡 1 + 라이플 3). 원본 다운로드·미리듣기·이전 버전은 Git 제외 Artifacts에 보관. 게임 규칙·씬 배선·다른 효과음은 변경하지 않았다.

### 2026-09-06 — 로열 전용 라이플과 현대적 다크 BGM

- 로열 소버린에 FireRoyalRifle 큐를 enum 끝에 추가하고 UnitDefinition.Id로 선택한다. 기존 Multi 유닛 소리는 유지했다. 0.155초 파열·몸통·볼트 3변형, 0.04초 DSP 게이트, 게인 0.24로 ★3의 초당 13.5발에 대응한다.
- BGM 4곡을 미드베이스 싱코페이션·단조 FM 훅·클랩/햇·8마디 필로 재작곡했다. 기존 150 BPM·76.8/38.4초 그리드와 씬 볼륨 0.55를 유지했다. 신호 수치와 재생성 순서는 AUDIO_REQUEST·Tools/Audio/README 참고.
- 사용자 AK-12 링크는 제목·영상 화면만 확인했으며 청음 비교·음원 추출은 하지 않았다. 원본 샘플 없이 NumPy 합성한 후보이며 최종 음색 채택은 사용자 청취 대기.
- 검증: 변경 어셈블리 컴파일 확인, 오디오 EditMode 10개 통과, 런타임 WAV 56개 전부 씬 GUID 참조 확인. 새 WAV 유한값/PCM 피크 및 BGM 루프 경계 검증, 오프라인 단발·성급별 연사·BGM 혼합 미리듣기 생성. 테스트는 열린 씬에 의존하지 않도록 수정 후 재실행했다.
- 이전 음악·미리듣기는 Git 제외 Artifacts/Audio/BeforeModernCombat·ModernCombat에 보관. 카드 딜 1회 및 기존 카드·보상음은 유지. 커밋·푸시 없음.

### 2026-09-06 — 오디오 산출물 Git 정리

- 아직 추적되지 않은 Artifacts의 이전 오디오·미리듣기·검증 결과·UI 캡처를 `.gitignore`로 제외했다. 193개 파일 약 446.5 MiB는 로컬에 유지한다. 문서의 Artifacts 경로는 로컬 작업 자료이며 새 clone에는 포함되지 않는다.
- 현재 런타임 오디오 53개와 `.meta`, 재생성 Python 소스는 커밋 대상으로 유지한다. `_v2`·`_v3`는 실제 재생 변형이므로 삭제하지 않는다. Python 캐시와 개인 도구 설정도 제외했다.
- 검증: 제외 규칙과 런타임 에셋/생성 소스의 비제외 여부 확인. 게임 파일 삭제·Git 이력 변경·커밋·푸시는 하지 않았다.

### 2026-09-06 — 운빨존많겜 참고 UI/UX 개편 기획

- 사용자 제공 쇼츠의 소환 버튼·비용·재화 배치·획득 피드백을 관찰하고, Unity 교체 단계 캡처와 CardView/RoundScreen/BoardScreen/ShopScreen을 대조했다. 영상 자막에 가린 정보와 미확인 조작은 추정하지 않았다.
- DESIGN §15에 단계별 액션 영역, 카드 상태, 보드 목적지 구분, 상점 선택·구매 분리, 비활성 사유, 공통 시각 규칙과 터치 검증을 추가했다. POLISH 최우선 작업을 UX-01~08로 재정렬했다.
- 미구현 기획만 반영. 게임 규칙·UI 코드·씬 편집 없음. 화면 확인을 위해 진입한 Play Mode는 종료했다. 휴대폰 터치·전체 런 검증은 후속 구현 단계에서 수행한다.

### 2026-09-06 — 현재 구현 기준 기획 발전안

- DESIGN §14에 위협 예고·선택 안내·결과 비트·Act 진행·모바일 중단 복구를 검토안으로 구체화했다. 기존 규칙과 미구현 제안을 구분하고 구현 순서·사람 플레이 관찰 기준을 정리했다.
- RoundScreen/CombatScreen/GameFlowController/RunStats와 Stage_1·Economy 에셋을 대조했다. 상태 요약의 이전 라이프 12/5를 실제 20/3으로 수정하고 오디오 상태를 갱신했다.
- 문서만 변경. 실제 플레이 관찰·게임 동작 변경·새 테스트 실행은 하지 않았다.

### 2026-09-06 — 카드 딜 효과음 단일 재생

- 새 손패의 카드 수만큼 반복하던 딜 코루틴을 제거하고 Exchange 진입 시 한 번 재생한다.
- 딜 3개 변형을 0.38초의 연속 카드 펼침·안착 소리로 교체하고 중복 재생 방지 간격을 0.45초로 설정했다. 미리듣기도 단일 재생으로 맞췄다.
- 검증: Unity 컴파일 오류 없음, 오디오 EditMode 9/9 통과. WAV 해시 비교에서 딜 3개 외 BGM·효과음은 동일함을 확인했다.


### 2026-09-06 — 오디오 2차: 종이 질감·절제된 보상·하프타임 BGM

- 사용자 피드백: 카드가 실제 덱을 만지는 질감이 아니고, 음악이 올드하며, 머지/라운드 차임이 저렴하게 들림.
- 카드 4종(선택/딜/플립/교체)을 종이 마찰·불규칙 섬유·카드 휨·착지 레이어로 다시 합성, 각 3변형. 실사 녹음은 아님.
- CardView 버튼에 겹치던 공용 클릭음을 제외. 카드 피치 랜덤화 제거, 변형 클립만 교대.
- BGM 4곡 재작곡: 레트로 톱니파 리드와 16분 아르페지오 제거. 150 BPM 하프타임 드럼, 싱코페이션 서브베이스, 절제된 현 질감/공간음. 기존 길이·박자 그리드 유지.
- 머지/족보/종료/보상 스팅어는 상승 벨 대신 짧은 마찰-클로저-따뜻한 저역 잔향으로 교체. 웨이브 클리어 0.32초, 머지 0.36/0.58초. 자주 나는 큐의 게인을 낮춤.
- 현재 31큐 / SFX 파일49 + BGM4. V2 교체·추가33파일, 이전 총성 등은 유지.
- 생성기 Tools/Audio/refine_table_audio.py. 이전 버전 Artifacts/Audio/BeforePolishV2, 새 파일/수치검증/미리듣기 Artifacts/Audio/PolishV2.
- V2 음색의 최종 채택 여부는 사용자 청취로 결정. 아래 1차 기록의 차임/사이드체인 설명은 구버전 이력.


### 2026-09-06 — 코드 합성 오디오 전체 패스

- Docs의 50웨이브·5패턴 자동 전투·포커/머지/보상 구조와 총성/BGM 반려 피드백을 기준으로 재작업.
- SFX 31큐 / 파일 41개(총성 5종 ×3 변형), BGM 4개. 외부 샘플 없는 재현 가능한 NumPy 합성. 원본 파일은 Artifacts/Audio/BeforePolish에 유지.
- 계획/전투/보스 150 BPM·76.8초 + 결과 38.4초. 박자 위치 유지 크로스페이드와 중요 스팅어 덕킹. BGM 스트리밍 임포트.
- 전투/피드백 음원 풀 분리·DSP 쿨다운·합산 게인 제한. 5패턴 발사/특수 착탄, 보스 등장/처치, 족보 3단, 판매/유지 보너스/상점/조커, 승패 스팅어 연결. Game/Poker 규칙 변경 없음.
- EditMode **229/229 통과**(오디오 테스트 9개 포함), WAV 45개 형식·피크·루프 경계 자동 검증. 최종 청취 품질과 모바일 기기 믹스는 별도 확인 대상. RMS를 LUFS로 주장하지 않음.
- 플레이 검증: 계획→일반 전투→보스 음악, 800회 몰아친 전투음 요청 후 보상음 유지, 개발 스킵으로 50웨이브 종료 후 결과 음악/클리어 핸들러 확인. 편집 중 리로드로 개발 스킵 초기화 예외가 한 번 있었고, 새 플레이 세션에서는 재현되지 않음. 검증 후 편집 모드로 복귀.
- 재현 절차: Tools/Audio/README.md. Unity 메뉴 Tools > Poker Defense > Apply Audio Polish로 임포트·씬 큐 매핑 재적용.

### 2026-09-02 — 화면 상단 레이아웃 Phase A (DESIGN §7-4, [POLISH.md](POLISH.md) 1번)

- **문제:** HUD(`LifeLabel`·`WaveLabel`)가 화면 y≈99%(트랙 상단 밴드 위)에 배경 없이 떠 있어 라이프 텍스트가 좌측 잘림 + 안 읽힘. 보드(월드 y 2.4~4.4)가 화면 68~83%에 몰려 상단이 답답했다.
- **HUD 바 신설:** `RoundScreen` 안에 `HudBar`(상단 고정, 반투명 다크 패널, 높이 150px) 추가. 기존 `LifeLabel`·`WaveLabel`을 이 안으로 재배치(좌/우 정렬)하고 `ChipLabel`·`JokerLabel`(조커 0이면 비활성) 신규 — 넷 다 아이콘+숫자 위젯(`♥`·`◆`·`★`, `CardText`처럼 이미 커버되는 글리프만 사용). `CombatScreen`에 필드 3개 추가, `UpdateLabels()`가 항목별로 텍스트를 씀(라운드는 `N/TotalWaves`로 변경, 총 웨이브 수가 보이게).
- **카메라 리프레임:** `Main Camera` Y 0 → **1.0**. 월드 지오메트리·사거리·적 경로는 전부 불변이라 **밸런스 영향 없음** — 순수 화면 구도 값. `Background`(월드 (0,0) 고정, 화면 꽉 채움)도 Y 1.0으로 같이 옮겨 상단에 틈이 안 생기게 동기화.
- **함정 재발:** `GameObject.Find("Background")`가 루트 `Background`가 아니라 `Board/Slot13/Background`(슬롯 타일 자식, 같은 이름)를 잡아 그 슬롯 위치가 튐 — 반복 함정 표에 있는 이름 충돌 패턴. `SceneManager.GetActiveScene().GetRootGameObjects()`로 루트만 걸러 잡고, 슬롯 쪽은 `localPosition = zero`로 복구. HUD 위젯 3개(라이프·Chip·조커) 박스가 처음엔 겹쳐 배치돼(28~168 / 178~318 / 300~420) 재배치(24~174 / 190~340 / 356~476, 16px 간격)로 수정.
- **검증:** 플레이 모드 스크린샷으로 확인 — HUD 안 잘림·안 겹침, 보드가 HUD 바 아래로 내려와 트랙 상단에 여백 확보, 배치→전투 진입까지 라이프·Chip 갱신 정상(전투 중 스크린샷: 라이프 20, Chip 3, 조커 위젯 비활성). **EditMode 214/214.** 씬 저장 후 TMP 폰트 아틀라스 churn(반복 함정) 발생 → `git checkout`으로 되돌림.
- **Phase B (아트, 2026-09-02):** 세 장을 새로 교체·추가.
  - `TrackLoop.png` 교체 — 밋밋한 자갈 링 → 횃불 켜진 전투 피트(코너 화로 글로우·배너·핏자국). 크기·PPU 동일이라 `.meta`·씬 무변경. 적 이동 경로(밸런스 락)가 보이는 돌 레인 위에 정확히 얹히는 것 확인. 투명 창이 슬롯 그리드보다 살짝 작아 연석이 바깥 슬롯을 ~20~54px 덮지만 슬롯 타일(sorting 0)·유닛(3+)이 위에 그려져 안 보임.
  - `ArenaDais.png` 신규 — 팔각 2단 석재 단상 + 청록 마법진 이음새. `Board/ArenaDais`, localPos 0, PPU 250(월드 6.0×4.2), **sortingOrder -15**(Background -100 앞, TrackLoop -10 뒤). 임포트 자동 `.meta`가 Default·PPU 100·NPOT 켜짐으로 나와 수정(반복 함정).
  - `SlotTile.png` 교체 — 불투명 회색 슬래브 → **석재 소켓 프레임**(바깥 테두리만 불투명, 중앙 알파 0, 네 모서리 청록 젬). 이걸로 단상의 발광 이음새가 칸 사이로 비쳐 중앙이 "밋밋한 회색"에서 "마법 소환진 위 그리드"로 바뀜. 크기·PPU 동일이라 씬 무변경. 배치 명료도 유지(프레임이 칸을 정의). `SlotHighlight`(배치 가능 흰 α0.22)는 조금 옅어 보이나 유지 — 안 보이는 수준은 아님.
  - 검증: 플레이 모드 스크린샷(빈 칸·유닛 배치·배치 단계). EditMode 영향 없음(씬·아트만).
- **남음(당시):** POLISH.md 성공·실패 연출부터 → 2026-09-05에 핵심 둘 착수·완료(아래 항목). §7-4는 사실상 마무리(트랙 여백을 아레나·단상으로 연출 완료).

### 2026-09-06 — 오디오 패스 착수: 에셋 요청 목록 ([POLISH.md](POLISH.md) 1번)

- 코드에 `AudioSource`·`AudioClip` 참조 0건 = 완전 무음 확인.
- [AUDIO_REQUEST.md](AUDIO_REQUEST.md) 신설 — 생성형 AI로 뽑을 SFX 31종 + BGM 4곡. 공통 스펙(포맷·라우드니스·루프·파일명·라이선스), 분류별 트리거·길이·"성격"(=생성 프롬프트 씨앗), 우선순위 3단(1순위 최소 세트 13개). ART_REQUEST와 같은 순서 — **에셋 스펙만**, AudioManager·믹서 배선은 에셋 도착 후 별도.
- 발사음은 유닛별이 아니라 **공격 패턴 5종(연사·강타·다중·범위·관통)별**로 잡음. 머지 차임은 ★2·★3 두 단계. 족보 스팅어는 티어 3단(low/mid/high), high는 POLISH 2번 골드 플래시와 동기화.
- **1순위 세트 반입·임포트 (같은 날):** `Assets/_Project/Audio/`에 WAV 13개. 스펙 대비 검토 — 48kHz/24-bit, SFX 모노·BGM 스테레오, 길이 전부 목표치 일치, 피크 −1.0 dBFS 정규화 완료. 임포트 설정: SFX는 `DecompressOnLoad`/`PCM`/preload(짧아서 디코드 비용 0), `bgm_combat`은 `Streaming`/`Vorbis` q0.7/백그라운드 로드. 루프 이음매·라우드니스(LUFS)는 헤더로 검증 불가 — 배선 후 실제 재생으로 확인 필요.

**배선 (같은 날):**
- `Scripts/UI/AudioManager.cs` 신설 — 씬 싱글턴(`Instance`), Music/Sfx AudioSource 2개, `Play(Sfx)` enum API + `musicVolume`/`sfxVolume` 인스펙터 값. **믹서 안 씀**(2곡·13클립 규모엔 과함). Awake에서 씬의 모든 `Button.onClick`에 공용 클릭음을 걸고, `GameFlowController.HoldBonusEarned`만 직접 구독(스크린에 안 걸리는 이벤트).
- 씬: Main Camera에 `AudioListener`(0개였음), `AudioManager` GameObject + Music/Sfx 자식. 클립 13 + `flow` 참조 배선.
- 트리거는 **Game 레이어 무변경** — 기존 UI 스크린 핸들러에서만 호출. `CombatScreen`(웨이브 클리어/라이프 손실/발사(Rapid만)/피격/처치, 스팸 방지 0.05초 게이트), `BoardScreen.OnPlaced`(배치/머지 ★2·★3), `RoundScreen`(축포 문턱에서 `hand_high`, 새 손패에 카드 딜 5연타 스태거).
- 임시 매핑: 발사음은 Rapid 패턴만, BGM은 계획 단계에도 `bgm_combat`, 조커 성급업은 머지 차임 재사용. 상세는 [AUDIO_REQUEST.md](AUDIO_REQUEST.md).
- 검증: **EditMode 220/220.** 플레이 모드 — BGM 루프 재생 확인, 전투 1웨이브 완주(타임아웃, 라이프 20→17) + 다음 라운드 진입까지 콘솔 에러 0. 씬 저장 후 폰트 아틀라스 churn → `git checkout`.
- **1차 청취 피드백 반영 (같은 날):** 효과음이 커서 `sfxVolume` 1.0→0.65, `musicVolume` 0.45→0.4(스크립트 기본값 + 씬 둘 다). `sfx_fire_rapid`(장난감 같음)·`bgm_combat`(비트 약함) 재요청 대상 — 성격 설명을 [AUDIO_REQUEST.md](AUDIO_REQUEST.md) §2.3·§3 피드백 박스에 구체화.
- **2차 청취 피드백 (같은 날):** 다듬은 서술로 재생성한 `sfx_fire_rapid`·`bgm_combat` **둘 다 다시 반려**(총성 여전히 장난감, BGM "올드함"). 서술 다듬기로는 한계 → 접근 전환을 [AUDIO_REQUEST.md](AUDIO_REQUEST.md)에 기록: **총성은 AI 생성 포기하고 실사 라이브러리(freesound CC0 등)에서 조달**, **BGM은 장르 태그+레퍼런스 아티스트+배제 키워드**로 프롬프트 재구성. 코드·배선 변경 없음.

### 2026-09-05 — 보드 유닛 자리 교환 (DESIGN §9.4)

- **문제:** 유닛을 고른 상태에서 이미 다른 유닛이 있는(머지 불가능한) 칸을 누르면 그 칸의 hitbox가 꺼져 있어(`UnitSlotView.SetInteractable`) 클릭 자체가 무시됐다 — 재배치하려면 일단 빈 칸으로 옮기고 원래 유닛을 다시 옮기는 두 단계가 필요했다.
- **해결:** `GridBoard.TrySwapSlots(from, to)` 신설 — 두 슬롯 다 유닛이 있어야 하고(`from == to`나 어느 한쪽이라도 비었으면 거부) 머지 가능 여부는 따지지 않는, `TryMoveSlot`과 대칭인 순수 자리 교환. `PlacementController`에 같은 패턴의 래퍼(`BoardChanged` 발행) 추가. `BoardScreen.OnSlotClicked`의 분기를 "빈 칸→이동 / 머지 가능→머지 / 그 외→교환" 3단으로 확장(기존은 "빈 칸→이동 / 그 외→머지"뿐이라 머지 불가 조합은 조용히 막혔음). `Refresh()`의 하이라이트 로직도 "이동·머지 가능한 칸만" → "자신 외 전부"로 단순화 — 이제 유닛을 고르면 어느 칸을 눌러도 뭔가는 일어난다.
- **검증:** `GridBoardTests`에 6개 테스트 추가(정상 교환·점유 칸 수 불변·자기 자신 거부·빈 칸 섞임 거부·범위 예외 2종). 플레이 모드에서 `BoardScreen.OnSlotClicked`를 리플렉션으로 직접 호출해 실제 클릭 경로 확인 — 머지 불가 조합(원페어 유닛 vs 하이카드 유닛)은 자리 교환, 머지 가능 조합(★1 페어 유닛 두 기)은 기존대로 머지가 우선 적용됨을 둘 다 확인. **EditMode 220/220.**
- DESIGN §9.4 보드 행동 목록에 5번(자리 교환) 추가, 이하 번호 재정렬. "다섯 행동이 한 선택으로 흡수된다" → "여섯 행동"으로.

### 2026-09-05 — 성공·실패 연출 핵심 둘 (DESIGN 무관, [POLISH.md](POLISH.md) 2번)

- **웨이브 실패 연출:** `CombatScreen`에 전체 화면 붉은 플래시(`FailFlash`, 알파 0→0.4→0, 0.45초)와 라이프 라벨 펀치 스케일(사인 곡선, 1→1.4→1, 0.35초) 추가. `OnCombatFinished`에서 `TimedOut`이고 실제 라이프 피해(`LifeDamageFor` > 0)가 있을 때만 발동 — 피해 0인 타임아웃(이론상 없음)까지 번쩍이지 않도록 가드. `LifeDamageFor`(§5.4)가 계산만 하고 화면엔 안 보이던 것을 해결.
- **상위 족보 축포:** `RoundScreen`에 전체 화면 골드 플래시(`CelebrateFlash`)와 배너(`CelebrateStamp`, "{족보명}!\n{유닛명} 소환") 추가. `HandRarity.RankOf(result.Category) >= RankOf(Straight)`일 때 `ShowResult`에서 발동 — 팝인(0→1.15배, 0.18초) → 정착(1.15→1배 + 플래시 페이드, 0.35초) → 유지(0.55초) → 텍스트 페이드아웃(0.35초). 사운드 스팅은 §1(오디오 패스)이 아직 없어 보류. 교체 단계 재진입 시 배너·코루틴을 강제 정리(빠른 다음 라운드 진입에 이전 배너가 남는 것 방지).
- **범위에서 제외:** §2 표의 나머지 셋(피격 피드백·화면 흔들림·보스 등장)은 사운드 없이는 체감이 제한적이라 §1과 묶어 나중에.
- **검증:** `DevForceHand`로 스트레이트·백스트레이트 등 강제 확정 + `Time.timeScale` 저속화(0.05)로 애니메이션 프레임 단위 스크린샷 확인 — 골드 플래시·배너 팝인 정상. 실패 플래시·라이프 펀치는 리플렉션으로 코루틴 직접 실행해 확인(레드 플래시 정상, 예외 없음). **EditMode 214/214.** 씬 저장 후 TMP 폰트 아틀라스 churn 재발 → `git checkout`으로 되돌림.

### 2026-08-30 — M12 착수: 딜러 특전 → 카드 상점 (계획)

- **결정:** 상위 족보(스트레이트+)에 단판 도달이 사실상 불가능하고, 노리면 교체를 쏟아 유지 보너스가 무너진다. 특전은 4번째 선택이 죽어 있다(§9.5). **미지근한 두 시스템을 카드 상점 하나로 합친다.** 설계 전문 DESIGN §13.
- **핵심 규칙:** 상점 카드로 손패를 고쳐도 자리별 교체권·유지 보너스를 안 깎는다. Chip을 상점에서 미리 지불해 손패를 개선하는 경로 — 이게 아니면 상점이 "비싼 교체"라 무의미.
- **범위 좁히기:** 경제 특전 4종은 상점이 대체. 전투 특전 2종(신병 훈련·고참)은 유닛 ★ 계수에 흡수 후 재밸런싱으로 확정 → 전투 재밸런싱을 "경제 특전 → 상점"으로 축소.
- **검토에서 사용자 제안을 수정한 점:** ① "완전 랜덤 카드" → "제시된 4장 중 선택"(결정이 남게). ② "보유 카드는 덱에서 빠짐"은 라운드마다 새 셔플이라(§3.2) 확률 영향 미미 — 테마용으로만 유지, 상한 3장. ③ 밸런스와 한 묶음(기존 "B" 재검증을 M12에 병합). ④ dev 토글로 특전과 병행 프로토타입 → 플레이테스트 후 특전 제거 확정.
- **단계(DESIGN §13.7):** RoundContext 리팩터(잠금↔교체카운터 분리, `PlaceHeldCard`, 덱 제외) → StageContext 보유카드+상점 롤 → 상점 UI+트레이 → dev 병행 플레이테스트(go/no-go) → 특전 제거 → 재밸런싱.
- **Phase 1 완료:** `RoundContext`에 `exchangedCount`를 새로 두어 자리 잠금(`locked`)과 분리 — `UsedExchanges`가 이제 이 카운터를 본다(상점 배치는 자리를 잠그되 안 셈). `PlaceHeldCard(index, card)` 추가. `RoundContext(seed, heldCards)`·`Deck(seed, excluded)`로 보유 카드를 덱에서 제외. 기존 흐름 무변화(교체만 할 땐 값이 동일). 테스트 +12, **EditMode 231/231**.
- **Phase 2 완료:** `StageContext`에 보유 카드 인벤토리(`HeldCards`/`CanHoldMoreCards`/`TryAddHeldCard`/`TryRemoveHeldCard`, 상한은 ctor 인자 기본 3). `ShopOffer`(순수) 신설 — `Roll(held)`가 held 뺀 52장에서 4장 추첨(시드 결정적). `Deck.BuildCards(excluded)` static 추출해 덱·상점이 공유. `EconomyDefinition`에 `shopCardPrice 4`·`heldCardCapacity 3` + `Economy.asset`에 명시 값 + 에셋 가드 테스트(HISTORY 함정). 테스트 +11, **EditMode 242/242**.
- **Phase 3a 완료 (흐름 배선, UI 없음):** `RoundController.StartRound(heldCards)`·`PlaceHeldCard`. `StageController`가 `HeldCardCapacity` 주입, `TryBuyShopCard`(상한·Chip 체크 후 구매)·`RemoveHeldCard`·`HeldCards`. `GameFlowController`: `ShopOffer` 보유, `AdvanceRound`가 `RoundNumber % 5 == 0 && < TotalWaves`면 `shopPending` 표시 → **특전 선택이 끝난 뒤** `ResumeFlow`가 상점을 연다(`ShopCards`/`ShopOpened`/`CloseShop`). `CanDevSkip`에 `ShopCards == null` 추가. 플레이 모드 검증: 웨이브5 클리어 → 특전 → 상점 4장 → 구매(상한 3에서 4번째 거부) → CloseShop → 다음 라운드 손패에 보유 카드 안 나옴 → `PlaceHeldCard`가 `UsedExchanges` 안 올림(교체는 정상으로 셈). **EditMode 242/242.**
- **발견(내 변경 아님):** `Stage_1.asset`의 `perkReward`가 웨이브 5·10·15·20·30·40에 켜져 있다 — DESIGN §9.5의 "10·20·30·40"과 불일치(2026-08-23 "5·10·15 주기→10" 변경 때 데이터 미갱신 추정). Phase 5에서 특전 제거 시 무의미해지나, 그 전까지 상점 웨이브(5·10·…·45)와 겹친다. 겹칠 때 "특전 먼저, 상점 다음"으로 처리됨.
- **Phase 3b 완료 (UI):** `ShopScreen`(전면 패널, `PerkScreen`과 같은 구조 — `flow.ShopOpened` 구독, 진열 4장 = `CardView` 재사용, 산 카드는 선택 프레임 표시, `닫기` → `CloseShop`). `RoundScreen`: 화면 하단에 `HeldCardTray`(보유 카드 0~3장, 축소 `CardView`), 교체 단계 & 보유>0일 때만 표시. **2탭 배치:** 트레이 카드 탭 → `armedHeld` 무장(교체-선택 전부 해제) → 손패 칸 탭 → `PlaceHeldCard` + `RemoveHeldCard`. 무장 중 상태 문구 "손패 칸을 눌러 카드를 놓으세요 (교체 아님)". `Game.unity`에 `ShopPanel`(PerkPanel 복제 후 개조)·`HeldCardTray` 신규 — 씬 재직렬화로 diff가 큼(±수천 줄, TMP 머티리얼 누수는 없음 확인).
- **검증:** 플레이 모드에서 실제 버튼 클릭 경로로 전 과정 확인 — 웨이브5 → 특전 버튼 → 상점 4장 → 카드 버튼으로 구매(Chip·상한) → 닫기 → 라운드6 트레이에 보유 카드 → 트레이 탭 무장 → 손패 칸 탭 배치(`UsedExchanges` 안 오름, 자리 잠김, 인벤토리 소모) → 이후 실제 교체는 정상 카운트. **EditMode 242/242.** 폰트 아틀라스 churn은 `git checkout`으로 되돌림.
- **Phase 4 생략:** 상점이 이미 특전과 병행 동작함을 확인 → dev 토글 없이 사용자가 특전 제거를 확정.
- **Phase 5 완료 (특전 전면 삭제, 2026-08-31):** 파일 삭제 `PerkSet`·`PerkTable`·`PerkOffer`·`PerkId`·`PerkCategory`·`PerkScreen`·`PerkCategoryNames`·`PerkTests` + `PerkTable.asset`. 참조 제거 `StageContext`(`Perks`·`WaveEndChip`)·`StageController`(`perkTable`·`AddPerk`, `HoldBonusFor`는 `economy` 직결)·`GameFlowController`(`Offer`·`PerkOffered`·`ChoosePerk`·특전 분기·`ResumeFlow`/`shopPending` 인라인화)·`CombatContext`(`perks`·`AttackPowerOf` → `unit.AttackPower`)·`CombatController`·`PlacementController`·`BoardScreen`(`perkLabel`·`ShowPerks`) + Editor 툴 2개. 씬에서 `PerkPanel`·`PerkLabel` GO + `PerkScreen` 컴포넌트 삭제. **전투 특전은 흡수 안 함**(★ 계수가 공격력·속도를 둘 다 곱해 공격력만 +X%를 못 넣음) — 성급 유닛 DPS가 내려가고 Phase 6이 조정. `WaveDefinition.perkReward`는 死 필드로 남김. `PerkTests` 28개 삭제로 **EditMode 242→214/214**. 플레이 모드에서 특전 패널 없이 상점만 뜨는 것 + 조커 보상·상점 전 흐름 확인.
- **Phase 6 진행 중 (재밸런싱, 마지막):**
  - **死 데이터 정리 완료 (2026-09-01):** `WaveDefinition.perkReward` 필드·프로퍼티·툴팁 제거(읽는 코드 0건이었음), 웨이브 에셋 50개에서 `perkReward` 키 삭제(5·10·15·20·30·40에 `1`이 켜져 있어 §9.5 Joker 주기 10·20·30·40과 불일치했다 — 2026-08-23 주기 변경 때 데이터 미갱신). 코드·에셋 참조 0건 확인, **EditMode 214/214**.
  - **밸런스 재검증 (2026-09-02):** `BalanceSimRunner`로 자동 플레이 시뮬레이션 10차 반복. **50웨이브 확장(2026-08-23)이 밸런스 재검증을 못 받아 곡선 자체가 망가져 있었다** — 특전 제거보다 이게 더 컸다. 손댄 것:
    - **웨이브 곡선 전면 재조정 (Wave_11~50):** HP/s 수요(총 HP ÷ 제한시간)로 곡선을 잡음. ① W11→12 3.2배 절벽 제거(201→85). ② 보스 웨이브(15·20·30·40)는 그대로 두되 다음 웨이브를 낮춰 톱니 해소. ③ Brute 폭주(14→112마리) 완화(→~75). 목표 곡선: W11~30 62→470, W31~49 490→740, W50 결승 ~400(보스 자체가 벽). W1 오프너도 소폭 완화(Grunt 5→4). 파이썬 스크립트로 `count`만 재계산, 구성·간격·보스 마릿수 불변.
    - **라이프 모델 완화 (A):** `Stage_1.asset` 시작 라이프 12→20, `maxLifeDamagePerWave` 5→3. 클리어/시간초과가 이분법이라(중간 누수 없음) 한 번 밀리면 3웨이브 만에 게임오버였다 — 이제 ~3/웨이브로 천천히 흘린다. `StageDefinition.cs` 코드 기본값은 안 건드림(CombatTests가 의존).
    - **`BalanceSimRunner` 상점 전략 (B):** 상점을 그냥 닫던 것 → Chip 여유분으로 카드 구매(이미 든 숫자 우선, `greedy_summon`은 지원 소환 한 번치 유보) + 교체 후 보유 카드를 손패에 배치(페어·플러시 확장). `shopVisits`·`cardsBought`·`heldPlaced` 계측을 summary에 추가.
  - **시뮬 결과 (iter8 곡선):** `greedy_summon`(적극 플레이) 50/50 클리어 라이프 12~18 안정. `no_exchange`(방치) W41 사망. **`greedy`(기계적 중간 플레이)는 7런 중 4런 클리어(라이프 8·10·17·18) / 3런 W38~42 사망 — 57% 동전 던지기.** 원인: **덱 운으로 보드 DPS가 3배 요동**(같은 곡선에 "라이프 17 클리어"와 "W32 전멸"이 공존) + 이분법 라이프 모델이 이를 증폭. 웨이브 튜닝은 여기서 한계 — 보드 편차 자체를 줄이려면 포커·머지·경제를 손봐야 함(웨이브 밖). EditMode 214/214.
  - **경제 레버 실험, 둘 다 기각 (2026-09-03):**
    - `shopCardPrice` 4→3 (5런): `greedy` 클리어율 57%→80%(4/5, 라이프 17·18·18·17) — **얼핏 성공으로 보였으나 `no_exchange`를 같이 돌려보니 50/50 클리어(라이프 18, `cardsBought` 26)로 뒤집힘.** 원인: 교체를 안 하면 유지 보너스가 매 라운드 최대치라 Chip이 무제한으로 쌓이는데, 카드가 싸지니 그 Chip만으로 손패를 통째로 사서 조립 — **포커 축(교체 결정)이 무의미해짐.** M10 4번 조건 위반이라 즉시 되돌림.
    - `startingChip` 0→4 (5런, `shopCardPrice`는 4로 원복 후): 2/5 클리어(40%, die@42·44·47 / clear@17·17) — 베이스라인(57%)보다 나아졌다는 근거가 없다. 표본 5~7개로는 시뮬 자체의 런간 변동폭(같은 설정에서도 "클리어" ↔ "전멸"이 갈림 — r5a/r5b 선례)에 묻힌다. 원복.
    - **결론:** 이 경제 구조에서 단일 수치로 `greedy` 바닥만 선택적으로 올리는 손잡이가 없다 — 손을 대면 `no_exchange`도 같이 좋아지거나(포커 축 붕괴), 표본 노이즈에 묻혀 효과를 확인할 수 없다. **자동 시뮬레이션 튜닝은 여기서 멈춘다.**
  - **최종 판단:** `greedy_summon`(적극 플레이) 안정적 클리어 + `no_exchange`(방치) 안정적 패배로 **포커·경제 축은 살아있음**을 확인. `greedy`의 57% 클리어율은 "교체만 하고 소환은 절대 안 함"이라는 시뮬 정책 자체의 한계로 본다(실제 플레이어는 상황 따라 지원 소환을 섞는다). **남음: 사람 플레이테스트로 M10 4조건 최종 판정 → 통과 시 M12 종료.** 상단 레이아웃 패스(§7-4)는 완료([POLISH.md](POLISH.md) 참고).

### 2026-08-30 — 화면 상단 레이아웃 패스 1차 (DESIGN §7-4 일부)

M11 5단계가 남긴 슬롯·트랙 시각 이슈 중 큰 지오메트리 변경 없이 되는 것만 처리. 카메라 리프레임은 안 함(밸런스 재검증 이후로 미룸).

- **성급 배지 위치:** 캐릭터 정강이(로컬 y −0.345) → **머리 위(`StarTopY = 0.40`)**. `UnitSlotView.Awake`에서 `sortingOrder`와 같이 지정. 정강이에 겹치면 캐릭터 무늬처럼 읽혔다.
- **슬롯 타일 금색 톤 다운:** `SlotTile.png`의 금색 픽셀만 채도(−55%)·명도(×0.82) 낮춤 — mean (177,141,89) → (145,131,110). 돌 판·회색은 안 건드림. `Docs/tools/slottile_tone.py` 신규(카드 툴과 같은 패턴, 원본은 git). 15칸 깔았을 때 금색이 시끄럽던 것 완화.
- **상세 문구 이동:** `DetailPlate`·`PendingLabel`을 화면 top(anchoredPos y 862, 스크린 y≈1822 — 트랙 상단 밴드 + HUD와 겹침)에서 **그리드 바로 아래(y 220, 스크린 y≈1180)**로. RectTransform 2개만.
- **트랙 명도:** `TrackLoop` SpriteRenderer 색 흰색 → (0.78, 0.78, 0.80). 돌 밴드가 살짝 물러나 위에 얹히는 라벨 대비가 나아지고 보드가 조금 더 분리돼 보인다.
- **`TrackMargin 0.9`는 안 건드렸다** — 가운데 칸이 트랙에서 정확히 2.4, 최단 사거리 2.5가 여기서 나온다(반복 함정 "사거리는 자유롭게 못 내린다"). 줄이면 전 유닛 사거리 유효성이 바뀌어 밸런스 재검증 대상. "15칸이 링 안 작은 섬" 인상은 수용.
- **미해결:** HUD(라이프·라운드)가 여전히 트랙 상단 밴드 위 — 실제 렌더(어두운 톤)에서 읽히는지 확인 필요, 안 되면 라벨에 아웃라인. 카메라 리프레임(보드를 화면에서 내려 상단 공간 확보 + 라벨 5그룹 재배치)은 밸런스 확정 후 §7-4 본 패스에서.
- **검증:** 컴파일 0건, EditMode 219/219, 플레이 모드에서 8유닛 배치해 배지·톤·문구 위치 스크린샷 확인. 씬 diff 3줄(anchoredPos ×2, color ×1).

### 2026-08-30 — M11 5단계: 배경·트랙 아트 4장 + 동반 코드 완료 (M11 종료)

- 배경 / 트랙 루프 / 빈 슬롯 타일 / 슬롯 하이라이트 4장을 임포트하고 씬에 얹었다. **M11 아트 교체가 전부 끝났다.**
- **씬 신규 오브젝트 2개:** 루트 `Background`(풀스크린 스프라이트, `sortingOrder -100`, 카메라 원점) / `Board` 자식 `TrackLoop`(`sortingOrder -10`, 로컬 원점). 적 위치가 `Board` 기준(월드 0, 3.4)이라 트랙은 자식으로 둬야 동기된다. 카메라 단색 clear는 폴백으로 유지.
- **슬롯 15칸:** `Background` 자식 → `SlotTile`, `Highlight` 자식 → `SlotHighlight`. 두 자식 `localScale` 0.92 → 1.06(새 스프라이트가 6% 투명 여백을 물어 칸이 맞닿게).
- **`UnitSlotView`:** `EmptyColor` 상수 + `background` 필드 + `background.color` 대입 2곳 제거(타일이 항상 같은 그림이라 틴트가 불필요, 오히려 어둡게 깔렸다). `highlight`는 `PlaceableColor`(흰 α0.22)·`SelectedColor`(금 α0.45) 틴트를 그대로 곱한다 — 하이라이트 스프라이트를 중립 니어화이트로 뽑은 이유.
- **임포트:** 배경 PPU 126(불투명, 1080×1920의 87% 해상도지만 비율 정확). 트랙 PPU 170 — 제출본 중심선 비율이 1.53(목표 1.417)이라 왜곡 없는 균등 스케일로 흡수(좌우 도로는 정중앙, 상·하는 적이 도로 중앙보다 0.13월드 바깥 — 두꺼운 밴드가 덮음). 타일·하이라이트 PPU 1275(512로 다운스케일), 밉맵 켬.
- **후속 수정(플레이 확인 후):** ① 성급 `★`이 아트에 가려짐 — `starLabel.sortingOrder`가 씬 값 2(아트 3보다 아래)라 항상 묻혔다. `Awake`에서 6으로 올림. ② 폭 넓은 유닛(롱바렐·보머 측면 뷰 ~1.08월드)이 `ArtScale 0.85` 고정이라 타일 금테를 넘어 이웃 칸에 걸침. `FitArtToCell()` 추가 — `artFitScale = Min(ArtScale, 0.9 / 아트월드폭)`, 스프라이트 바뀔 때마다 재계산. 좁은 유닛은 0.85 그대로, 넓은 5종만 2~6% 축소해 정지 시 금테 안. 반동 스파이크(~15ms)엔 셀(1.0) 안에서 잠깐 금테를 스침. 총구 화염 배율도 `artScale / artFitScale`로 맞춤.
- **미해결(전부 DESIGN §7-4로):** ① 트랙 여백(`TrackMargin 0.9`)이 커서 15칸이 넓은 돌 링 안의 작은 섬처럼 보임 ② 슬롯 타일 금색 장식이 15칸 합치면 다소 시끄러움(톤 다운은 원본 후처리) ③ 하단 상세 문구(화면 y≈1800)가 트랙 상단 밴드와 겹침 — `detailPlate`가 뜰 땐 가려지나 안내 문구만 뜰 땐 돌 위에 얹힘 ④ 성급 배지 위치가 캐릭터 정강이(로컬 y −0.345)라 어색 — 배지로 옮길지는 씬 레이아웃 패스에서.
- **검증:** 컴파일 0건, EditMode 219/219(2회), 플레이 모드에서 롱바렐·킹·스카우트 강제 배치해 정지·반동 아트 폭(0.89·0.89·0.46월드)과 `★` 표시 확인. 씬 저장 때 TMP 폰트 아틀라스가 또 부풀어 `git checkout`으로 되돌림(두 번째 저장엔 재churn 없음).

### 2026-08-30 — M11 4단계: 카드 동반 코드 완료

- 임포트만 해 둔 카드 스프라이트 5장을 코드가 쓰게 배선했다.
- **`CardVisualSet` SO 신설** (`namespace PokerDefense.Game`, `Scripts/Game/Data/` — 스프라이트를 든 SO라 `UnitDefinition`·`EnemyDefinition`과 같은 관례). `Front` / `Highlight` / `SuitOf(Suit)`. 에셋 1개에 6장 배선.
- **`CardView`: `suitLabel`(TMP) → `suitIcon`(`Image`).** 무늬 색 틴트 제거(PNG에 구워짐), rank 색만 `IsRed` 유지. 세트는 `RoundScreen`이 `Awake`에서 `Bind(CardVisualSet)`으로 손패 5장에 물려준다.
- **씬:** 루트 `Image` → `CardFront`+Sliced, `SelectionFrame` → `CardHighlight`+Sliced, `Suit` 자식의 TMP 제거 후 `Image` 붙여 `SuitIcon`으로 리네임. `CardText.SuitOf`는 그대로(키카드 줄·전투 라벨에서 계속 씀 — `◆` 대체 유지).
- **검증:** EditMode 219/219, Play Mode에서 렌더·선택 토글 스크린샷 확인, 콘솔 0건.
- **잔여 시각 폴리시(막지 않음):** `CardFront` 4모서리 금색 플로리시가 중앙 쪽으로 다소 침범. 더 나은 테두리가 나오면 `Docs/tools/cardfront_composite.py`를 원본에 다시 돌린다.

### 2026-08-28~29 — M11 4단계: 카드 아트 5장 완료

- 카드는 52장을 개별 생성하지 않고 프레임 + 무늬 아이콘 + rank(Maplestory TMP)를 런타임 합성한다. 실제 이미지는 `CardFront` / `CardHighlight` / 무늬 `♠♥◆♣` = 5장. **뒷면은 보류** — 덱 더미·페이스다운·플립 연출이 없어 어떤 코드도 뒤집힌 카드를 안 그린다.
- **`CardFront.png` — 생성 3회 반려, 후처리로 마감.** 생성기가 "평탄 불투명 크림 면"을 세 번 다 못 뽑았다(알파 그라데이션 / 속 빈 액자). 금 테두리·플로리시만 남기고 안쪽을 파이썬으로 평탄 크림 `#F3EEDF`·알파 255로 덮었다(`Docs/tools/cardfront_composite.py`). **교훈: 이미지 생성기는 "flat opaque fill"을 신뢰성 있게 못 만든다 — AI엔 테두리·장식만, 평면 채움은 코드로.**
- **무늬 4종** — 2×2 시트 1장 → 파이썬 4등분(`Docs/tools/suit_sheet_crop.py`). 하트만 bbox 높이가 12% 작아 네 심볼 높이를 209px로 통일. 색 `♠♣ #1E1E24` / `♥◆ #CC2626`.
- **임포트:** CardFront Sprite/Single·FullRect·9-slice `spriteBorder 240`·maxTextureSize 1024, 무늬 4장 Center pivot·maxTextureSize 256.

### 2026-08-23~27 — M11 3단계: 적 7종 아트 완료

- **컨셉:** 몬스터/크리처. 유닛의 "카드 군단"(붉은 군복 + 흰·금)과 대비되는 "그림자 무리"(어두운 몸통 + 종류별 글로우 악센트). 글로우 색은 `placeholderColor`를 물려받는다.
- **`EnemyDefinition.visualScale` 추가** — `CombatScreen.CreateView`의 `BodyScale`에 곱한다. 그런트 1.0 / 스웜 0.75 / 브루트 1.35 / 레이스 1.5 / 베히모스 1.8 / 아바돈 2.3.
- **적의 "방향"은 트랙 이동 방향**(유닛은 조준 방향). 트랙 4구간(상단=우향/우측=하향/하단=좌향/좌측=상향)이라 결정적이다. `GridBoard.TrackDirection(progress)` + `EnemyDefinition.artDown/Up/Left/Right` + `ArtFor(direction)`. `CombatScreen`이 매 프레임 스프라이트를 갈아 끼운다. 유닛과 같은 2×2 시트 방식.
- **7종을 실루엣이 안 겹치게 구조로 갈랐다:** 직립 임프(그런트) / 스프린트 임프(러너) / 네발짐승(스웜) / 오우거형(브루트) / 다리 없는 부유형(레이스) / 전갈형(베히모스) / 유익종(아바돈). 방향 판정은 눈 위치 비율이 역동적 포즈에서 안 통해 사지·머리·꼬리 위치를 직접 봤다.
- **`displayName`을 고유명으로:** MiniBoss→`Wraith`, Boss→`Behemoth`, FinalBoss→`Abaddon`(화면엔 안 뜨는 값, `id`·에셋 파일명은 웨이브 시스템이 참조하는 슬롯 타입이라 유지).
- **검증:** EditMode 219/219. 그런트만 실플레이(W1에서 트랙 도는 것 확인), 나머지는 방향 로직이 그런트에서 검증됐으므로 리플렉션(`HasArt`·`VisualScale`·4방향 연결)으로 대체. 이로써 `CombatScreen`의 적 아트-없음 분기는 실사용되지 않으나 코드는 남겨 뒀다(신규 적 대비).

### 2026-08-13~23 — M11 1·2단계: 유닛 13종 아트 완료

- **스카우트 파일럿으로 절차 확정** → 나머지 12종에 적용. 생성 단위는 **유닛별 4방향 턴어라운드 시트**(여러 종을 한 장에 담으면 캐릭터끼리 붙어 자동 분할이 깨진다). 2프레임 애니메이션은 몸통·다리가 통째로 달라져 버리고 **코드 반동**으로 갔다.
- **구조 숫자가 있는 여섯**(1·2·2+2·3·3+2·4)은 실루엣으로 읽히게, **없는 일곱**(계단·산·같은 무늬 반복·원형 회전·레일·왕좌·장총)은 컨셉과 색 블록으로 갈랐다 — 종횡비는 판별에 거의 안 쓰였고 매번 실제로 가른 건 색·머리·복장.
- **총구 좌표 모델이 세 번 넓어졌다:** 방향별(어깨에 멘 총) → 쌍무기(둘) → 방향별 목록(배터리 5총구, 좌우 반전 시 정면·후면 두 점이 한 자리로 붕괴). 최종 `muzzlesSide/Facing/Back` 목록 + 발사 방식(`muzzlesFireTogether`·`muzzleRandomOrder`).
- **정렬 절충:** 무기가 한쪽으로 길면 "몸 튐"과 "무기 돌출"의 맞바꿈. 무기 끝을 ±0.46에 닿게 두고 남는 만큼만 몸을 민다(캔버스 비대칭 패딩으로 계산하는 게 재현이 쉽다). 레일거너는 자세 설계 단계에서 무기를 옆구리에 붙여 이 문제를 피한 첫 사례.
- **스트레이트를 랜서(창) → 롱바렐(장총)로 재기획.** 찌르기 궤적 이펙트가 총기에 훨씬 잘 맞았다. 판정(Pierce)·수치는 그대로. §10.4의 "유닛마다 고정 심볼 하나"에 예외 둘(스트레이트·스트레이트 플러시 — 무늬 통일성이 곧 족보). 롱바렐 반동을 크기 반동으로 되돌리며 `RecoilStyle` 분기 자체를 삭제.
- **검증:** 각 유닛 EditMode 전량 통과, 124px 판별 확인, 화염 위치를 반동 최대 순간의 아트에 점 찍어 검산.

### 2026-08-13~17 — M11: 이펙트 6장 완료

- **공용 2장**(`MuzzleEffect` 총구 화염 0.06초 / `HitEffect` 타격 스파크 0.12초) — 유닛별이 아니라 13종 공용, 스카우트 레퍼런스로 먼저 잡았다. 날아가는 총알은 안 만들었다(§5.5 즉시 히트). 총구 위치는 눈대중(가슴 +0.05)으로 30px 어긋나 스프라이트에서 직접 재서 닫았다.
- **롱바렐 전용 3장**(`BeamMuzzleEffect` 은백 8방 / `PierceHitEffect` 관통 자국 / `PierceTrailEffect` 빔 궤적) — 골격(흰 코어 + 방사)은 공용과 같고 색(불→강철)·형태(구름→바늘)·고리(원 금지, 원은 Splash로 읽힘)만 뒤집었다. 빔 궤적은 방사 대칭이 아니라 상하 대칭 + 길이 방향 균일이 조건(코드가 회전·신축시킨다). 타격 쪽 색은 무기가 아니라 **적을 보고 고른다**(진홍은 붉은 적 위에서 묻힌다 → 금·호박빛).
- **`SplashEffect` 3회 시도:** 1차 실사풍 반려(수치는 통과 — 수치 통과가 스타일 통과를 보장 안 함), 2차 십자형 축·`MuzzleEffect`와 실루엣 겹침 반려, 3차 채택(경미한 대각선 편향은 실사용 크기에서 안 보임). 임포트 PPU 1254(캔버스 폭과 같게 두면 `bounds.size`가 정확히 `(1,1)`이라 `fullScale = splashRadius × 2`가 그대로 성립).

### 2026-08-23 — 웨이브 20→50 확장 + 상위 유닛 재조정 + Splash→Multi + 유닛 에셋 rename

- **웨이브 20→50 (순수 데이터).** DevMode 실측 결과 소버린 한 유닛으로 1~20을 전부 클리어 가능했다. 원인: 상위권 유닛 불균형 + DESIGN §7의 진단(유닛은 머지로 배수 성장, 웨이브는 덧셈). 1~19는 그대로, 21~50을 공식으로 생성 — 적 HP는 공용 에셋이라 못 올려서 **개체 수 + Brute 비중 + 스폰 밀도**를 후반부일수록 가파르게(Act3 20→30%, Act4 30→40%, Act5 42→54%). 보스 웨이브는 트래시 대신 Boss/FinalBoss를 직접 쌓았다. 보스 주기 5→10, 특전 10·20·30·40 (4회째는 남은 3종 전부 제시라 선택 의미 없음 — 특전 확장은 별도 작업).
- **상위 유닛 4종 재조정** (성급 공식 `{1, 1.6, 2.25}`은 유지): 마운틴 킹 ATK 156→176, 템페스트 68→54 + `multiSpread` 4→2.2, 레일거너 128→118, 소버린 35.8→25. 쿼드캐논이 anchor. ★1 단일 DPS 곡선(8~13번)이 47.5 → 88 → 108 → 118 → 124 → 150 단조 증가로 정리됨.
- **소버린 Splash → Multi.** 아트가 자동소총으로 확정되며 "연사"에 맞췄다. 단일 대상 DPS(214.8)는 보존하고 배분만 초당 6발·3타겟으로. 앞서 **배터리도 같은 이유**(초당 2.5회 폭발이 "난사")로 Splash → Multi(3타겟). 지금 Splash는 보머 하나.
- **`multiSpread` 추가** — Multi가 사거리 넓은 유닛에서 트랙 반대편 적까지 한 발에 때리는 문제. 첫 타겟에서 이 거리 안의 적만 후보. 기존 3종은 자기 사거리와 같은 값으로 둬 동작 유지, 소버린만 1.5.
- **유닛 에셋 레거시 이름 정리** — `Unit_Champion`→`Unit_QuadCannon` 등 11종을 아트 폴더 이름에 맞춰 `git mv`(내부 `m_Name`까지). GUID 보존이라 참조는 코드 변경 없이 유지.
- **DevMode 웨이브 스킵 창** 추가 — 실제 전투 결과 처리 경로(`GameFlowController.AdvanceRound`)를 재사용, 특전 대기·게임 종료를 만나면 멈춘다.
- **밸런스 자동 시뮬레이션 (9회).** `BalanceSimRunner.cs`로 전략 3종(no_exchange / greedy / greedy_summon) × 조커 사용. 결과: greedy 평균 사망 웨이브 ≈34, greedy_summon은 편차 큼(18~완주 1/7). 사망 구간은 대체로 Act3 후반~Act4로 설계 의도와 맞음. **소버린 단독 캐리 패턴은 재현되지 않았다.** 정확한 합격선은 사용자가 직접 플레이테스트로 정하기로 함. `PerkTests`의 보스 주기 기댓값 `{5,10,15}` → `{5,10,15,20,30,40}` 갱신, EditMode 219/219.

### 2026-08-24 — 유닛 슬롯의 "아트 없음" 폴백 제거

- 유닛 13종이 전부 아트를 갖춘 뒤 `UnitSlotView`의 플레이스홀더 경로(색 사각형 + 이름 라벨)가 죽은 코드가 됐다. 항상 아트를 그리고, `HasArt == false`면 `Debug.LogWarning`만.
- `nameLabel` 필드와 `UnitDefinition.PlaceholderColor`(+ 백킹 필드)도 제거. 씬의 15개 `NameLabel` 자식 오브젝트는 안 쓰이는 채로 남아 있다(완전히 치우려면 별도 요청).
- 적 쪽(`EnemyDefinition.PlaceholderColor`, `CombatScreen` 분기)은 신규 적 대비로 남겨 뒀다.

### 2026-08-12 — M10: 밸런싱 (아트와 분리)

- **M10을 밸런싱, M11을 아트로 나눴다** — §10.4가 구조 숫자를 고정해 밸런싱 결과가 아트 사양을 안 바꾼다. 아트가 먼저면 밸런싱 한 번마다 렌더링 검증이 따라붙는다.
- **성급 배수 `{1, 2, 4}` → `{1, 1.6, 2.25}`.** 기존 배수는 공격력·공격속도에 동시에 곱해져 ★3 = 16배 — `하이카드 스카우트 ★3`(DPS 320)이 `로열 스트레이트 플러시 소버린 ★1`(162)을 이겨 §10.2가 통째로 뒤집혀 있었다. 새 배수는 DPS ×1 / ×2.56 / ×5.06.
- **유지 보너스 상한 4→3, 지원 소환 라운드당 2→1회.** 걱정은 "좋은 패가 Chip까지"였는데 실측은 반대 — 교체하면 Chip이 0이 되어 지원 소환이 20회→1회로 무너진다(Chip 획득처가 유지 보너스 하나뿐). 지원 소환 2회일 땐 유입 40기로 ★3이 10기, 파이프라인이 포커 축을 지배했다.
- **★3 목표를 1~3기 → 4~9기로.** 개수를 정하는 건 배수도 경제도 아니라 유입 유닛 수(20라운드 × 포커 20기, 손패 92%가 2종). 웨이브 20으로 늘린 이상 전제다. §12에 "보드가 ★3으로만 차지 않는가"를 함께 본다.
- 스카우트·건슬링어 사거리 2 → 2.5(중앙 3칸이 트랙에서 2.4). 시작 라이프 15 → 12(15는 11을 남기고 끝나 "지지 않는 판").
- **M10 검증 4조건 통과** (자동 진행 11회, 교체 0장/전부 교체): 잔여 라이프 9·8(목표 3~10), ★3 9·7기(목표 4~9), ★1·★2 잔존 4·6칸(목표 3+), 특전 3회 수령. 자동 진행은 판마다 ±2 흔들린다(덱·특전이 매 판 새 시드).

### 2026-08-09~10 — M6~M9: Chip 경제 / 웨이브·보스·Joker / 유닛 패턴·적 타입 / 딜러 특전

구조는 전부 DESIGN.md에 있다. 이때 정한 것 중 코드에 안 드러나는 것:

- **M6 Chip 경제.** 유지 보너스가 핵심 — `ExchangeableCount`가 곧 미사용 교체권이라 새 상태 없이 계산. `StageController` 분리(라이프·Chip·웨이브는 한 판 유지되는 같은 층위). 판매가는 성급으로만(1/2/4). **M6 검증:** 스카우트 4기 확보까지 평균 5.3라운드(M3의 13 → 목표 4~7 한가운데).
- **M7 웨이브·보스·Joker (에셋만, 코드 0줄).** 라이프 피해 상한(§5.4), 보스는 한 기만 남아도 상한만큼. 최고 족보는 `HandRarity`, 최고 유닛은 DPS. `RunStats` 분리(규칙에 관여 안 함). 재시작은 씬 리로드.
- **M8 공격 패턴 5종 + Runner·Tank.** 패턴이 바꾸는 건 "몇 기를 때리는가"뿐, 겨냥 대상은 항상 사거리 내 가장 앞선 1기. Rapid↔Heavy는 코드가 같고 오버킬 낭비로만 갈린다. Pierce는 트랙 기준, Splash는 착탄 지점 기준. `EnemyType`은 뒤에 붙였다(정수 고정). 스탯은 기준 DPS 유지하고 배분만 옮김.
- **M9 딜러 특전.** 6종이 건드리는 곳 5개를 `PerkSet` 메서드 5개로 고정, 호출부는 "지금 값이 얼마인가"만 묻는다. `PerkSet`은 `StageContext`가 소유(규칙을 바꾸므로). `AttackPowerOf`를 `UnitInstance`에 안 넣음(유닛은 스테이지를 모르는 불변, 전투 피해와 상세 표기가 반드시 같은 수여야 함). 특전마다 SO 없이 전역 `PerkTable` 1개. 화면 이름은 한글 하나(뚝심·위로금·흥정·이자·신병 훈련·고참), `PerkId`는 영문.

### 2026-08-06~09 — M0~M5: 포커 코어 / 카드 UI / 그리드·머지 / 전투·웨이브 / 라운드 루프

구조는 DESIGN.md. 이때 정한 것 중 코드에 안 드러나는 것:

- **M0 asmdef 전면 폐지.** "불가능"이라던 판단이 틀렸다 — Test Runner는 asmdef가 아니라 `nunit.framework`를 참조하는 로드된 어셈블리를 스캔하고, 필요한 참조 방향(`Assembly-CSharp-Editor` → `Assembly-CSharp`)은 predefined끼리 자동 성립. 폴더명만 `Editor`로. 포기: `[UnityTest]`·`LogAssert`·PlayMode 테스트·컴파일러의 레이어 강제.
- **M2 교체는 자리 단위로 잠근다** — 한 라운드에 여러 번, 같은 자리는 한 번. 잠긴 자리가 섞인 요청은 손패를 한 장도 안 건드리고 통째로 거부.
- **M3 설계 구멍:** 머지가 "소환 유닛 놓을 때"만이면 ★3에 영원히 도달 못 한다(소환은 항상 ★1). `TryMergeSlots(from, to)` 추가, UI는 2탭. `GridSlot`·`MergeResolver` 클래스는 안 만들었다. **저등급 편중 실측: 스카우트 4기 = 13라운드** (이후 마일스톤의 baseline).
- **M4 보드를 월드 스페이스로.** "기존 87개 테스트 무수정 통과"가 검증 조건이었다(표현만 바꾸고 규칙 코드는 안 건드림). 전투는 순수 C# `CombatContext.Tick(deltaTime)` — 고정 틱을 강제 안 하면서 테스트에서 전투 한 판을 시뮬레이션. 트랙은 `GridBoard`가 계산하는 닫힌 사각 루프, 적 위치는 진행도 하나.
- **M5 `GameFlowController`.** 자동 구간만 맡는다. 새 자동 전이는 `Result` → 다음 `Draw` 하나. `새 라운드` 버튼 제거(루프가 자동이면 어긋남만). `Combat` 진입은 `Place` 페이즈 + 배치 대기 유닛 없음 둘 다 만족해야.
- **한글 폰트.** Maplestory TTF, TMP Dynamic 아틀라스(한글 11,172자라 정적 불가). 커버리지: `♠♥♣★☆◆◇` 있음 / `♦`·`—`·`–` 없음. `CardText` 분리(키카드가 `4h`로 나와 앞면 `4♥`와 어긋났다).
- **전투 중 배치 변경을 정식 규칙으로.** 제보(포커 스킵)는 재현 안 됐으나 전투 중 보드가 전부 열려 있었다(DESIGN §1과 어긋남 → 규칙으로 올림). 먼저 막을 구멍: 이동이 공격 쿨다운을 초기화(슬롯 인덱스로 들어서 — `Dictionary<UnitInstance, float>`로). 제보 증상의 실체는 UI(고른 채 웨이브 종료 시 `selected`가 남음 → `PhaseChanged` 구독).

### 2026-09-06 — V2 음악 비트/가청성 보강
- 새 어두운 분위기는 채택, 음악이 묻힌다는 청취 피드백 반영. 계획 드럼 추가, 백비트/하이햇/전투 드럼 비중 보강.
- 음악 PCM RMS 약 +3/+2.5/+1.9 dB(계획/전투/보스), 피크 -3 dBFS 이하. WAV 유한 샘플/루프 경계 검증, Unity 에셋 재임포트. 효과음과 결과 음악 변경 없음.
- 이전 BGM: Artifacts/Audio/BeforeBeatLift. 갱신 미리듣기: Artifacts/Audio/PolishV2/Audio_Polish_Preview.wav.

### 2026-09-06 — 음악 드라이빙 그루브
- 추가 청취 피드백에 따라 하프타임 대신 매 박 킥·2/4 백비트·엇박 하이햇과 싱코페이션 베이스로 재편. 어두운 음색 유지.
- 계획/전투/보스 RMS -18.5/-14/-13, 씬 음악 게인 0.4→0.55. WAV 루프/피크 검증·Unity 임포트. BGM만 있는 비교 미리듣기 추가.
